<!-- GENERATED by _ob/scripts/sync-repo-standards.ts -- DO NOT EDIT.
     source: _DOCS/GIT_STANDARDS.md
     sections: (entire document)
     source-hash: 6bdf80806aca
     Refresh: _ob/bin/sync-repo-standards <repo>
     Rule changes belong in the source above, never in this copy. -->
# Git And GitHub Standards

## Repo Ownership (the responsibility boundary)

**The git boundary is the responsibility boundary.** An agent working in a repo
is responsible for that repo's git state and nothing else. This is what keeps a
mistake small and cheap to undo: every change is a commit in the repo that owns
the files, and a commit is the most reversible artifact there is.

- **Commit in the repo that owns the files, not the repo you started in.** If a
  task takes you into another repo, the work lands there, on that repo's branch,
  with its own message. Never version an artifact belonging to repo A inside
  repo B — it escapes A's log, so A's history and any hygiene job reading it will
  never know the artifact exists.
- Do not reach into another repo's git state on your own initiative. Rico may
  authorize a crossing ("just do x over there"); that authorization is what
  lifts the default, and the crossing is still subject to the rule above.
- A tool belongs in the repo whose subject matter it serves. A Development
  hygiene script lives in Development, even when it is invoked from elsewhere.
- Leaving work uncommitted is the expensive failure. Dirty files carry no
  author, no reason, and no timestamp beyond mtime, so they sit outside the
  audit trail that makes everything else recoverable.

## Branches

- Do not commit directly to `main`, `master`, or protected branches unless the
  user explicitly approves it.
- Inspect branch state and dirty files before edits.
- Never revert user changes unless explicitly asked.
- Use focused branches for implementation slices.
- **After a merge, fetch and branch the next task from fresh `origin/main`.**
  Every task starts from the merged remote state:

  ```bash
  git fetch origin --prune                    # refresh refs; prune deleted remote branches
  git checkout -b <type>/<slice> origin/main  # branch from the tip that includes the merge
  ```

  Branch from `origin/main` directly rather than checking out the local default
  branch first: some repos run a guard that blocks `git checkout main` outright
  ("do not switch to main/master for work"), and `fetch` + branch-from-remote
  needs no checkout, so it works everywhere and cannot leave you sitting on the
  default branch by accident.

  Continuing on the just-merged branch, or branching off a stale local `main`,
  produces a PR whose diff replays work already on `main` — it looks like new
  changes to every reviewer and to CI, and it invites the semantic conflicts
  that have no textual conflict (a cherry-picked slice silently reverting a
  neighbouring change that landed in the meantime).
- **Never commit onto a branch that already has an open PR unless the commit
  belongs to that PR.** Adding an unrelated commit silently expands the PR's
  scope after review has started. Check first —
  `gh pr list --head <branch>` — and if the work is unrelated, branch from
  fresh `main` instead.
- Verify the base before starting, not after committing:
  `git merge-base --is-ancestor origin/main HEAD` exits 0 when the branch is
  current. If it exits 1, the branch is behind and should be rebased or
  recreated before more work lands on it.
- **At most one branch per open PR or actively-worked issue.** A branch exists to
  carry one piece of work to a merge. A second branch for the same item splits
  the diff across two heads, so neither one is the thing being reviewed and the
  merge order becomes load-bearing. Before cutting a branch, check whether the
  item already has one — `gh pr list --head <branch>`, or the issue's linked
  branches — and continue on it instead. Branches with no open PR and no
  actively-worked issue behind them are finished or abandoned; they are reported
  at wrap, not left to accumulate as ambiguous heads.

### Worktrees Need A Stated Reason

**No git worktrees without asking** (Rico, 2026-08-02). The default is working
in the branch the session is already on. Opening a worktree needs *"a damn good
reason stated to Rico first"* — his words — and that supersedes the dispatch
habit of handing every worker its own worktree cut from `origin/main`.

- The reason is stated BEFORE the worktree exists, in prose, to Rico. "The
  worker needs an isolated checkout" is not a reason; it is the habit this rule
  retired. A real reason names what breaks without it — for example, that the
  live tree is running enforcement hooks and switching its branch would break
  every session currently relying on them.
- A worktree authorized for one task covers that task and nothing else. It is
  not standing permission for the next one.
- The authorized worktree is removed in the SAME session that created it, with
  `git worktree remove` — a git operation that unregisters and removes together,
  which is exactly what `rm -rf` gets wrong by stranding `.git/worktrees`
  entries. Anything worth keeping is copied out first; the worktree is never the
  durable home.
- **The Development root repo is the absolute case and is NOT covered by this
  rule.** "Development must never have worktrees" (see the section below) admits
  no stated reason, because its rationale is not workflow tidiness: a worktree
  removal takes a directory, and the Development main checkout routinely holds
  uncommitted work from several agents at once. That is stricter and it wins.
  If a task genuinely cannot run in the Development main checkout — the usual
  case being that switching its branch would break the enforcement hooks every
  live session depends on — that is a conflict to put to Rico in prose, not an
  exception to take.

- Temporary review worktrees belong under the configured temp workspace, grouped
  as `{temp_workspace}/{project-or-repo}/...`. On Rico's Mac this is
  `/Volumes/ThunderBolt/_tmp`; on cc-* boxes this is `/mnt/collab/tmp_space`.
- Do not create scratch worktrees, review checkouts, generated patches, or test
  clones directly under `/Volumes/ThunderBolt/Development`.
- Every `{temp_workspace}/{project-or-repo}` area must have an `_archive/`
  folder. Move stale temp worktrees and scratch artifacts there when they are no
  longer needed, unless the user asks to preserve them active.
- Temp workspace paths, including `_archive/`, have no lifetime persistence
  guarantee. Anything that must be retained belongs in the owning repo/project
  folder or another durable user-approved location, not in temp. A `_keep/`
  folder may be used only for short-term explicitly preserved temp state.
- Do not ask before archiving current-run temp worktrees/artifacts under the
  configured temp workspace. Ask only for durable, user-created,
  outside-temp-root, or ambiguous paths.
- MUST NOT use raw `rm -f` or `rm -rf` for temp worktree cleanup in agent
  workflows. Move stale paths into the matching `_archive/`, then rerun
  `git worktree add`.

## The Development Root Repo (weekly `wip/` rotation)

The Development root follows the same lifecycle as every other repo — branch,
work, PR, review, merge, delete, branch fresh from `origin/main`. The only
difference is that its branch is **time-boxed rather than task-boxed**, because
agents from other projects drop unrelated changes into it all week.

- The working branch is **`wip/<YYYY-MM-DD>`**, dated from the day it was cut.
- **When you change something in Development, commit it with a message.** Do not
  branch, rebase, squash, merge, push, or open a PR. Those are the weekly
  rotation's job, not yours.
- That rule is what makes the weekly close cheap: the commit log *is* the audit
  trail, written by whichever agent had the context at the time. Nothing has to
  infer intent afterwards. An agent that squashes or rewrites destroys exactly
  the granularity that makes a single bad drop revertible without losing the
  week.
- **Development must never have worktrees.** One registered worktree — the main
  checkout — and nothing else, ever. A worktree removal takes a directory and
  can destroy the only copy of uncommitted work, which is the one failure this
  system cannot undo.
- The weekly rotation reviews the week's commits, merges `wip/<date>`, deletes
  the merged branch, and cuts `wip/<today>` from fresh `origin/main`. A week
  with nothing worth merging still rotates.
- A violated invariant (worktrees present, stray branches, uncommitted files
  with no commit) is a **hard failure that gets reported, not quietly tidied**.
  It means an agent broke the rule, and which agent is the useful information.

## Commits And PRs

- Commit only related changes.
- **Commit as you work, not as you stop.** Each coherent step commits, with its
  paths named. Stopping is just the last step — it is not the moment work
  becomes committable. An agent that batches a session's worth of changes into
  one terminal commit has spent the whole session with its work outside the
  audit trail, and a commit is the most reversible artifact there is only while
  the reasons are still attached to it.
- **Stage by explicit path. Never `git add -A`, `git add .`, or `git commit
  -a`.** The staging area is shared state and is frequently not empty when you
  arrive: a sweep absorbs whatever another session or an earlier step left
  staged. On 2026-07-30 a scoped `git add CLAUDE.md` plus a commit absorbed 174
  pre-staged files in `ArmPros_Stuff`, because the staging area was not empty
  when the commit ran. Name the paths, then verify with
  `git diff --cached --name-only` BEFORE committing that the staged set is
  exactly what was intended.
- **Over-communicate in commit messages.** A commit body is read by agents, and
  what looks like too much detail for a human reader is correct for one: carry
  what changed, why, what state it leaves the system in (see the RUNNING /
  MERGED / WRITTEN / PROPOSED vocabulary in `_DOCS/CODING_STANDARDS.md`), and
  the receipts behind any claim. The subject line stays conventional and short;
  the body is where the context lives. `wip`, `updates`, and `fixes` are not
  messages — the message is what makes the commit findable later, when the
  conversation that produced it is gone.
- **An edit in a branch that is not yours commits immediately and alone.** If
  work takes you into a branch owned by another session or another agent, a
  deliberate edit there is committed by itself, in its own commit, with a
  message stating exactly why it was necessary. Never sweep another owner's
  in-flight changes into your commit, and never leave your edit sitting dirty in
  their tree for them to find unattributed. Everything else in that tree is
  REPORTED, not committed — dirty paths that are not yours are someone's
  uncommitted work, and classifying them is required before any commit lands
  (see the dirty-state reconciliation rules in `AGENTS.md`).
- **Local secret gate** -- Gitleaks is the default scanner for routine local
  commits and pushes. The global hooks are
  `~/.config/git/hooks/{pre-commit,pre-push}`, selected by the global
  `core.hooksPath`. They must fail closed when Gitleaks is unavailable and use
  full redaction (`--redact=100`).
- Pre-commit scans staged changes only. Pre-push scans commits reachable from
  each pushed ref that are not already present on known remote refs. Do not run
  a full-history scan on every commit or push.
- **GitGuardian boundary** -- Keep GitGuardian for managed public-repository
  monitoring and deliberate targeted scans. Do not use quota-backed `ggshield`
  as the routine local hook scanner or run broad private-repository history
  scans through it. GitGuardian API quota is workspace-wide and rolling over 30
  days, not a calendar-month bucket.
- **Private-repository depth** -- Gitleaks protects the developer commit/push
  boundary. The central private-repository target is scheduled Betterleaks
  incremental scans plus a monthly full-history scan on a dedicated scanner
  LXC. Until that service is deployed and verified, do not claim central or
  monthly private-repository coverage.
- Gitleaks is accepted as equivalent for the narrow local hook enforcement job,
  not as feature-equivalent to the GitGuardian platform. A clean synthetic
  smoke test proves hook wiring, not detector parity across every secret type.
- Never bypass secret hooks in a durable repository. `--no-verify` is allowed
  only inside an isolated temp test repository when explicitly proving that the
  pre-push gate independently blocks a synthetic finding; the test remote must
  also remain local and disposable.
- One issue does not imply one PR. Group related issues into the smallest
  coherent PR by default. Split them only when concerns, risk, ownership,
  deployment order, reviewability, or diff size make the combined PR unsafe or
  unwieldy, or when Rico explicitly requests separate PRs.
- Before a PR is considered ready, verify the remote PR branch contains the
  intended commits.
- PRs must link relevant issues and include validation evidence.
- A queued self-hosted GitHub Actions job is not passive `CI pending`. If a job
  is queued before runner pickup beyond a short grace period, diagnose runner
  routing immediately: exact `runs-on`, repo runner labels, and canonical
  sibling/source repo workflow patterns.
- Posting a PR comment is not a fix. Fixes must land in code, docs, tests, or be
  explicitly waived.
- Generated reports or artifacts belong in the owning repo's visible expected
  folder only when they are intended project artifacts. Otherwise put temporary
  output under `{temp_workspace}/{project-or-repo}/...` and archive it after
  use.

### PR And Issue Content Hygiene (Rico, 2026-08-11)

Adopted from a measured external audit of agent-filed PRs (t3.gg fleet
conventions); the failure modes it fixes are ours too. These rules govern the
CONTENT of PRs, issues, and review responses -- the mechanics above still
apply.

- **Titles say why the change matters, in the repo's own convention.** Read
  recently merged PRs and `git log` for the convention before writing one.
  Concise and human-readable beats mechanically accurate:
  - BAD: `perf(server): negotiate permessage-deflate on the websocket`
  - GOOD: `perf(server): cut websocket frame size by 70%+ with gzipping`
- **Descriptions open with the problem, in the requester's original words,
  then the solution. Never lead with an implementation inventory.** A reader
  who was not in the session must be able to say in one sentence why this PR
  exists:
  - BAD: `Removed implicit workspace carry-over from every "new thread" entry
    point. New threads inherit only the project from context; branch,
    worktree, and env mode always come from the configured defaults. Deleted
    buildContextualThreadOptions...`
  - GOOD: `My "new worktree" default was ignored when starting new threads on
    existing worktrees. Super unintuitive. Now your preferences always
    apply.`
  When an agent notices a filed body leading with inventory, that is a
  correction: capture the literal bad/good pair here or in the owning repo's
  standards, because agents learn examples better than rules.
- **Open real PRs, not drafts,** so the review bots and CI run. A draft needs
  Rico's explicit request.
- **Attribution tag on every agent-authored PR body and issue/PR comment.**
  End PR bodies with the model/harness blurb (the Co-Authored-By footer
  already carries this for commits). Comments posted on Rico's behalf open
  with the tag so nobody mistakes agent words for his:
  `[<model-slug>] responding on behalf of Rico` -- then the actual reply.
- **Bot review feedback: verify, then fix or dismiss with a written reason.**
  Verify every bot finding against the source before changing code.
  Distinguish repo failures from infrastructure flakes. A finding not worth
  addressing gets a reply stating why, and the comment resolved -- silent
  dismissal and silent compliance are both wrong.
- **Review feedback never expands the PR beyond the original goal.** Address
  real shortcomings; scope creep dressed as responsiveness is how a
  one-problem PR triples. Out-of-scope findings become issues (see
  file-issues-on-discovery), not commits.
- **If nothing has changed, stay quiet.** No filler comments on a PR being
  monitored. Act only on checks and comments newer than the latest push.
- **Issues get the same title discipline plus routing:** problem-first title,
  body opens with what is broken or wanted and why it matters, then evidence.
  Label and milestone per the owning repo's scheme (`_DOCS/BOARD_FIELDS.md`
  for boards); an unlabeled drive-by issue is half an issue.

## Issue And Project Mutations

GitHub issue, PR, and project-board mutations change shared state.

- Use REST for issues, PRs, checks, comments, reviews, labels, milestones,
  merges, and repository state. Reserve GraphQL for GitHub Projects v2
  operations that have no suitable REST endpoint.
- Follow `_DOCS/BOARD_FIELDS.md` for Project IDs, targeted reads, mutation
  batching, rate-budget handling, and event-driven board synchronization.
- Creating issues is additive and usually safe.
- Closing issues, bulk label changes, milestone changes, and project field
  changes require current-state verification.
- Do not close an issue unless implementation is verified or the user explicitly
  approves the closure.
- **"Close" means finish, not flip.** A request to close an issue or PR is an
  instruction to work it to real completion -- implement, validate, review,
  merge, synchronize -- and then close it as done. It is never authorization to
  set the state to closed, apply `not planned`/`wontfix`/duplicate, abandon the
  branch, or otherwise retire an unfinished item to reduce the open count.
  Retiring unfinished work requires an explicit user decision to drop that item.
  If it cannot be finished, leave it open and report the blocker with evidence.
- For multi-issue work, update the project board on concrete state transitions:
  before dispatch, at review/fix/validation transitions, and at closure. Do not
  poll or rediscover the whole board to prove each update.

## Review Gates

- **Reviews fire ONLY at the PR boundary** (Rico 2026-07-10). No review rounds
  on specs, briefs, or intermediate heads during development.
- Review weight scales with blast radius, not line-count theater. Canonical:
  `_ob/skills/pre-merge-gauntlet`.
  0. Pre-PR: author runs critical-mode and exact automated gates (self-check,
     not a review).
  1. Capture one pinned candidate diff and run ONE risk-sized review pass.
     Tiny mechanical/docs changes may use one terminal reviewer; ordinary
     behavior changes use SME + antagonist; security/deploy/shared-contract
     changes add only the specialist lenses their failure modes require. At
     least one lane must be opposite-runtime for meaningful behavior/risk work.
  2. Fix or explicitly waive every finding, then rerun the exact repo gates.
     Reviewers report findings; a separate fixer writes the code.
  3. Re-swarm the new head and loop 1-3 until a round returns no new findings,
     or until three rounds have run with nothing open above P3. An open
     P0/P1/P2 keeps the loop running past the cap; a same-root finding
     surviving two rounds triggers redesign/split.
  4. Once a round is clean, the opposite runtime reviews the whole PR on that
     head; its findings re-enter the loop at step 2.
  5. Controller verifies exact head/CI/comments, then deliberately merges.
- Pure typo/comment changes with no executable or operational claim may pass on
  controller diff inspection plus exact automated gates; do not spawn agents by
  habit.
- A review swarm, when used, still needs an SME and an antagonist. A single
  terminal reviewer is a review lane, not a swarm.
- Findings are work orders, not notes.
- No known material P0/P1/P2 issues may remain at merge, unless the user
  explicitly accepts a blocker or deferral.

### What Makes A PR Mergeable (Rico, 2026-08-02)

A PR is mergeable when **either** of exactly two things is true. There is no
third, and "CI is green" is not one of them.

1. **Rico directs the merge.** His instruction is the gate; nothing further is
   required.
2. **The review loop reached zero, and the opposite harness re-audited it.** All
   three parts, in order:
   - **Findings to zero.** *Resolved* means fixed and re-verified, or waived by
     Rico. A finding answered with a PR comment is not resolved; a finding
     marked resolved by the agent that wrote the fix is not verified.
   - **Re-audit by the OPPOSITE harness**, at medium or higher effort, **in its
     own session.** The whole PR on the clean head, not the fix delta.
   - **The reviewer is pinned by SESSION, never by route label.** A route
     labelled `claude-opus-5` that runs inside a proxied session resolves to
     whatever that session is bound to, and the record still says Opus. Observed
     2026-07-30: a review recorded as native Claude was served by Sol. To pin a
     cross-family reviewer, launch its own process (`claudex <model> -p`) — do
     not request it from inside a session bound to another model. Provenance
     records what was REQUESTED, not what ANSWERED.

The point of the opposite-harness pass is that a harness does not catch its own
blind spots by trying harder. Same-family self-review at the end of a loop the
same family drove is the failure this gate exists to prevent, which is why it is
a separate session and not another lane in the same swarm.

### No Parked PRs In Development (Rico, 2026-08-13)

**In `rodaddy/development`, a green PR gets merged, not parked.** Rico's
standing direction covers this repo's PRs, so an agent does not re-ask per PR:
when the gate above is satisfied, squash-merge it and treat merged `main` as
the new baseline for whatever comes next. A PR sitting open behind a passing
gate is the failure — it makes every later branch start from a stale base.

- **Squash-merging a stacked chain bottom-up breaks the stack.** Merging the
  parent squashes and deletes its branch, and GitHub auto-closes the child PR
  whose base that branch was. The close is not recoverable by reopening.
- **Recovery is `git rebase --onto origin/main <old-base> <child-branch>`, then
  a fresh PR** — observed on #140 → #144. Cheaper than the recovery: rebase each
  child onto fresh `origin/main` and re-target its base BEFORE merging the
  parent, or land the chain as one PR.
