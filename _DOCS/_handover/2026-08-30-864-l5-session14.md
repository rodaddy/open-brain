# Handover — issue 864 L5, collect the last three pull requests and open L6 (2026-08-30)

## State 0 — BASE
Read /Volumes/ThunderBolt/Development/_DOCS/HANDOVER-BASE.md in full
(183 lines). It is the standing contract for this session.
- Any question this document does not directly override → the rules layers
  answer it, nearest layer first.
- Anything neither the base nor this document covers → ask Rico before acting.
- Output discipline: minimum verbosity, only the context needed, output tokens
  low. If Rico wants more, he will ask.
- Layer 0.1: read open-brain/_DOCS/HANDOVER-RULES.md in full (506 lines on this
  branch). It overrides the base; this document overrides it. Rules 28-68 bind;
  65-68 are this session's: a Done-means is run with `bash` so it must be a
  script under `scripts/done-means/`, a pass-alone/fail-in-suite test is a
  whole-run shared-state question answered by searching `mock.module` first,
  M16 twin adapters are found by exported symbol not filename, and jobs
  scheduled on `open-brain-ct108` fail at `Set up job` while its disk is full.
- The program's own rules M1-M16 live at
  `/Volumes/ThunderBolt/_tmp/open-brain/_scratch/session14/common-rules-864.txt`
  and are harvested into `docs/lane-contract.md` by #1006.

## State 1 — ORIENT
- `origin/main` is `8e6cfeb8` (#1005); 40 pull requests merged for issue 864 since `42944000` — MERGED (`git log --oneline 42944000..origin/main`)
- Shim/adapter-excluded runtime closure on `main` is 3: `src/tools/agent-context-pack.ts`, `agent-context-pack-durable-lane.ts`, `agent-context-pack-durable-memory.ts` — MERGED (scribe note 4, issue 864 comment 5465646022)
- #1007 `fix/864-bulk-import-logger-mock` at `1bf16fa2`: `bulk-import.test.ts` split three ways (59 → 59 tests), done-means `scripts/done-means/864-logger-never-mocked-process-wide.sh` RED at main — WRITTEN (`gh pr list --state open --json number,headRefOid`)
- #1009 `refactor/864-c9d-context-pack-adapters` at `fd45ce6f`: carries both #1007 commits, the three modules become M16 adapters over their `server/tools/` twins, full isolated suite 4035 pass 0 fail, gate `864-moved-out-of-src.sh` PASS `judged=48 shims 19 adapters`, closure 0 on the branch — WRITTEN (same command, scribe note 4)
- #1006 `docs/864-session14-harvest` at `0ea19dca`: lane-contract round 44 and two SME entries — WRITTEN (same command)
- All three are blocked only on CI jobs scheduled to `open-brain-ct108` (`10.71.20.116`), root filesystem 100% (50G/50G, 1.0M free at 00:00); jobs landing on `open-brain-ct107` pass — RUNNING (scribe note 4; the prune is Rico's)
- L6 preconditions measured and written into `_plans/864-l5-cut-plan.md` (`package.json:4`/`:8`, `scripts/run-two-worker.ts:30`, `HOST`/`HOSTNAME` with no server reader, the search-embedding timeout reader, `nats-bridge-envelope.ts:3`, 170 tests under `src/`, `src/index.ts` 498 lines) — WRITTEN (this branch)
- `scripts/done-means/744-recall-serves-durable.sh` FAILs both clauses at `main`, pre-existing and unchanged by #1009 — RUNNING (#1009 body)
- Graph mode: converted — RUNNING (`ls scripts/done-means/` holds `864-moved-out-of-src.sh`, `864-logger-never-mocked-process-wide.sh`)
- Reusable, temp: `/Volumes/ThunderBolt/_tmp/open-brain/_scratch/session14/{collect.sh,closure-count.sh,common-rules-864.txt,next-batch-864.md}` — WRITTEN
Re-probe before dispatching anything (live state beats this doc):
- `gh pr list --state open --json number,headRefOid` → expect #1007 #1009 #1006 or fewer
- `ssh root@10.71.20.116 df -h /` → expect free space before any rerun
- `bun scripts/src-runtime-closure.ts | wc -l` in the root checkout → expect 3 at `8e6cfeb8`

## State 2 — LAND THE PAPERWORK
Branch: `chore/864-l6-preconditions` from `origin/main` — cut it once the three
open pull requests merge; if the checkout is `main` or `docs/864-session14-handover`
is merged, switch first, never work there.
Commit this handover: branch `docs/864-session14-handover`, paths
`_DOCS/_handover/2026-08-30-864-l5-session14.md`, `_DOCS/HANDOVER-RULES.md` and
`_plans/864-l5-cut-plan.md`, explicit-path staging, `git commit -F` message
file. Done by the authoring session.
Scribe: issue #864 — started: `gh issue comment 864 --body "Session-14 scribe note 4 ..."` (comment 5465646022)
Done-check: `git log -1 --stat`

## State 3 — Collect #1007, then #1009, then #1006
Tier: T1 — merges already-verified branches; the merge order is the only shared risk
Deliverable: the three pull requests merged in that order, each collected with
`/opt/homebrew/bin/bash /Volumes/ThunderBolt/_tmp/open-brain/_scratch/session14/collect.sh <pr>`
once `open-brain-ct108` has disk space, at most one `gh run rerun <id> --failed` per run
Scope: those three branches, the collect script, `gh` on this repo
Must NOT: rerun a job twice on a full runner (rule 68); merge on red; `--no-verify`; touch the branches' contents
Record: issue #864 comment per merge, then the closing note
Done-check: `gh pr view 1009 --json state` → `MERGED` (RED: 8e6cfeb8, all three OPEN)

## State 4 — Confirm closure 0 on main and post scribe note 5
Tier: T1 — a read-only confirmation that gates every L6 lane
Deliverable: `bun scripts/src-runtime-closure.ts | wc -l` and
`/opt/homebrew/bin/bash /Volumes/ThunderBolt/_tmp/open-brain/_scratch/session14/closure-count.sh`
both run in the root checkout at the merged `main`, both reporting 0, and
scribe note 5 on issue 864 carrying both numbers and the three merge SHAs
Scope: the root checkout, read-only; one issue comment
Must NOT: edit anything to make the number move; claim 0 from a branch receipt
Record: issue #864, scribe note 5
Done-check: `bash /Volumes/ThunderBolt/_tmp/open-brain/_scratch/session14/closure-count.sh` → `0` (RED: 8e6cfeb8, reports 3)

## State 5 — Open L6 from the cut-plan preconditions
Tier: T2 — `package.json` `start` is the serving entrypoint; a wrong flip breaks the app
Deliverable: the first L6 lane cut from `_plans/864-l5-cut-plan.md` `## L6 preconditions`
items 5-8 — `package.json:4`/`:8` and `scripts/run-two-worker.ts:30` flipped to
`server/main.ts`, plus the `HOST`/`HOSTNAME` and search-embedding-timeout readers
registered in `server/main.ts` from config — as its own pull request with a done-means
Scope: `package.json`, `scripts/run-two-worker.ts`, `server/main.ts`, `server/config*`, a new check
Must NOT: delete `src/`, retire a shim or adapter, or touch the 170 tests under `src/` in this lane
Record: issue #864, then the pull request
Done-check: the new `scripts/done-means/864-l6-entrypoint.sh` → exit 0 (RED: not yet run)

## State 6 — WAYFINDER
Close: https://github.com/rodaddy/open-brain/issues/864 stays OPEN through L6;
tick the L5 rows in `_plans/864-l5-cut-plan.md` once State 4 reports 0, and open
the L6 map issue in the same motion if Rico wants L6 tracked separately.

## State FINAL — WRAP
Invoke the handover-author skill; next handover passes the validator; `aqmd up`.

## HANDED-OVER UNKNOWNS
- `open-brain-ct108` disk: the docker prune is Rico's (`ssh root@10.71.20.116`); recurrence prevention is an unowned rtech-infra item.
- #1008: the pre-push isolated suite fails six langfuse-tracing tests on the Mac; not reproduced in session-14 lanes (4035 pass) — owner unassigned.
- `scripts/done-means/744-recall-serves-durable.sh` FAILs at `main` pre-existing; whether it is repaired or retired at L6 is undecided.
- `server/contracts/contract-types.ts:206` names a `src/` path as a contract VALUE; L6 rules on it (M16).
- The 170 tests under `src/` — 97 import only real `src/` sources — need a retire-or-move decision before `src/` is deleted.
- `aqmd in <repo>` returned nothing from this clone and from the root: the repo-local `.qmd` index was retired by #960 and the Development catalogue has no `open-brain` card — Development-owned, blocks the design-lookup route in rule 63.
