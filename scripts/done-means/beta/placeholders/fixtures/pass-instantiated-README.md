# placeholders — graph-mode beta

Status: WRITTEN 2026-08-27 (rodaddy/development, lane graph-mode-beta).

Owner: rodaddy. Repo: development. Scope: `_ob/skills/graph-mode/beta`.

This run's README is fully instantiated: every scaffold slot carries a real
value, so the placeholder check passes.

Run it:

```bash
./check.sh fixtures/pass-instantiated-README.md
```

Estimating tokens: ceil(chars/4).
