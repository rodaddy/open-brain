deliverable:   Resynced the vendored v1.3-beta checks in open-brain from Development canon 6bfc3e3f to 7130d53a (nine adversarial-review fix commits) and re-ran the pilot receipts on https://github.com/rodaddy/open-brain/pull/892
claim-states:  vendored beta tree: WRITTEN; provenance manifest: WRITTEN; 55-fixture cross-repo agreement: RUNNING; graduation of the 45 live tightenings: PROPOSED (the controller's call with Rico, not done here); pull request 892: WRITTEN and open, not merged
verified:      pilot-verify.sh over 55 fixtures in this copy -> exit 0, zero mismatches against documented exits; same script against Development canon and the software-factory copy -> exit 0 each, all three repos agree fixture-for-fixture; decisions/check.sh decisions/fixtures/pass-clause6-retire.md -> exit 0, ok 1 rows 0 failures, was exit 1 before the portability fix; ratchet-bound/check.sh docs/lane-contract.md -> exit 1, live=45 graduated=0 bound=15 source=default shape=heading; placeholders/check.sh docs/lane-contract.md -> exit 0, PASS no unresolved placeholders; decisions/check.sh docs/decisions.md -> exit 0, ok 1 rows 0 failures; loop-policy/check.sh docs/dispatch-plan.template.md -> exit 0, no output; node --test loop-policy/test/snippet.test.js -> exit 0
deviations:    One, carried forward unchanged. The 45-live-vs-15-bound ratchet failure on docs/lane-contract.md is a harvest backlog, not a checker defect, and graduating those entries is a decision for Rico rather than something this lane takes on its own.
lessons:       A byte-faithful copy does not make a fixture portable. The clause-6 passing fixture retired a Development path and so passed only in the repo that authored it, giving exit 0 there and exit 1 in both pilots against an identical checker. A fixture that reaches outside its own bytes for evidence is testing the repo it landed in; running the whole fixture set in every pilot is what surfaced it, and a single-repo green never would have.
