# Database (shared PostgreSQL instance)
DB_HOST=10.71.20.49
DB_PORT=5432
DB_NAME=open_brain
DB_USER=open_brain
DB_PASSWORD=

# LiteLLM Proxy
LITELLM_URL=http://10.71.20.53:4000

# Server
PORT=3100

# CORS allowed origins (comma-separated, e.g. http://localhost:3000,https://app.example.com)
ALLOWED_ORIGINS=

# Auth tokens (one per consumer)
AUTH_TOKEN_ADMIN=
AUTH_TOKEN_AGENT=
AUTH_TOKEN_DISCORD=
AUTH_TOKEN_N8N=
AUTH_TOKEN_READONLY=
