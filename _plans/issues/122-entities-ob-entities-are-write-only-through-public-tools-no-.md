<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #122 — Entities (ob_entities) are write-only through public tools — no get_entity/list/search/stats coverage

State: CLOSED
Author: rodaddy
Labels: bug
Created: 2026-06-18T02:58:50Z
Updated: 2026-06-18T03:54:46Z
Closed: 2026-06-18T03:54:46Z

---

## Correction
Original title ("never persists") was WRONG. Entities DO persist — verified by direct DB query: `upsert_entity` rows land in **`ob_entities`** (the graph table), exactly where the code intends. The confusion: `entity_type:"project"` is a GRAPH ENTITY in `ob_entities`, NOT a row in the legacy `projects` domain table.

## The real gap (contract/usability)
Entities are writable but **effectively opaque through public tools**:
- `get_entry(table:"projects", id)` fails — `get_entry` only reads legacy domain tables (thoughts, decisions, relationships, projects, sessions), not `ob_entities`.
- `get_stats` shows `projects.active = 0` — it only counts legacy domain tables, so entity writes are invisible there.
- `search_brain` does not return entities — its schema/search only covers the 5 legacy tables.

Net: you can write an entity and link it, but no public read tool surfaces it, so it looks "lost" to any caller using the standard tools.

## Verified
- `upsert_entity` -> id `73888d88-...`; direct query: `ob_entities` count 1, `projects` count 0 (correct — different tables).
- Local tests pass: `bun test src/tools/__tests__/{upsert-entity,link-entities,adjacent-context}.test.ts`

## Proposed fixes (one or more)
- Add `get_entity` / `list_entities` public tools.
- Include `ob_entities` in `search_brain`.
- Add entity counts to `get_stats`.
- Clarify docs: `entity_type:"project"` is a graph entity in `ob_entities`, not a `projects` row.

## Impact
Without a read path, the entity/link graph (#38) can't back a hub-spoke knowledge model via public tools — callers can't retrieve or search what they wrote. `adjacent_context` traverses links but you need a starting node id you can otherwise discover.

---

## Resolution

Closed by **PR #123** — [codex] expose graph entities through public tools

- Linkage: GitHub recorded this pull request as the closer.
- Merge commit: `c2c4632591a44929b2e1a5eee5e9b78f4c4fa6f2`
- Merged at: 2026-06-18T03:54:45Z
- PR state: MERGED
- Issue closed: 2026-06-18T03:54:46Z by rodaddy (COMPLETED)

### Direction taken and why — PR #123 body

> Fixes rodaddy/open-brain#122.
>
> ## Summary
> - Add public `get_entity` and `list_entities` tools for graph entities stored in `ob_entities`
> - Include graph entity search via `search_brain(table: "entities")`
> - Add `get_stats.graph_counts` for entity/link visibility without conflating graph entities with legacy `projects`
> - Add Python client wrappers and contract docs clarifying that `entity_type: "project"` is not a `projects` row
>
> ## Validation
> - `bunx tsc --noEmit`
> - `bun test`
> - `cd python/openbrain-memory && uv run mypy src/openbrain_memory`
> - `cd python/openbrain-memory && uv run ruff check src tests`
> - `cd python/openbrain-memory && uv run pytest -q`
>

---

## Discussion (1)

### rodaddy — 2026-06-18T03:32:22Z

Fix chain opened as draft PRs:

- Open Brain server/client fix: https://github.com/rodaddy/open-brain/pull/123
  - Adds `get_entity`, `list_entities`, `search_brain(table:"entities")`, and `get_stats.graph_counts`.
- rtech-mcps registry hardening: https://github.com/rodaddy/rtech-mcps/pull/5
  - Removes base Open Brain Authorization from generated registry and forces remote-only routing.
- mcp2cli schema refresh/generation fix: https://github.com/rodaddy/mcp2cli/pull/52
  - Routes cache warm/diff and skill generation through daemon-backed schema discovery so Open Brain does not hit raw HTTP and 401.

Local validation passed in the clean worktrees. CI is still running on the Open Brain and mcp2cli PRs.
