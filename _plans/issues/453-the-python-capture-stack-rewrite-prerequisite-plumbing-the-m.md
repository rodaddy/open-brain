<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #453 — The Python capture-stack rewrite: prerequisite plumbing the map runs on

State: CLOSED
Author: rodaddy
Labels: wayfinder:task
Created: 2026-08-01T03:53:55Z
Updated: 2026-08-01T09:20:47Z
Closed: 2026-08-01T09:20:47Z

---

## Question

Rebuild the Python capture/hook stack so canon has a working writer and loader to sit on.

The capture and hook plumbing that this map's destination depends on is being
rewritten in Python (plan of record `_plans/python-port-sequence.md`, standing
rulings `_plans/rewrite-gotchas.md`). It is prerequisite plumbing for the
destination — canon cannot load itself, and capture cannot record both sides,
until the stack underneath them exists. This detour was necessary and was never
recorded on the map; it is recorded here as of 2026-08-01.

State (LAW 0):

- **#418 hook capabilities — COMPLETE on branch `rewrite/418-hook-capabilities`.**
  Commits `2201a22`, `0d5dc09`, `28bbb9f`, **merged to the branch, not pushed**.
- **#420 settings.json cutover — IN FLIGHT on branch `rewrite/420-settings-cutover`.**
- **Single hard-testing pass — DEFERRED** until all pieces exist; a real session
  start / capture / checkpoint / reflex proven by row in psql is the one gate,
  run once at the end rather than per slice.

Plan of record: `_plans/python-port-sequence.md`. Rulings: `_plans/rewrite-gotchas.md`.

Type: task (AFK). This does rather than decides — it earns its place by
unblocking the loader (#446), the writer (#445/#447), and the enforcement gate
(#451), not by delivering the destination itself.

---

## Resolution

Closed without a pull request.

- Issue closed: 2026-08-01T09:20:47Z by rodaddy
- State reason: COMPLETED

The closing rationale, if it was written anywhere, is in the discussion below — most recently by rodaddy on 2026-08-01T09:20:41Z.

---

## Discussion (4)

### rodaddy — 2026-08-01T05:28:34Z

## Status through the cutover + hard pass (2026-08-01)

On the wayfinder map [Open Brain is the memory: canon loads itself](https://github.com/rodaddy/open-brain/issues/443), this detour ticket carries the Python capture-stack rewrite. Bringing it current from the `#418 COMPLETE / #420 IN FLIGHT / hard pass DEFERRED` state recorded when this ticket was opened. LAW-0 state on each line.

### Where the slices landed

- **#418 hook capabilities — CLOSED, done and proven.** Console-script entrypoints (`openbrain.apps.hooks.{stop,subagent_stop,session_end,post_compact,session_start}:main`) exercised over captured harness stdin; 73 entrypoint tests + 53 capture-signal tests pass RUNNING this session. Original + added acceptance criteria (no length floor, pasted-rejector, watermark-to-EOF replacing the 8-entry window) all mapped to receipts. Closed with the mapping at issue #418. Commits on branch `rewrite/418-hook-capabilities` (merged-to-branch, not pushed).
- **#420 settings.json cutover — CLOSED, EXECUTED and row-proven.** `settings.json` invokes the five uv-tool console scripts, zero `sha256-` paths (`rg -c sha256- ~/.claude/settings.json` → 0, exit 1, this session). sha256 TS store archived by `mv`; rollback documented `docs/420-cutover-rollback.md`. Commit `25dbf0b`. A real 2026-08-01 session start injected the Python canon pack (`openbrain.agent_context_pack.v1`, status ok) via the new path — RUNNING receipt.
- **The one hard-testing pass — EXECUTED, not deferred anymore.** Commit `cb7b051`, plan-as-run `_plans/hard-pass-2026-08-01.md`, independent verdict "HARD PASS WITH OPEN DEFECTS". Playground DB `open_brain_local_play` on `:3101`; the live gate proven to FAIL LOUD with no env (P1, 14 errors, zero silent skips); mypy/ruff clean, 282 unit passed, 15 live passed. Live-MET: session_end slot release, subagent_stop watermark isolation, post_compact whole-summary + prompt_id dedup, session_start canon-only pack, resume watermark→EOF.

### What this unblocks on the map

The loader (#446) and the enforcement gate (#451) now have a proven Python writer and loader underneath them: capture records both sides via the Stop spine + PostCompact, and the canon pack loads canon-only through the new session-start hook. This ticket earned its place — it does, it does not decide.

### Residuals carried forward (RUNNING state honesty)

- **LAN cross-host live proof — UNVERIFIED.** Hard-pass Item 6 BLOCKED as a premise correction: `10.71.1.20` is this host's own IP, `:3100` is the forbidden real dogfood DB, `:3101` is loopback-bound. No disposable LAN target exists. Closing it is an infra task (a real second disposable Open Brain host over the LAN — Air symlink pending on operator), not a capture-stack code gap.
- **prompt_id re-fire residual.** Whether a real harness PostCompact re-fire assigns a fresh `prompt_id` per compaction (latent double-store if so; dedup keys on `prompt_id`, not content). Logged in `_plans/rewrite-gotchas.md`.
- **Canon sections empty pending corpus/promotion.** The pack loads and is canon-only, but the guidance tiers are unpopulated: per #445 the DB holds one `user_preference` + one `process_rule` (both `candidate`) and zero `repo_fact` rows for open-brain. Loader proven; the writer/promotion track (#445/#447) is the separate work that fills it.

Prerequisite plumbing is in place: the writer and loader the destination sits on now exist and are proven RUNNING against the playground DB.

---

### rodaddy — 2026-08-01T06:10:21Z

## Tracker current as of 2026-08-01 — plumbing is RUNNING; two residuals + one blocked path (LAW 0)

Independent-lane verified against live forge and a served playground DB. States below are LAW-0.

**#418 hook capabilities — MERGED (branch, not pushed).** Console-script entrypoints exist and are wired.

**#420 settings.json cutover — RUNNING, row-proven.** `~/.claude/settings.json` carries zero `sha256-`/SHA-256-pinned paths; all five Python console scripts install via the uv tool as `openbrain-capture-{stop,subagent-stop,session-end,post-compact,session-start}`; gate hooks point at `_ob/scripts`; retired no-op blocks are absent. Dogfood proof rows re-queried by a second lane (Stop `252bb247`/`2c23d0dd`, PostCompact `71f057c8`, post-retirement re-proof `9dcd7313`). The TypeScript store is retired to `~/.local/share/openbrain-memory/_archive/420-cutover-retired-20260801/`; rollback in `docs/420-cutover-rollback.md`; cutover artifacts on this branch at `25dbf0b`.

**Single hard-testing pass — RUNNING, executed at `cb7b051`.** `_plans/hard-pass-2026-08-01.md` is the plan-as-run against the disposable `open_brain_local_play` DB on port 3101. Independent verifier verdict: **HARD PASS WITH OPEN DEFECTS**.
- The four never-before-live hooks each met their criteria: `session_end` accepted the lifecycle event and wrote no row; `subagent_stop` stored the composite `parent:agent` key and full content; `post_compact` stored the full 200,000-char summary and deduplicated a same-`prompt_id` re-fire to one row (two rows after `prompt_id` changed); `session_start` returned canon-only sections (including for `source=compact`), surfacing `working_set` only under the widened override.
- Resume round: the second Stop delivered only appended turns.
- The `-m live` gate was proven to fail loudly without its DB env (14 errors, zero skips) — a green live run cannot be silent-empty.

**This resolve wave (2026-08-01) changed no code.** `git diff cb7b051..HEAD` is exactly `_plans/worklog/2026-08-01.md` and `docs/sme/correctness.md` — zero TS, zero Python, zero test files. Gates re-run this session on the branch tip: `mypy` clean (17 files), `ruff` clean, `pytest` 545 passed / 9 skipped / **1 pre-existing failure** (`test_event_vocabulary.py::test_vocabulary_is_declared_exactly_where_expected`, a drift-guard on `python/openbrain/models/turn.py`, red at the base `cb7b051` — an ancestor, not introduced here; flagged as branch-base-not-green, not this wave's regression).

**Open residuals (UNVERIFIED / BLOCKED):**
- **Same-`prompt_id` freshness** after a re-fire is not proven (dedup-to-one-row is; freshness on re-fire is the residual). Carried in `_plans/rewrite-gotchas.md`.
- **Canon-only SessionStart sections are structurally correct but EMPTY** pending a canon corpus — nothing promoted yet (#445 settled the mechanism; #444 seeds the set).
- **LAN cross-host live path — BLOCKED.** No disposable LAN target existed (`10.71.1.20` is this host; port 3100 is the forbidden real dogfood DB; the playground is loopback-bound). No cross-host mutation was attempted.
- **Air symlink check — UNVERIFIED.** No Air-side proof produced.

**Net:** the writer and loader the map's destination sits on now exist and fire (RUNNING), so #453's unblocking role is discharged for the local runtime. It stays open until the residuals above close, and the downstream loader (#446), writer policy (#445/#447), and enforcement gate (#451) are separate map tickets, not this tracker's.

---

### rodaddy — 2026-08-01T09:12:19Z

## Landed to `main` — staying OPEN by this ticket's own gate

Both branches this ticket tracks are now MERGED to `main`:

- **#418 hook capabilities + #420 settings.json cutover** — PR [#455](https://github.com/rodaddy/open-brain/pull/455), squash `eb84b02`. CI-green at merge (check, db-integration, python-package, python-provider, contract-parity, validate; deploy skipped-by-design).
- **LAN-bind clone-mode mirror** — PR [#456](https://github.com/rodaddy/open-brain/pull/456), squash `3584527`. CI-green.

So the first two State lines in the body are no longer accurate as written: #418 is MERGED (not "merged to branch, not pushed"), and #420 is MERGED (not "IN FLIGHT").

**The single hard-testing gate — PARTIALLY proven, so this ticket stays OPEN.**
This ticket's gate is: *"a real session start / capture / checkpoint / reflex proven by row in psql."* Proven this session against the running local service (3100) + dogfood DB `open_brain_local_20260724`, through the **installed** `python/openbrain` provider:

- **session start** — RUNNING. SessionStart fixture through the hook-env wrapper → well-formed `openbrain.agent_context_pack.v1` canon pack (status ok, namespace rico, sections profile/process/repo_facts).
- **capture** — RUNNING. One operator-typed turn through `openbrain-capture-stop` → service `ingest_raw_turn_ok ingested:1`, row `7fc1279c-a47b-4d3e-bd01-83a6bb22bdbf` in `ob_raw_turns` (namespace rico, role user, is_human_prompt t).
- **checkpoint** — NOT proven this session.
- **reflex** — NOT proven this session.

Two of the four legs are green by psql row; the checkpoint and reflex legs are not demonstrated. Per this ticket's own criterion (and CLOSE-means-finish), it is not closed on a half-proven gate. Remaining to close: prove checkpoint and reflex end-to-end by psql row through the installed provider.

**Note (not blocking this ticket, but real):** the dogfood clone's `_migrations` records `032_raw_turns.sql` as applied but `to_regclass('public.raw_turns')` is NULL, and the DB carries a divergent 033/034 lineage (`033_system_facts`/`034_memory_axes` alongside `033_candidate_memory`/`034_content_occurrences`). The running service writes to `ob_raw_turns` (which exists), so capture works; but the clone's migration history has drifted from the branch lineage. Flagging for a deliberate look before the hosted deploy — freshly-migrated playground and the CI ephemeral DB both carry the current lineage cleanly.

---

### rodaddy — 2026-08-01T09:20:41Z

## Four-leg gate COMPLETE — checkpoint + reflex now proven by psql row (2026-08-01)

The two remaining legs are proven end-to-end through the **installed** `openbrain-memory` provider (uv tool + `openbrain-hook-env` wrapper), against the RUNNING local service `http://127.0.0.1:3100` (`/health` → healthy, db connected) and the dogfood DB `open_brain_local_20260724`. LAW-0 states below are RUNNING (checked this session).

### Leg 3 — checkpoint — RUNNING, proven by row

**Command:** `echo '<JSON>' | sh openbrain-hook-env openbrain-memory` with `operation:checkpoint`, `distilled:true`, `config.namespace:rico` / `project:open-brain`, `scope.session_key: dev:open-brain:453-legs-20260801`, a unique summary marker `453-legs-checkpoint-proof-1785575855`.

**Receipt (RUNNING):**
```json
{"receipt":{"direct_attempted":true,"durable":true,"fallback_attempted":false,"operation":"checkpoint","schema":"openbrain.runtime_receipt.v1","status":"saved"},
 "result":{"context_updated":true,"created_at":"2026-08-01T09:17:37.885Z","lane_id":"a89e7a02-0f68-4874-b7a7-f4172c881671","lane_status":"active","session_id":"[REDACTED]"}}
```
(`session_id` is client-redacted in the receipt by design; the row is proven in psql below.)

**Call shape:** `checkpoint` → `FirstClassMemoryRuntime.checkpoint` → `AgentMemory.checkpoint` → server `session_wrap` → `INSERT INTO sessions`. The runtime's `_ensure_lane` creates the lane for the fresh non-colliding `session_key` before the wrap.

**psql row (dogfood DB, table `sessions`):**
```
id         c7bc6eef-2041-4c03-9c41-063f3ce2ff9d
project    open-brain
namespace  rico
created_by rico
created_at 2026-08-01 05:17:37.88562-04   (= 09:17:37.885Z, matches the receipt to the ms)
tier       warm
summary    453-legs-checkpoint-proof-1785575855 :: Issue #453 four-leg gate ...
embedding  present (not null)
key_decisions {"checkpoint operation routes through session_wrap then INSERT INTO sessions"}
next_steps    {"verify the row by psql in open_brain_local_20260724"}
```
A durable row with my exact marker, my exact key_decisions/next_steps, an embedding, and a `created_at` matching the receipt to the millisecond. **Checkpoint leg: proven.**

### Leg 4 — reflex — RUNNING, proven (READ leg: request succeeds against real data + psql evidence of what it read)

Reflex is a **pure read** by contract (direct-only, never spooled, never mcp2cli-fallback; `durable:false`). It writes NO row — so the leg is proven by (a) the request returning a validated body-free envelope over real data, and (b) psql evidence that the pointers it returned resolve to real namespace-scoped durable rows. This is the only proof this leg admits, and I state it as such.

**Command:** same wrapper, `operation:reflex`, same scope/config, `query:"issue 453 four-leg gate checkpoint capture reflex proof"`.

**Receipt (RUNNING):**
```json
{"receipt":{"direct_attempted":true,"durable":false,"fallback_attempted":false,"operation":"reflex","schema":"openbrain.runtime_receipt.v1","status":"direct"}}
```

**Result envelope (RUNNING):** `schema: openbrain.agent_reflex_pointers.v1`, `status: ok`, `placement: client_owned`, `resolvable_reference_only: true`, `pointers.item_count: 20`, `pointers.namespace_scoped: true`, `label: pointers`, scope echoed back exactly (`namespace_source: authorization`, `namespace: rico`, `session_key: dev:open-brain:453-legs-20260801`), citations a bijection with the pointers (`kind: pointer`). 19 pointers `namespace=rico`, 1 `namespace=shared-kb` — the honest two-namespace projection.

**psql evidence — the pointers resolve to real durable `thoughts` rows, values matching the envelope:**
```
id                                    ns        tier  created_at (psql)            updated_at (psql)
d6d13494-8f2c-4579-a64e-2b895b917058  rico      hot   2026-07-17 02:30:16.258-04   2026-07-28 03:06:37.725-04
8c15ec6d-b682-4998-92f5-559a0c59f550  rico      hot   2026-07-14 23:36:14.318-04   2026-07-28 03:06:37.569-04
05e0bd7a-1e1e-4a9b-a9c7-eb7a4cd49155  rico      hot   2026-07-03 09:11:56.916-04   2026-07-28 03:06:37.725-04
71bbeefb-ad24-4f33-8c93-26bcdb1e3ce3  shared-kb hot   2026-06-19 02:17:32.095-04   2026-06-19 02:17:32.095-04
```
Each row's `namespace`/`tier`/`created_at`/`updated_at` matches the reflex envelope exactly (e.g. `d6d13494`: envelope `created_at 2026-07-17T06:30:16.258Z` / `updated_at 2026-07-28T07:06:37.725Z` = the psql instants). Namespace scoping is faithful: the `rico`-token reflex returned only `rico` + `shared-kb` pointers, no other namespace leaked. **Reflex leg: proven (read + resolution evidence).**

### Gate status — all four legs proven by psql row

| Leg | State | Evidence |
|-----|-------|----------|
| session start | RUNNING | canon pack `openbrain.agent_context_pack.v1` status ok (prior comment) |
| capture | RUNNING | row `7fc1279c-...` in `ob_raw_turns` (prior comment) |
| checkpoint | RUNNING | row `c7bc6eef-2041-4c03-9c41-063f3ce2ff9d` in `sessions` (above) |
| reflex | RUNNING | `agent_reflex_pointers.v1` ok, 20 pointers resolving to real `thoughts` rows (above) |

The single hard-testing gate this ticket carried — *"a real session start / capture / checkpoint / reflex proven by row in psql"* — is fully met. Closing per CLOSE-means-finish.

### Open flags re-homed before close (nothing orphaned)

- **Migration drift** (dogfood clone `_migrations`: `032_raw_turns` applied but `raw_turns` regclass NULL; divergent `033_system_facts`/`034_memory_axes` alongside branch `033_candidate_memory`/`034_content_occurrences`) — split out to **#458**, verified by row there, operator-directed reconciliation before hosted deploy.
- **Downstream rollout steps 2–5** (hosted verification, rtech-mcps handoff, mcp2cli pull/skill refresh, Hermes live rollout) — already homed in `docs/downstream-rollout.md` §§2–6 and referenced from PR #455; not orphaned by this close.
- **Residuals from the hard pass** (LAN cross-host live proof BLOCKED on a disposable second host; prompt_id re-fire; empty canon tiers pending #445/#447) remain tracked on their own lines/tickets — none are this ticket's gate.
