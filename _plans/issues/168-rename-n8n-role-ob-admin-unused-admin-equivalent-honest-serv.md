<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #168 — Rename n8n role -> ob-admin (unused admin-equivalent; honest server-admin identity)

State: CLOSED
Author: rodaddy
Labels: none
Created: 2026-06-19T15:28:41Z
Updated: 2026-07-05T21:34:57Z
Closed: 2026-07-05T21:34:57Z

---

## Goal

Rename the `n8n` role → `ob-admin` and repurpose it as the explicit server-side break-glass admin identity for manual user-promotions and deletions.

## Why — n8n is a misnamed, 100% unused second-admin token

Evidence (verified 2026-06-19):
- **Capability:** `n8n` has `RWD` on every table in `PERMISSIONS` — **identical to `admin`** — and is paired with `admin` in every privileged gate (shared-kb write, promote, scan, curate-with-delete, cross-namespace read, `"all"` keyword, X-Namespace delegation, rest-promotion).
- **Usage:** ZERO. No rows ever `created_by = 'n8n'` (thoughts/decisions/entities all 0). Not referenced in the n8n skill, no n8n workflow calls Open Brain, `AUTH_TOKEN_N8N` is not used as a client anywhere, no n8n-role auth in server logs.
- So it's full-power dead weight with a name implying it's for n8n.io automations — which it is not used for.

## Distinct from #159 promoter (intentional separation)

- `promoter` (#159): least-privilege automation identity — scoped to promotion (lane→shared-kb pipeline). RWD on curation tables, RO projects, write/read across namespaces, but NOT curate-with-delete or `"all"`.
- `ob-admin` (this issue): full admin break-glass for HUMAN server-side surgery (deletions, manual user promotions). Effectively today's n8n capability, honestly named.

## Approach (Rico's call)

Preferred end-state: **add `ob-admin` as the canonical role name, keep `n8n` as a deprecated alias** for safety, remove `n8n` in a later cleanup. (Conservative — avoids a hard breaking change to a security primitive.)

## Work

- [ ] Add `ob-admin` to Role union + `AUTH_TOKEN_OB_ADMIN` env key + PERMISSIONS (mirror current n8n = full RWD).
- [ ] Every `role === "admin" || role === "n8n"` gate also accepts `ob-admin` (or refactor to an `isAdminLike(role)` helper covering admin + ob-admin + deprecated n8n).
- [ ] Keep `n8n` as a deprecated alias (same capabilities) with a deprecation log warning; plan removal.
- [ ] Provision `AUTH_TOKEN_OB_ADMIN` on the host; use it for the server-side promotion/deletion ops.
- [ ] Tests for ob-admin parity + n8n-alias-still-works.

## Sequencing
**After #159 (promoter role) is merged.** Do NOT fold into the #159 PR — separate security-role change with its own tests. Lower urgency since n8n is unused (no active exposure).

## Provenance
Surfaced 2026-06-19 while building #159; Rico asked what the n8n role was. Confirmed unused + admin-equivalent.
