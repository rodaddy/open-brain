<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #69 — Add safety layer: redaction, bounded recall, retries, idempotency, JSONL spool

State: CLOSED
Author: rodaddy
Labels: none
Created: 2026-06-10T23:41:21Z
Updated: 2026-06-11T00:26:52Z
Closed: 2026-06-11T00:26:52Z

---

## Goal
Add the safety and reliability primitives needed before agent runtimes depend on Open Brain memory.

## Sequencing
Can start partly in parallel with #68 after #67 has settled the client call/error shape. This issue is not optional; it gates production use so we do not rebuild unbounded attic dumping.

## Package paths
- `python/openbrain-memory/src/openbrain_memory/policy.py`
- `python/openbrain-memory/src/openbrain_memory/spool.py`
- retry/idempotency helpers wherever they fit cleanly

## Requirements
### Redaction
- Redact obvious secrets before writes:
  - bearer tokens
  - API keys
  - GitHub tokens
  - private-key blocks
  - common `.env` assignment patterns
- Redaction must be conservative and test-covered.

### Bounded recall assembly
- Limit recall by item count and character budget.
- Preserve source/type metadata for prompt consumers.
- Avoid dumping raw oversized memory into prompts.

### Retry/backoff
- Small bounded retry loop for transient HTTP/network failures.
- No infinite retries.
- Surface final failure clearly.

### Idempotency
- Generate or accept idempotency keys where possible.
- Avoid duplicate writes during retry/spool replay.

### JSONL spool
- Spool failed writes to local JSONL file.
- File mode `0600`.
- Bounded max lines/bytes.
- Never store raw unredacted secrets.

## Tests
- Redaction examples.
- Bounded recall truncation.
- Retry success-after-failure.
- Spool file mode and truncation behavior.

## Acceptance criteria
- Safety tests pass with `uv run pytest`.
- Failed writes are recoverable without leaking secrets.
- Recall output is bounded by count and character budget.
- Retry/spool replay cannot create obvious duplicate writes in tested paths.

Parent: #66

---

## Resolution

Closed by **PR #74** — feat: add memory safety layer

- Linkage: GitHub recorded this pull request as the closer.
- Merge commit: `54093bf00de2f1fffde006b53b2338d658a0db9b`
- Merged at: 2026-06-11T00:26:51Z
- PR state: MERGED
- Issue closed: 2026-06-11T00:26:52Z by rodaddy (COMPLETED)

### Direction taken and why — PR #74 body

> Closes #69
>
> ## Summary
> - Add safety primitives for redaction, bounded retry policy, idempotency keys, and JSONL spooling.
> - Wire AgentMemory writes through conservative redaction and optional failed-write spooling.
> - Keep non-idempotent writes out of automatic retry; only session_start retries by session key.
> - Add bounded prompt/context tests and spool safety tests.
>
> ## Acceptance criteria
> - Redacts bearer tokens, API keys, GitHub tokens, private-key blocks, env-style and colon/JSON-style secret patterns before writes/spool persistence.
> - Recall output remains bounded by count and character budget.
> - Retry is bounded and covered for transient session_start failure.
> - Failed writes spool to JSONL with 0600 data and lock files, redacted payloads, max-line/max-byte trimming, symlink rejection, and replay ack semantics.
> - Replay dispatch receives full SpoolRecord including idempotency_key and removes successful records while preserving failed ones.
>
> ## Verification
> - uv run pytest in python/openbrain-memory: 45 passed, 1 skipped.
> - bun test at repo root: 579 passed, 16 skipped.
> - git diff --cached --check: passed.
>
> ## Notes
> Live writes are intentionally redacted before calling Open Brain per #69. Non-idempotent writes are spooled on failure instead of retried in-process to avoid duplicate writes after ambiguous transport failures.
