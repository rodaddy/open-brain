<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #380 — INGEST-1: Add content-bearing raw turn table and full-send ingest

State: CLOSED
Author: rodaddy
Labels: enhancement, memory
Created: 2026-07-24T21:30:15Z
Updated: 2026-07-26T05:01:29Z
Closed: 2026-07-26T05:01:29Z

---

Parent: #342
Program: #320
Spec: `docs/full-send-derivation-spec.md`

## Problem

The client hand-picks what to remember and nothing persists raw dialogue.
Measured 2026-07-24 on the local dogfood clone: a full working day produced
**3** `ob_session_events`, all written because the assistant manually chose to
run the capture command. `mcp_tool_audit_log` recorded 445 calls but is
content-free by design (`declared_parameter_keys`, `payload_size_bucket`,
`duration_ms`; no content column), so it cannot serve as a raw stream.
Prompts and responses are persisted nowhere.

Distillation and derivation are impossible because **their input does not
exist**.

## Scope

- Add `ob_raw_turns` (content-bearing, namespace-bound, `gen_random_uuid()`
  primary key, unique on `(namespace, lane_id, content_hash)` for idempotent
  re-send, partial index on `distilled_at IS NULL` as the sweep work queue).
- Add an `ingest_raw_turn` tool, agent role and above, namespace predicate
  derived from auth like every other write.
- Wire `UserPromptSubmit` (user turn) and `Stop` (assistant turn) to
  full-send. Batching allowed; the call must never block the interactive turn.
- Failures spool locally and replay through the existing spool path.

## Non-goals

- The client applies **no** salience judgment: no summarizing, scoring, or
  skipping. It ships what happened.
- No retrieval, `agent_context_pack`, or prompt-placement changes.

## Acceptance criteria

- [ ] A working day produces raw turns in the hundreds, not events in the
      single digits.
- [ ] Re-sending an identical turn conflicts and is ignored rather than
      duplicated.
- [ ] Namespace isolation regression test proves an ID-based read/mutation
      carries the auth-derived namespace predicate.
- [ ] Ingest failure never blocks or errors the interactive turn.

## Blocks

Retention/backpressure (INGEST-2) must land before sustained use.
