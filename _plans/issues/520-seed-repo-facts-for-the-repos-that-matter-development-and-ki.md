<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #520 — Seed repo facts for the repos that matter -- Development and king-* are near-empty and now honestly show it

State: OPEN
Author: rodaddy
Labels: none
Created: 2026-08-03T06:09:11Z
Updated: 2026-08-03T06:09:11Z

---

After #517 (merged, PR #518), sessions get the facts of the repo they are in — which makes the coverage gap visible instead of masked: current bindings are open-brain=7, king-core=2, development=1 (rebound 2026-08-03 from the legacy `rodaddy/development` slug — that was the AGENTS.md-referenced runtime-context fact `0c0a4e94`), and 1 each for the other king repos.

Seeding is content-authoring, not plumbing: each fact needs provenance (`repoFactMetadata`: repo, source_commit, source_url, verified_at — `src/tools/repo-facts.ts`) and should be a distilled operational truth an agent needs at startup, not a duplicate of what already auto-loads (Development's AGENTS.md router is in every session's context — do NOT mirror it into facts).

Candidates per repo: deploy/run truths, DB/test gotchas, host bindings, the things `# Stack`/`## Commands` sections carry. Write path: `upsert_repo_fact` per #445's decision record; the canon pack TOML + reconciler (#493 finding: no pack file exists yet) is the reviewable home once authored.

Slug convention is git-root basename lowercased (#517). Any future fact bound under another shape is invisible to the pack — bind exactly.
