<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #464 — Provider JSON-stdin capture silently drops candidate_type / memory_lifecycle_action / candidate_scope keys

State: CLOSED
Author: rodaddy
Labels: none
Created: 2026-08-01T22:45:08Z
Updated: 2026-08-04T01:52:45Z
Closed: 2026-08-04T01:52:45Z

---

## Observed (2026-08-01, canon seeding run)

The installed \`openbrain-memory\` JSON-stdin CLI accepts a \`capture\` operation whose allowed keys are content/distilled/event_type (+config/scope). Passing \`candidate_type\`, \`memory_lifecycle_action\`, or \`candidate_scope\` — the exact fields the #445 promotion mechanism requires — gets them **silently dropped**: the write succeeds, returns \`status:saved\`, and the row lands without the metadata. At most a \`compatibility_note: ignored_optional_request_keys\` appears in the receipt, which nothing fails on.

The canon seeder detected this and routed through the client library (\`AgentMemory.promote_candidate\`) instead — correct workaround, and the seed landed clean (31/31, verified). But the CLI path is the advertised durable-write route for hooks and operators, and a promotion attempted through it would report success while seeding nothing promotable.

## Why it matters

Silent-drop-on-success is the exact defect class the rewrite exists to kill (see #431's history). Anything that scripts a promotion via the CLI gets a false receipt.

## Fix direction (open)

Either the CLI accepts the full promotion vocabulary and forwards it, or it rejects unknown keys loudly (named error, non-zero exit) — never accept-and-ignore. Regression test: pipe a capture with \`candidate_scope\` through the console path and assert either the metadata lands or the call fails loud.

Refs: seeding receipts on #444/#438; OB gotcha event fac56442; #445 mechanism.
