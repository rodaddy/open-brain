<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #210 — Define receipt schema and provenance model

State: CLOSED
Author: rodaddy
Labels: none
Created: 2026-06-26T05:13:53Z
Updated: 2026-06-27T02:02:47Z
Closed: 2026-06-27T02:02:47Z

---

## Goal
Define the lightweight Open Brain receipt schema and the stricter hooks future Closed Brain will need for privileged provenance.

## Scope
- Model source files read, files written/touched, generated artifacts, base documents/templates, command/tool evidence, external channels, validation checks, timestamps, agent identity, namespace, and session key.
- Define which fields are required for normal Open Brain and which are Closed Brain strict-mode requirements.
- Keep receipts citation-safe and secret-safe.
- Map receipts to existing session event types artifact and receipt where possible.

## Acceptance Criteria
- Schema is documented and issue-sized implementation follow-ups are clear.
- Receipt examples cover report generation, source file reads, generated artifacts, and base PDF/template use.
- Closed Brain strict-mode deltas are explicitly marked.

## Timeline
Target date: 2026-06-26. This blocks receipt helper implementation on 2026-06-27.

Parent plan: docs/agent-memory-substrate-plan.md

---

## Discussion (2)

### rodaddy — 2026-06-26T21:48:48Z

Phase A receipt-schema receipt:

- Added `docs/agent-memory-adapter-contract.md` with lightweight Open Brain receipt schema `openbrain.receipt.v1`.
- Defined required fields: schema, action, agent, session_key, timestamp, sources, outputs, validations.
- Defined recommended provenance fields for namespace/project/branch, commands, external channels, artifact hashes, source refs, and residual risk.
- Added examples for report generation, source inspection, and base document/template use.
- Explicitly separated Closed Brain strict-mode deltas: pre/post-image hashes, base document hashes, tool call ids, approval chain, redaction policy, and tamper-evident storage.
- Added `receipt_contract` discoverability in `get_contract` via `src/contract.ts` and tests.

Validation:
- `bunx tsc --noEmit` passed.
- `bun test src/contract.test.ts src/tools/__tests__/get-contract.test.ts` passed: 8 tests.
- `uv run pytest -q tests/test_contract.py tests/test_client.py` passed: 66 tests.
- `git diff --check` passed.

Residual risk:
- Helper implementation is intentionally deferred to #212; this issue remains In Progress until review/PR evidence confirms the contract is accepted.

---

### rodaddy — 2026-06-26T22:03:06Z

Receipt schema local receipt:

- Added `docs/agent-memory-adapter-contract.md` with lightweight `openbrain.receipt.v1` receipt schema.
- Receipt schema covers sources, outputs, validations, commands, external channels, generated artifacts, base documents/templates, timestamps, agent identity, namespace/project/session metadata, and residual risk.
- Added examples for report generation, source inspection, and base document/template use.
- Marked Closed Brain strict-mode deltas separately: pre/post image hashes, base document hashes, tool call IDs, approval chain, redaction policy, and tamper-evident storage.
- Added `receipt_contract` to the public `get_contract` manifest and pinned it in `src/contract.test.ts`.

Validation:
- `bun test src/contract.test.ts src/__tests__/agent-memory.test.ts` passed: 16 tests.
- `uv run pytest python/openbrain-memory/tests/test_contract.py python/openbrain-memory/tests/test_client.py -q` passed: 66 tests.
- `bunx tsc --noEmit` passed.
- `git diff --check` passed.

Residual risk:
- Receipt helper expansion continues in #212; Closed Brain strict-mode enforcement is intentionally documented but not implemented in this local slice.
