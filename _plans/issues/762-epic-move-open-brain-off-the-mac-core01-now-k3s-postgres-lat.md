<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #762 — [Epic] Move Open Brain off the Mac: core01 now, k3s Postgres later

State: OPEN
Author: rodaddy
Labels: none
Created: 2026-08-26T01:25:52Z
Updated: 2026-08-26T04:49:37Z

---

## Ruling (Rico, 2026-08-25)

Open Brain leaves the Mac mini (10.71.1.20). Target: core01 (10.71.1.21) at minimum, likely a new Postgres box in the k3s cluster (CNPG, like authentik/listmonk/vaultwarden/webtier). This supersedes `_DOCS/HANDOFF-RULES.md` rule 13 (core01 out of scope) for this epic.

## Current state (RUNNING 2026-08-25)

- Mac clone: `ad3b59c` on `deploy/local-clone-20260825`, database `open_brain_local_20260724` (178,282 vectors, `halfvec(768)`), embedder `embed-gemma-dense` on k3s `ai/llama-swap` via `https://llama-swap.rodaddy.live/v1`.
- core01: degraded, both workers 503, database not connected. Open cutover blockers: #674 (serves `src/index.ts`, not `server/main.ts`), #675 (deploy has no revision proof, tars the working tree, unversioned plist).
- No k3s Postgres exists for Open Brain; no plan on record.
- Last backup on record: `core01-20260724`. The Mac database has no newer backup.

## Order of work

1. Fresh `pg_dump` of the Mac database with a `pg_restore --list` receipt, before anything else (Rico standing rule: backup before any mutation).
2. Land what is unmerged: AGENTS.md embedder amendment (#757), #744, #747, #761.
3. Decide the target with Rico: core01 first, or straight to a k3s CNPG cluster. Write the cutover checklist (dump/restore, embedding dimension check, token/env move, DNS, clients: mcp2cli, Hermes, the Air).
4. Execute one step at a time, each with a receipt and Rico's go.

## Blockers
- #674, #675 (core01), #298 (backup-chain/restore proof).

Claim state: RUNNING for the probes above; PROPOSED for the order of work.

---

## Discussion (2)

### rodaddy — 2026-08-26T03:35:58Z

**Target exists (Rico, 2026-08-25 evening, RUNNING per his live check):** k3s CNPG cluster `general`, PG 18.4, 3/3 healthy, LB `10.71.20.167:5432`, pgvector 0.8.6. Databases `qmd` and `open_brain`, both EMPTY. Verified from the Mac end-to-end: connect as `qmd` over the LB and cast `'[1,2,3]'::halfvec(3)`.

Credentials (names only): Vaultwarden `PostgreSQL - general qmd` (user `qmd`, db `qmd`), `PostgreSQL - general open_brain` (user `open_brain`, db `open_brain`). Shape: `postgresql://open_brain:<pw>@10.71.20.167:5432/open_brain`.

Sizing: 40Gi per instance; targets to beat 2,487 MB (qmd, measured from `qmd_vectest`) and ~1,337 MB (Open Brain production).

**Gap, blocks cutover:** the 04:40 daily `pg_dump` CronJob backs up the bootstrap database only. Neither tenant is backed up. `backup.sh` needs to enumerate databases at run time. Until that runs once against `open_brain` with a restore receipt, no client points at it.

**Order (PROPOSED, for the handoff's State 7):**
1. Fresh `pg_dump -Fc` of the Mac's `open_brain_local_20260724` (178,282 vectors) with `pg_restore --list` receipt.
2. `pg_restore` into `open_brain` on `10.71.20.167`; verify per-table row counts, `halfvec(768)` columns, and a cosine spot-check against the Mac (same query, same top-k).
3. Tenant backup CronJob fixed and proven on `open_brain` (rtech-infra side).
4. Only then point clients (the Mac clone first, via `DB_HOST`/`DB_*` in `local-clone.env`; `validateLocalCloneMode` currently requires `DB_HOST` loopback, `src/local-clone-mode.ts:138`, so a host opt-in like #759's embedding one is needed), then core01 / everything else.

Not executed: nothing has moved.

---

### rodaddy — 2026-08-26T04:49:37Z

Step 1 of the order of work is DONE with a receipt.

**Backup: `/Volumes/ThunderBolt/open-brain-local/backups/local-20260826-cutover`**
- `openbrain.dump` 1.82 GB, `manifest.json` written
- `pg_restore --list` reads it: 399 archive entries, 42 tables with data
- Source `open_brain_local_20260724`: 4824 MB, 202,976 raw turns, 26,453 thoughts
- `scripts/backup-verify.ts --dir <set>` → **exit 0, status passed**: checksum verified, migration head equal, contract equal, **embedding model/dimension equal**

That last verdict is the one that matters for the move — the corpus and the runtime agree on the vector space, so a restore will not silently corrupt retrieval.

**Target is now RUNNING and confirmed live this session** (it did not exist when this epic was written):
- CNPG cluster `general` in `databases`, PG 18.4, 3/3 healthy, pgvector 0.8.6
- `10.71.20.167:5432`, database `open_brain`, empty
- Credentials in Vaultwarden, "PostgreSQL - general open_brain"
- Probe requires `KUBECONFIG=~/.kube/config-rtech-k3s`; the default context is a dead `docker-desktop`

**Not yet backed up on the new box**: neither tenant. `backup.sh` there enumerates only the bootstrap database, so `open_brain` gets no daily dump until it enumerates databases at run time.

Claim state: backup RUNNING (verified this session). Restore NOT started — no data has moved, nothing points at the new host.
