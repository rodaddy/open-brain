<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #774 — lint: enable typescript/switch-exhaustiveness-check (type-aware, needs oxlint-tsgolint)

State: OPEN
Author: rodaddy
Labels: none
Created: 2026-08-26T08:46:35Z
Updated: 2026-08-26T08:46:35Z

---

## What

`_DOCS/STANDARDS-typescript.md:186` requires `switch-exhaustiveness-check`. The `.oxlintrc.json` landed by #771 does not carry it, on purpose: the rule is type-aware. Measured on #771's fixer lane 2 (2026-08-26): oxlint 1.80 accepts the rule name at config parse but reports 0 on a switch over a 3-member union missing a case while a `no-console` control fires on the same run; `--type-aware` fails with `Failed to find tsgolint executable`. Shipping the rule without the executable would read as enforcement and enforce nothing.

## Do

1. `bun add -d oxlint-tsgolint` (lockfile change), then `"typeAware": true` in `.oxlintrc.json` or `--type-aware` on the `lint` script and in `_githooks/pre-commit`'s oxlint step.
2. Measure the finding count on the tree and the added hook latency; report both in the PR.
3. RED-first: the probe pair archived at `/Volumes/ThunderBolt/_tmp/open-brain/_archive/switchprobe-20260826/` (`probe.ts` must be reported, `ctrl.ts` is the no-console control).

Claim state: PROPOSED.
