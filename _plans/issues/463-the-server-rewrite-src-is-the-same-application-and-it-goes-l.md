<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #463 — The server rewrite: src/ is the same application, and it goes last

State: OPEN
Author: rodaddy
Labels: wayfinder:task
Created: 2026-08-01T22:38:09Z
Updated: 2026-08-02T05:19:07Z

---

## Question

Rewrite the Bun/TypeScript server (`src/`) as the final rewrite phase.

**Sequencing gate:** this ticket starts only after the remaining Python work lands: the #419 gate disposition, the bulk ingester non-Claude format adapters, and the canon/lens pack reconciler.

When this ticket is taken up, the first step is **CHARTING**, not code: settle the target architecture/runtime, inventory what the Python client contract already pins, and define the migration/cutover shape for a running dogfood service. Implementation starts only after that route is charted.

**Plan of record:** `_plans/python-port-sequence.md:438` ("`src/` is the same application being rewritten") and the operator ruling of 2026-08-01: all three Python applications, then the TypeScript application, then full testing, then swap to the new reality.

Type: task (AFK). This records the final rewrite phase and its sequencing gate; it does not authorize implementation before the Python remainder lands.

---

## Discussion (1)

### rodaddy — 2026-08-02T05:19:07Z

## #463 start receipt

- Charter: **WRITTEN** — `_plans/463-server-rewrite-charter.md`
- Scaffold: **WRITTEN** — `server/`, explicitly scaffold-only and serving nothing
- Contract harness: **WRITTEN** — parity now checks `current-src` and `server-rewrite-scaffold` against the same reviewed declaration fixture
- Cutover: **NOT STARTED**
- Sequence: final `src/` cutover remains last, after the remaining Python work in map #443
- Running application: unchanged; the branch has no `src/` diff and no startup/deploy entrypoint points at `server/`

Validation receipts:

- TypeScript typecheck passed
- Dual-provider parity passed: 14 fixtures / 9 capabilities / 2 providers
- Python fixture parity passed: 14 tests
- Full Bun + isolated Postgres passed: 3,070 pass / 34 skip / 0 fail
- DB anti-skip guard passed: 194 live-Postgres cases across all 10 required suites

Draft PR: https://github.com/rodaddy/open-brain/pull/476

No issue was closed and the map was not edited.
