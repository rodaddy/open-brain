<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #825 — L2b-2: route the remaining process.env reads through ServerConfig

State: CLOSED
Author: rodaddy
Labels: enhancement
Created: 2026-08-26T19:42:30Z
Updated: 2026-08-27T00:05:25Z
Closed: 2026-08-26T22:13:27Z

---

Filed 2026-08-26 by the #780 drain lane (native Claude head session, `780-sweep-session2`), draining the session-3 handoff (PR #824) per `_ob/skills/handoff-author/_DOCS/steps/01-drain.md`. The inventory below was carried in a temp-workspace scratch file with no persistence guarantee; it is reproduced here in full so the content has a durable home.

## What

`_plans/server-hardening-ladder.md` rung **L2b-2** wants every `process.env` read in `server/` routed through the validated `ServerConfig` object instead of reaching back into the environment. `server/config.ts:416` is the single intended entry point (`parseServerConfig(process.env, readDeployStamp())`); everything else that reads `process.env` directly is the remaining work.

`server/config/env-groups.ts:6-7` states the standing rule in the tree itself: domain code must not import `process.env`, per `_DOCS/STANDARDS-typescript.md:183-191`.

Parent: #750.

## Inventory, re-probed at `origin/main`

Probe run this session against `origin/main` (40628c9), server tree extracted with `git archive origin/main server`:

```
rg -n 'process\.env' server --glob '!*.test.ts'
```

23 hits across 9 files. **Every read from the original audit still exists**, with one file rename (see below).

```
server/config.ts:95: * `process.env.DB_PASSWORD` straight to pg, and `src/embedding.ts`'s
server/config.ts:176:    // `process.env`. Declarations and the reasoning behind each parser live in
server/config.ts:351:    // validated object rather than reaching back into `process.env`. That is
server/config.ts:416:  const result = parseServerConfig(process.env, readDeployStamp());
server/application/nats.ts:19: * `readNatsRuntimeBoundary(process.env)` instead, the process would have two
server/observability/trace-config.ts:33:  env: Record<string, string | undefined> = process.env,
server/main.ts:105:   * database on an ephemeral port without mutating `process.env` — which is
server/main.ts:177:      // The values the tool layer used to read from `process.env` itself, now
server/main.ts:379:        parseAllowedOrigins(process.env.ALLOWED_ORIGINS),
server/main.ts:429:  const port = options.port ?? Number(process.env.PORT ?? DEFAULT_PORT);
server/main.ts:430:  const bindHost = options.bindHost ?? process.env.OPEN_BRAIN_BIND_HOST?.trim();
server/main.ts:526:      env: process.env,
server/config/env-groups.ts:6: * process.env readers with injected config" — and `:119`, domain code must not
server/config/env-groups.ts:7: * import `process.env`. `_DOCS/STANDARDS-typescript.md:183-191` requires one
server/config/env-groups.ts:48: * cannot receive the string it would get from `process.env`.
server/config/env-groups.ts:111: * The reader is `Number(process.env.PORT ?? DEFAULT_PORT)` followed by
server/config/nats.ts:6: * 18 scattered `process.env` readers the charter counts in `src/`. The behavior
server/tools/search-all.ts:124:  env: Record<string, string | undefined> = process.env,
server/tools/search-engine.ts:124:    process.env.OPENBRAIN_SEARCH_EMBEDDING_TIMEOUT_MS ??
server/tools/search-engine.ts:125:    process.env.SEARCH_EMBEDDING_TIMEOUT_MS;
server/tools/shared-namespace.ts:37:    const raw = process.env[name]?.trim();
server/tools/shared-namespace.ts:45:  const raw = process.env[name];
server/tools/shared-namespace.ts:52:  const raw = process.env[name];
```

### Drift since the original audit (taken at `chore/780-lint-search-all` @ dc5b82c)

- `server/observability/langfuse-tracing.ts:599` is now **`server/observability/trace-config.ts:33`** — same construct (`env: Record<string, string | undefined> = process.env` default parameter), file renamed/split since the audit.
- `server/tools/search-engine.ts` reads moved from `:148-149` to **`:124-125`**; unchanged otherwise.
- No read in the original inventory has been removed.

## Classification — what is actual work

Not all 23 hits are code. Splitting them:

**Prose only (comments/doc blocks) — no change needed, they describe the rule:**
`server/config.ts:95,176,351`, `server/application/nats.ts:19`, `server/config/env-groups.ts:6,7,48,111`, `server/config/nats.ts:6`, `server/main.ts:105,177`.

**Sanctioned entry point — keep:**
`server/config.ts:416` — the one intended `process.env` read.

**Injectable default parameter — already testable, decide whether the rung wants it removed anyway:**
`server/observability/trace-config.ts:33`, `server/tools/search-all.ts:124`. Both take `env` as a parameter defaulting to `process.env`, so a caller can inject. The rung's phrasing ("route through ServerConfig") suggests the default should come from config rather than the environment, but that is a judgement call for whoever takes it.

**Direct reads — the real L2b-2 work:**
- `server/main.ts:379` — `parseAllowedOrigins(process.env.ALLOWED_ORIGINS)`
- `server/main.ts:429` — `Number(process.env.PORT ?? DEFAULT_PORT)`
- `server/main.ts:430` — `process.env.OPEN_BRAIN_BIND_HOST?.trim()`
- `server/main.ts:526` — `env: process.env` passed wholesale
- `server/tools/search-engine.ts:124-125` — two timeout variables read directly
- `server/tools/shared-namespace.ts:37,45,52` — three dynamic `process.env[name]` reads

`server/config/env-groups.ts:111` documents that the `PORT` reader at `main.ts:429` is a known instance.

## Done means

Each read in the "direct reads" group either resolves from the validated config object or is documented in place as a deliberate exception with the reason. A re-run of `rg -n 'process\.env' server --glob '!*.test.ts'` returns only the sanctioned entry point, prose, and any documented exceptions.

## Notes

Original audit file: `{temp_workspace}/open-brain/_scratch/server-env-lint-audit.md` — temp workspace, no persistence guarantee. Its second section (an oxlint findings sweep over 102 non-test files under `server/`) belongs to the #780 whole-file lint checklist and is tracked there, not here.

---

## Resolution

Closed by **PR #847** — feat(config): pass the search embedding timeout from validated config into search-engine (L2b-2, #825)

- Linkage: GitHub recorded this pull request as the closer.
- Merge commit: `6905139fad0faab6a03dfe2bc3482a07e50bde6e`
- Merged at: 2026-08-26T22:13:26Z
- PR state: MERGED
- Issue closed: 2026-08-26T22:13:27Z by rodaddy (COMPLETED)

### Direction taken and why — PR #847 body

> ## Summary
>
> - `server/tools/search-engine.ts` read `OPENBRAIN_SEARCH_EMBEDDING_TIMEOUT_MS` / `SEARCH_EMBEDDING_TIMEOUT_MS` directly, while `server/config/env-groups.ts` `searchGroup` already parsed the same two names into `config.search.embeddingTimeoutMs`. Both existed and only the reader was consulted, so the validated value was a record of an intention rather than the number in force, and the two could drift silently. This is State 4 of #825 (rung L2b-2).
> - The value now arrives through dependencies: `SearchDependencies` (`server/tools/search-engine-types.ts`) and `MemoryToolDependencies` (`server/tools/types.ts`) gain an optional `searchEmbeddingTimeoutMs`, following exactly the pattern #779 used for `ftsCorpusConfig` / `recoveryWalPath` / `natsRuntimeBoundary`. The composition root `server/main.ts:184` hands down `config.search.embeddingTimeoutMs` next to `ftsCorpusConfig`.
> - Behavior is unchanged, and that was checked before editing rather than assumed. `searchGroup` (`server/config/env-groups.ts:289-298`) applies the identical two names, the identical `OPENBRAIN_`-first precedence, the identical blank-is-absent handling, and the identical 3000 fallback for a non-numeric or sub-1 value. An absent injection still answers `DEFAULT_SEARCH_EMBEDDING_TIMEOUT_MS`, which is what an unset environment produced.
> - Scope is this rewiring only. `shared-namespace.ts`, `search-all.ts`, `trace-config.ts` and `langfuse-tracing.ts` are the other three L2b-2 lanes and are untouched here. No lint suppression was added and no default changed.
>
> ## Verification
>
> - Done-means: scripts/done-means/750-l2b2-check-well-formed.sh
> - [x] Relevant Open Brain tests/typecheck/migrations passed
> - [ ] Python package checks passed or are not applicable — not applicable; no Python source changed.
> - [ ] Live Open Brain smoke passed or is not applicable — not applicable; this is an internal wiring change with no client-facing surface.
>
> `scripts/done-means/750-l2b2-env-readers-take-config.sh` stays RED overall until all four L2b-2 lanes land — three readers and three arrivals remain, owned by the sibling lanes. What this lane moved, measured on the committed tree:
>
> ```
> CLAUSE 2 (no process.env in any of the 4):    FAIL — 5 process.env read(s) remain:   [was 7; search-engine.ts:124-125 no longer listed]
> CLAUSE 4 (values arrive from validated config):  FAIL — 1/4 values arrive; the rest are not wired:   [was 0/4; searchEmbeddingTimeoutMs now found]
> ```
>
> Other checks, all on the committed tree (the pre-commit hook reformats, so these were re-run after committing):
>
> - `./node_modules/.bin/oxlint --deny-warnings server/tools/search-engine.ts server/tools/search-engine-types.ts server/tools/types.ts server/main.ts` -> 0 findings
> - `bunx tsc --noEmit` -> exit 0
> - `bun run test:isolated server/tools/` -> 166 pass, 0 fail across 18 files
> - `bun run test:isolated server/tools/search-engine.test.ts server/config.test.ts server/tools/dependency-arrival.test.ts` -> 90 pass, 0 fail
>
> ## Critical Self-Review
>
> - Highest-risk behavior: a search silently running on a different embedding timeout than before, which degrades hybrid results and fails vector searches without an error anyone reads. Mitigated by comparing the two parsers input-by-input before editing rather than comparing their constants: same two names, same `OPENBRAIN_`-first precedence, same blank-is-absent rule, same 3000 answer for blank / non-numeric / sub-1.
> - Assumptions that could be wrong: that every caller of `executeSearch` / `executeSearchWithSharedFallback` passes an object derived from `MemoryToolDependencies`, so adding the field to both interfaces makes it flow without touching call sites. Checked — `answer-retrieval.ts:44`, `search-all.ts:399`, `search-brain.ts:222` and `context-pack-durable-memory.ts:256` all pass a `MemoryToolDependencies`-shaped object, and `tsc --noEmit` is clean.
> - Missing/weak tests: the injected-value test asserts through the logged `timeout_ms`, not by inspecting the timer, so a future change that logs one number and arms another would pass. The default-fallback case is proven via an unusable injected value rather than by waiting 3000ms for a genuinely absent one, so the absent path is asserted at the type level only. Both were traded deliberately against a test that takes three seconds to run.
> - Security/permission risk: none. No namespace predicate, auth check, SQL, or role handling is touched; the field is a number carried on an existing dependency object.
> - Migration/deploy risk: none. No schema change and no new environment variable — the same two names are read, one layer earlier.
> - Downstream client/runtime risk: none per `docs/downstream-rollout.md`. No MCP tool schema, transport, wire contract, or Python client surface changes; `MemoryToolDependencies` is an internal server-side type with no external implementers.
> - Rollback/cleanup concern: reverting the commit restores the direct read; nothing else depends on the new field, and the config group it consumes predates this change.
> - Fixes made before PR: the first draft test used `"memories"` as a search table, which is not in `ResourceTable`; `tsc` caught it and it became `"thoughts"`. The test was also proven able to fail — asserting 41 against an injected 40 reported `Expected: 41 / Received: 40` — then restored.
> - Known residual risk: the done-means check remains RED until the three sibling lanes land, so this branch alone does not close #825, and clause 4 asserts the arrival line's presence rather than its runtime effect. The runtime effect is what `server/tools/search-engine.test.ts` covers.
> - SME review-memory update: [ ] `docs/sme/` updated or [x] not applicable because: this lane surfaced no new review pattern — it is a mechanical application of the dependency-injection shape #779 established and the existing SME entries already cover, and no MEDIUM+ finding arose.
>
> ## Review Gate
>
> - [x] Critical self-review fields above are filled with specific, non-placeholder content
> - [x] MEDIUM+ review findings were captured in `docs/sme/` or explicitly marked not applicable
> - Live Open Brain checks: [ ] linked below or [x] not applicable because: no deployed surface, schema, or client contract changes; the value read is the same one, resolved one layer earlier.
>
> ## Contract Parity
>
> - Contract parity: [ ] fixtures updated
> - Contract parity: [x] runtime-specific because: `MemoryToolDependencies` and `SearchDependencies` are internal TypeScript server types with no fixture, wire schema, or Python-client counterpart.
>
> ## Downstream Rollout
>
> - [x] I checked `docs/downstream-rollout.md`
> - [x] rtech-mcps handoff is complete or not applicable
> - [x] mcp2cli cache/skill refresh is complete or not applicable
> - [x] rtech-hermes Python runtime/plugin changes are complete or not applicable
> - [x] Hermes live rollout/canaries are complete or not applicable
>
> Notes/evidence:
>
> - Not applicable on every downstream row: no MCP tool, schema, protocol, or client-facing behavior changes. The classification rule in `docs/downstream-rollout.md` keys on contract-changing MCP / transport / Python-client / agent-facing changes, and this is an internal composition-root wiring change behind an unchanged tool surface.
>

---

## Discussion (21)

### rodaddy — 2026-08-26T21:30:43Z

Scribe, session 5 (authored by session 4): handoff written at _DOCS/_handoff/2026-08-26-l2b2-env-rewiring-session5.md on branch docs/780-sweep-session4 (ba252ba), validator exit 0. Live readers at 3995531 outside config.ts/main.ts: search-engine.ts:124-125, shared-namespace.ts:37,45,52, search-all.ts:124 and trace-config.ts:33 (default params). Slice: RED-first done-means, four rewirings, then no-process-env. main.ts reads left for Rico's ruling (ladder override sanctions main.ts; this issue lists them).

---

### rodaddy — 2026-08-26T21:34:52Z

Scribe, session 5: started. Handoff #845 merged as a80484b. origin/main=a80484b. Re-probed: 5 process.env code lines in search-engine.ts/shared-namespace.ts, 2 default-param readers (search-all.ts:124, trace-config.ts:33). Graph mode declared T1.

---

### rodaddy — 2026-08-26T21:48:49Z

Scribe, session 5, State 3: PR #846 adds scripts/done-means/750-l2b2-env-readers-take-config.sh (commit 9115937). Head re-ran it in lane-1: exit 1 at a80484b, CLAUSE 1 PASS, CLAUSE 2 FAIL (7 reads), CLAUSE 3 PASS, CLAUSE 4 FAIL (0/4 arrivals). RED receipt RUNNING. Arrival contract for the rewiring lanes: server/main.ts searchEmbeddingTimeoutMs: config.search.embeddingTimeoutMs; server/main.ts sharedNamespaceNames: config.sharedNamespaceNames; server/main.ts searchAllEnv:; server/observability/langfuse-tracing.ts readMcpTracingConfig(config.

---

### rodaddy — 2026-08-26T21:51:09Z

Scribe, session 5: collector for #846 STOPPED correctly. verify-lane ran 750-l2b2-env-readers-take-config.sh at head 9115937, exit 1, posted no receipt (RED by design). Precedent (#778, #779) landed the check and the fix in one PR; a standalone RED check has no green receipt path. Fix in flight on the same branch: scripts/done-means/750-l2b2-check-well-formed.sh (proves the check is well-formed regardless of RED/GREEN) becomes #846's Done-means line.

---

### rodaddy — 2026-08-26T21:58:06Z

Scribe, session 5, State 3 DONE: #846 merged as 15d3cb8 (receipt exit 0 on 1029fab via 750-l2b2-check-well-formed.sh; 750-l2b2-env-readers-take-config.sh is on main and RED by design, exit 1). Next: State 4 (search-engine.ts) and State 6b (trace-config.ts) dispatched in parallel, disjoint files.

---

### rodaddy — 2026-08-26T21:59:49Z

Scribe, session 5: dispatched in parallel on disjoint files from 15d3cb8: State 4 search-engine.ts (lane-3), State 6b trace-config.ts + langfuse-tracing.ts (lane-4), State 6a search-all.ts (lane-5). State 5 shared-namespace.ts is being cut into lanes from a caller-cascade recon (25 call sites across 15 files; too wide for one lane).

---

### rodaddy — 2026-08-26T22:11:54Z

Scribe, session 5: three rewiring PRs open from 15d3cb8, each RED-by-design on the env-readers check and receipted by 750-l2b2-check-well-formed.sh: #847 search-engine.ts (c6c7476; searchEmbeddingTimeoutMs via SearchDependencies, precedence/default proven equal input by input at env-groups.ts:289-298), #848 search-all.ts (ae7c5c6; resolveQmdPath takes config.qmd.path, ARRIVALS entry c changed from searchAllEnv: to qmdPath: config.qmd.path), #849 trace-config.ts + langfuse-tracing.ts (f92e491; createTracingRuntime({ config: config.tracing }) from main.ts:505, ARRIVALS entry d changed accordingly; lane flagged a possible Zod-coercion vs literal-"1" difference on OPENBRAIN_TRACING_ENABLED, equivalence check lane running before merge). Recon: config.sharedNamespaceNames (SharedNamespaceGroup) is NOT structurally identical to SharedNamespaceConfig (no canonicalSharedNamespace), contrary to the #846 comment; State 5a lane (PR pending) aligns the shapes and adds the optional names parameter; caller threading follows in 4 parallel lanes then a final fallback-removal lane. Collector for #847 running.

---

### rodaddy — 2026-08-26T22:17:00Z

Scribe, session 5, State 4 DONE: #847 merged as 6905139 (receipt exit 0 on c6c7476, CI 8/8). #849 equivalence check (Luna, read-only): every tracing input class equal old vs new, enabled uses strictOneFlag (=== "1"), masking strictZeroDisablesFlag (!== "0"), no z.coerce involved; EQUIVALENT: yes. Only src/index.ts:318 still reaches readMcpTracingConfig(process.env) via the default createTracingRuntime() (legacy root, out of L2b-2 scope).

---

### rodaddy — 2026-08-26T22:20:39Z

Scribe, session 5: #848 collector STOPPED on 6 failing check tests; triage lane matched them exactly to open flake #702 (core01 deploy ref-gate exit 127 x5 + JsonlSpool ENOENT; main run 33018820212 green on the same job; neither test file references anything #848 touched); python-capture is #764. Collector re-run with that triage. #849 is CONFLICTING against main after #847 (main.ts literal + done-means ARRIVALS adjacency); rebase lane queued behind #848 so the ARRIVALS block conflicts once, not twice.

---

### rodaddy — 2026-08-26T22:22:51Z

Scribe, session 5, State 6a DONE: #848 merged as c3ccd49 (receipt exit 0 on ae7c5c6; check-job failures matched #702, python-capture #764). State 5a: PR #850 open (68c96d2; shared-namespace exports take optional names, env-groups sharedNamespaceGroup carries the full SharedNamespaceConfig shape, main.ts passes sharedNamespaceNames: config.sharedNamespaceNames; clause 4 now 2/4 on that branch). Rebase lanes running for #849 and #850 onto c3ccd49.

---

### rodaddy — 2026-08-26T22:32:32Z

Scribe, session 5: #850 merged as 20754bd (receipt exit 0 on 9a094e4; note: at merge time gh reported only GitGuardian on that head, the CI run for the rebased head had not reported; main CI at 8d5c3c7 will be checked before closure). #849 merged as 8d5c3c7 (receipt exit 0 on 2c00c25, CI pass). Head re-ran 750-l2b2-env-readers-take-config.sh at origin/main 8d5c3c7: CLAUSE 1 PASS, CLAUSE 2 FAIL (3 reads, all shared-namespace.ts), CLAUSE 3 PASS, CLAUSE 4 PASS 4/4, exit 1. States 4, 6a, 6b DONE; State 5a DONE. Remaining: thread sharedNamespaceNames to the 25 call sites (batch 1: read-scope.ts / search-engine.ts+rows / scan-namespace+promote-entry+promotion / reporting+tier-lane+repo-facts+ingest; batch 2: read-scope callers; final: remove the env fallback).

---

### rodaddy — 2026-08-26T22:41:32Z

Scribe, session 5: main CI at 20754bd (run 33019928754) and 8d5c3c7 (run 33019952674) green; the #850 PR-head run 33019924485 also passed. Batch 1 threading: #851 read-scope.ts (00e8706) and #852 search-engine.ts + search-engine-rows.ts (4ef882a) open, collectors running. Two lanes stopped with receipts and no edits: scan-namespace.ts, promote-entry.ts, promotion.ts, promotion-shared.ts, reporting.ts, tier-lane.ts, repo-facts.ts, ingest-conversation-facts.ts import src/shared-namespace.ts (the legacy twin, HANDOFF-RULES rule 8), not server/tools/shared-namespace.ts; the src copy has no names parameter and its SharedNamespaceConfig shape differs (sharedNamespace, allowLegacySharedWrites). Those reads live in src/, outside this rung's target (server/tools/shared-namespace.ts) and outside the no-process-env scope; not threaded here, recorded as a handed-over unknown for the src/ retirement rung. Remaining for the server copy: search-brain, search-all, brain-answer, adjacent-context, agent-context-pack, list-recent, context-pack-durable-memory callers, then the fallback removal.

---

### rodaddy — 2026-08-26T22:44:19Z

Scribe, session 5: #851 merged as 63283f7 (receipt exit 0 on 00e8706), #852 merged as adc8f2a (receipt exit 0 on 4ef882a). Note for both: gh showed no ci.yml run on the PR head at merge time (same as #850, whose run later reported green); main CI at adc8f2a will be checked before closure. Batch 2 dispatched: search-brain/search-all/brain-answer (lane-3) and adjacent-context/agent-context-pack/list-recent/context-pack durable memory (lane-5). Stray no-commit branches left in lane-4 and lane-6 clones by the two stopped lanes; cleaned at wrap.

---

### rodaddy — 2026-08-26T22:55:33Z

Scribe, session 5: batch 2 open: #853 search-brain/search-all/brain-answer (992e742), #854 adjacent-context/agent-context-pack/list-recent/context-pack-durable-memory (d1d6001); collectors running. State 7 recon (Luna, read-only): oxlint 1.80.0 offers node/no-process-env (node plugin not yet in the plugins array); overrides[].files globs are honoured (existing no-console override at .oxlintrc.json:119-132); CI has no oxlint step (ci.yml:368-369 comment only), the gate is _githooks/pre-commit on staged files and package.json:16 lint over '.'; src/ has 19 non-test files reading process.env, so the rule must be scoped to server/ or overridden for src/; ladder status line at _plans/server-hardening-ladder.md:104, Enforcement at :178-180.

---

### rodaddy — 2026-08-26T22:58:19Z

Scribe, session 5: #853 merged as 25e74ad (receipt exit 0 on 992e742), #854 merged as 34304f0 (receipt exit 0 on d1d6001). Every server/ caller of server/tools/shared-namespace.ts now passes dependencies.sharedNamespaceNames. Final State 5 lane dispatched (lane-7): delete the env readers in shared-namespace.ts, throw at the call site when names is undefined (rule 20), bridge src/index.ts only if it builds the dependencies object; done-means 750-l2b2-env-readers-take-config.sh expected exit 0 at its head.

---

### rodaddy — 2026-08-26T23:14:30Z

Scribe, session 5: State 7 DRAFT PR #856 (a5ad543): node plugin + node/no-process-env in .oxlintrc.json scoped to server/** with the two-file door override (server/config.ts, server/main.ts) off; head ruling recorded: NOT repo-wide because src/ has 19 env-reading files and is retired by a later rung (Rico can overrule). New done-means scripts/done-means/750-l2b2-lint-refuses-process-env.sh: probe clause PASS (pre-commit refuses a new read naming node/no-process-env), door clause PASS, whole-server clause RED until the last readers land. Ladder :104 now says L2b MERGED (in the draft). The rule surfaced a 4th reader the State 3 script does not scan: server/observability/langfuse-tracing.ts:398 readMcpTracingConfig(process.env) fallback kept for the legacy root; lane dispatched to move that read into src/index.ts. Final State 5 PR #857 open (de0fae1): env readers deleted from shared-namespace.ts, requireNames guard throws when the composition root did not supply names; 750-l2b2-env-readers-take-config.sh exit 0 at that head; server/ test:isolated 517 pass (535 at main: the #850 env-agreement matrix in config.test.ts is obsolete once the env reader is gone and was replaced by 4 throw proofs). Collector running.

---

### rodaddy — 2026-08-26T23:25:00Z

Scribe, session 5: #857 MERGED 49a8ae6 (server/tools/shared-namespace.ts env readers deleted, requireNames guard, fixture for tests; receipt exit 0 on de0fae1). #858 open (chore/l2b2-tracing-legacy-root-env, 874932f): removes the readMcpTracingConfig(process.env) fallback in server/observability/langfuse-tracing.ts:398; src/index.ts and scripts/run-nats-worker.ts pass the tracing config explicitly; new test proves the missing-config throw. Collector dispatched. Draft #856 (State 7 lint rule) rebases and goes ready after #858 lands.

---

### rodaddy — 2026-08-26T23:29:46Z

Scribe, session 5: main is RED at 49a8ae6 (#857). CI run 33022617001 fails 6 parity fixtures (contracts/server-tool-parity.test.ts:325: brain-answer-empty, context-pack-sections, namespace-denial, recall-family, reflex-pointers-empty, search-brain-arms) because the parity harness composes tool dependencies without sharedNamespaceNames and the new requireNames guard throws. The #857 lane ran server/tools tests, not contracts/. #858 fails the same 6 by inheritance only (its diff is tracing-only). Fix lane dispatched on lane-7 (chore/l2b2-parity-harness-names); #858 and #856 rebase after it lands.

---

### rodaddy — 2026-08-26T23:52:20Z

Scribe, session 5: #859 MERGED 2962740 (parity harness passes sharedNamespaceNames; whole suite 3907/0), #858 MERGED 3b9b751 (langfuse-tracing.ts has no process.env; legacy root src/index.ts and scripts/run-nats-worker.ts pass tracing config), #856 MERGED 9a76d31 (State 7: node/no-process-env armed for server/**, door override server/config.ts + server/main.ts only, 750-l2b2-lint-refuses-process-env.sh all clauses PASS on 2d020da, ladder L2b MERGED). No L2b-2 PRs remain open. Closure receipt lane running on origin/main.

---

### rodaddy — 2026-08-26T23:54:36Z

Closure receipt, session 5, on origin/main 9a76d31 (State 7 #856 merged):

- `bash scripts/done-means/750-l2b2-env-readers-take-config.sh` -> exit 0. CLAUSE 1 PASS (4 target files present), CLAUSE 2 PASS (no process.env in search-engine.ts, shared-namespace.ts, search-all.ts, trace-config.ts), CLAUSE 3 PASS (scanner still matches server/config.ts), CLAUSE 4 PASS (all 4 values arrive once from validated config in server/main.ts).
- `./node_modules/.bin/oxlint --deny-warnings --ignore-pattern '**/*.test.ts' server` -> exit 0, zero diagnostics with node/no-process-env armed for server/** and the door override on server/config.ts and server/main.ts only.
- `bash scripts/done-means/750-l2b2-lint-refuses-process-env.sh` -> all three clauses PASS on #856 head 2d020da (verify-lane receipt on the PR). On a clone without `_githooks/install.sh` run it stops at its hooksPath precondition, by design.
- Main CI run 33024780513 at 9a76d31: success.

Merged for this rung: #846 (RED-first done-means), #847, #848, #849, #850, #851, #852, #853, #854, #857, #858 (readers and composition root), #859 (parity harness fix after #857 broke main at 49a8ae6), #856 (lint rule, ladder L2b MERGED).

Left as handed-over unknowns for the next handoff: `server/main.ts` reads of process.env are sanctioned by the door override; `src/shared-namespace.ts` twin still reads env (used by promotion/scan/reporting/tier-lane/repo-facts/ingest paths in src/); the rule is scoped to server/** only, src/ retires in a later rung. `_plans/750-standards-sprint-map.md` exists only on `sprint/standards-fmt`, so no map tick.

---

### rodaddy — 2026-08-27T00:05:25Z

Scribe, session 5 wrap: next handover is PR #861 (_DOCS/_handover/2026-08-26-l3-logger-session6.md, validator exit 0 at 120 lines) plus HANDOVER-RULES rule 33; L3 tracked in #860. Clones lane-1..lane-11 clean; 11 merged remote branches deleted; session record in _reports/2026-08/ (untracked by design).
