<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #132 — Define qmd-derived context fact ingestion for Open Brain

State: CLOSED
Author: rodaddy
Labels: none
Created: 2026-06-18T06:14:20Z
Updated: 2026-06-18T10:05:34Z
Closed: 2026-06-18T10:04:53Z

---

## Problem

qmd is a GPU-machine tool. The GPU-less LXCs that run Bilby and the cc-* agents cannot run qmd embeddings, so they cannot rely on qmd for code/context retrieval. Open Brain is the shared memory layer those agents can reach.

That does **not** mean Open Brain should mirror raw qmd/code chunks. Raw code mirrors are high volume, drift immediately, and are worse than reading the repo with `gh` or a synced checkout. Open Brain should carry the curated qmd-derived context layer: facts, gotchas, ownership notes, and source pointers that tell an agent where to verify the live code.

## Desired Shape

Define a first-class convention and ingestion path for qmd-derived context facts in Open Brain.

Each fact should be:

- attached to the right OB entity or namespace
- keyword retrievable and graph-walkable without assuming embeddings are available
- stamped with source provenance and drift metadata
- explicit about whether it is stable guidance or a volatile implementation detail
- usable by GPU-less LXC agents as a pointer to the real source, not as a replacement for source

Suggested metadata fields:

- `source_system`: `qmd`
- `repo`
- `collection`
- `path`
- `symbol` or `subject`
- `fact_type`: e.g. `ownership`, `gotcha`, `api_contract`, `workflow`, `dependency`, `migration`
- `source_commit`
- `source_url`
- `verified_at`
- `confidence`
- `staleness_policy`
- `refresh_hint`

## Non-goals

- Do not bulk-import raw qmd chunks or full code excerpts into OB.
- Do not make OB a replacement for qmd semantic code search.
- Do not assume OB embeddings are available for retrieval.
- Do not store volatile signatures as timeless facts when a pointer to source is safer.

## Acceptance Criteria

- A documented OB metadata convention exists for qmd-derived context facts.
- There is an ingestion path, script, or MCP tool flow that can upsert these facts idempotently.
- Ingested facts include source commit and verification timestamp.
- Fact reads expose enough provenance for an agent to detect stale context and follow the pointer to live source.
- Tests cover the metadata shape and idempotent update behavior.
- A live canary proves a GPU-less LXC agent can retrieve a qmd-derived fact from OB and follow its source pointer with `gh` or an available synced checkout.
- The implementation explicitly rejects or avoids raw qmd/code chunk mirroring.

## Notes

The hardware constraint decides the architecture:

- qmd remains the local/GPU code-context power tool.
- OB becomes the shared curated fact/context layer for agents that cannot run qmd.
- Source code remains in GitHub/repos; OB points to it and carries the durable context needed to know what to read and why.

---

## Discussion (3)

### rodaddy — 2026-06-18T06:20:52Z

Refinement from follow-up discussion: for anything not running on the GPU/local qmd machine, qmd is not an available runtime dependency. If distributed agents need a repo fact, that fact has to be present in Open Brain or it is effectively unavailable to them.

This does not change the no-raw-code-mirror rule. It changes the promotion rule:

- qmd remains the code/source-of-knowledge system where it can run.
- OB is the required distribution layer for any qmd-derived repo knowledge agents need outside that machine.
- The unit promoted to OB should be curated, durable operating knowledge with source pointers and staleness metadata.
- Raw code stays in repos/qmd/GitHub unless a specific agent cannot reach source at all.

So the workflow is not optional export. It is: derive/verify from qmd/source, then promote required facts to OB so GPU-less/non-local agents can use them.

---

### rodaddy — 2026-06-18T06:26:56Z

Architecture decision refinement:

Default path is option 3: qmd on the local GPU Mac acts as the repo-knowledge compiler, and required facts are promoted into Open Brain as the shared runtime/distribution layer. Agents should not need qmd online to operate from required repo facts.

Option 1 is still useful as a controlled wrapper/escape hatch: expose this machine qmd through mcp2cli or a small remote wrapper for operator/debug/deep lookup flows. That wrapper should not replace OB promotion for facts agents are expected to rely on. It should be treated as best-effort remote source lookup, with clear failure behavior and no assumption that every agent can or should call it during normal memory flow.

Implementation implication:

- Build OB promotion as the durable path.
- Optionally add a qmd-remote wrapper/tool later for live deep lookup against the local qmd index.
- Do not make remote qmd availability a prerequisite for Hermes/Bilby/cc memory correctness.

---

### rodaddy — 2026-06-18T10:05:34Z

Closed by PR #135 / merge 2c8fea33ba22ea4c329d7b75c173b3e70fe97135.

Verification receipts:
- PR #135 checks passed on head 258883c78fa02ad91819d0d4ca54392c0e645267: CI check, python-package, Claude Code Review, GitGuardian.
- Local validation passed: bunx tsc --noEmit; bun test scripts/__tests__/promote-qmd-repo-facts.test.ts src/tools/__tests__/repo-facts.test.ts (22 pass); full bun test (697 pass, 16 skip).
- Hosted checkout on 10.71.1.21 fast-forwarded to main at 2c8fea33ba22ea4c329d7b75c173b3e70fe97135.
- Hosted OB list_repo_facts returned the 12 King qmd-derived facts in namespace collab / collection king.
- GPU-less Bilby canary on 10.71.1.71 retrieved the king-core ApiResponse repo fact via mcp2cli open-brain list_repo_facts and followed the pinned GitHub source pointer for src/types/api.ts.

Scope note: this PR is only the Open Brain qmd repo-fact promotion slice. Hermes channel recall, explicit cross-channel lookup, repo-fact read path, current/short-term lanes, and OB write spool/replay verification remain separate Hermes-side workstreams.
