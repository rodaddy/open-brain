<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #678 — [cutover-blocker] schema_hash drift receipt is blind — contract-schemas.ts mirror lags the live Zod schema; #563/#639 shape change moved no hash

State: OPEN
Author: rodaddy
Labels: none
Created: 2026-08-09T04:32:39Z
Updated: 2026-08-09T04:32:39Z

---

Found by the pre-cutover downstream audit (2026-08-09, read-only). Two related downstream-contract blockers for the #563 bounded-recall rollout.

## BLOCKER 1 — the drift receipt cannot move (WRITTEN, receipt broken)
docs/downstream-rollout.md:112 calls schema_hash "the authoritative drift receipt." But it is computed over src/contract-schemas.ts — a HAND-MAINTAINED mirror — not the Zod schema that actually serves. The mirror last changed at fe19956 (#361), BEFORE #639. Measured this session:
- agent_context_pack actual inputSchema vs manifest: IN ACTUAL NOT IN MANIFEST: repo, prior_context, continue_from. manifest version still 2.
- contract_version 2026-07-23.memory-tools.v23, schema_hash 4b69e9b4...a4a8, MATCH vs the Python client's pinned hash: true — before AND after the shape change.
Every downstream compat check (_runtime_router.py:375-382 pinning COMPATIBLE_CONTRACT_VERSIONS + exact hash) passes identically pre/post cutover. The rollout step "record schema_hash as the authoritative drift receipt" is unrunnable for #563 — same value either way.

FIX: reconcile src/contract-schemas.ts with the real Zod schema (repo, prior_context, continue_from), bump agent_context_pack past version:2, bump CONTRACT_VERSION off v23, re-pin CURRENT_CONTRACT_SCHEMA_HASH in python/openbrain-memory/src/openbrain_memory/client.py:60. Add a test asserting manifest keys == Zod keys so this drift can't recur silently. Without this, all other rollout steps have no receipt to record.

## BLOCKER 2 — no client walks the bounded-recall continuation (under-read, silent)
The additive/parse-safe half is VERIFIED (next emitted conditionally, response path allowlist-projects, old client won't crash). But `rg -rn 'continue_from|"next"|burst' python/openbrain-memory/src/` → no matches. Post-cutover, a budgetless broad agent_context_pack recall silently returns ~10 records where it previously returned the matching set (60 in #639's fixture), parses fine, reports success, answers from a truncated corpus. No error — degraded recall that looks healthy. Detecting it needs reading budget.recalled_net_new, which nothing does.

FIX: survey mcp2cli / rtech-hermes for budgetless broad callers; per caller either walk `next` or set an explicit budget. This is the risk steps 2 and 5 of the rollout catch — skipping them leaves it unobserved.

## SHOULD-FIX — hosted verification doesn't cover the changed surface
core01-deploy-local.sh:148 post-health test is search-all.test.ts, regression-only, no burst/continue_from/durable_memory coverage. scripts/done-means/563-bounded-recall.sh is the right probe but isn't wired into the deploy path. Wire it, or run it against hosted after deploy.

## VERIFIED HONEST (no action, recorded for trust)
PR checkboxes audited and found accurate: #639 leaves all four rollout boxes deliberately unchecked and says the work is outstanding; #657 checks mcp2cli N/A (Python-only diff, verified) and refuses the Hermes boxes as unrun; #664/#668 N/A defensible. #563's "MERGED, not deployed" is exemplary grammar. The #654/#657 delegation change IS safe to cut over — default-OFF verified both directions. Python client 0.9.0 satisfies the server floor >=0.1.15 <1.0.0.

NOTE: no cutover tracking artifact exists (no cutover issue, nothing in issue-graph.md). #563 carries the outstanding rollout in a comment body. Recommend a tracked cutover checklist.

All source-read; core01 not contacted (two-host rule).
