<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #39 — feat: extend MCP/mcp2cli with lane-aware OB tools

State: CLOSED
Author: rodaddy
Labels: enhancement
Created: 2026-06-04T22:17:49Z
Updated: 2026-06-08T05:17:02Z
Closed: 2026-06-08T05:17:02Z

---

## Goal
Harden and extend the Open Brain MCP/mcp2cli server surface for lane-aware memory operations.

## Motivation
Agents should call OB through tools, not hand-roll conventions in prompt text. The current operational failure mode is agents skipping OB or substituting Honcho/session vibes. First-class tools should make the right path easy.

## Proposed tool additions
- `start_session_lane`
- `session_context`
- `append_session_event`
- `link_entity`
- `wrap_session_lane`
- `list_active_lanes`
- `adjacent_context`

## Requirements
- Keep namespace/auth rules explicit.
- Return compact, receipt-friendly output.
- Avoid dumping large standing guidance into Discord/tool notifications.
- Gracefully degrade when OB is unavailable.
- Keep current tools (`search_brain`, `log_thought`, `log_decision`, `session_save`, etc.) backward-compatible.

## Acceptance criteria
- mcp2cli can call every new lane tool with JSON params.
- Tools are covered by tests.
- Errors identify the failed operation and do not leak secrets.
- Agents can recover active work after compaction/reset with a single lane lookup.

---

## Resolution

Closed by **PR #47** — feat: entity graph tools, qmd timeout fix, OB memory contract (#39, #40)

- Linkage: GitHub recorded this pull request as the closer.
- Merge commit: `0c6cea199adf5ec0a660ea8d67e592392de48d1d`
- Merged at: 2026-06-08T05:17:01Z
- PR state: MERGED
- Issue closed: 2026-06-08T05:17:02Z by rodaddy (COMPLETED)

### Direction taken and why — PR #47 body

> ## Summary
> Completes the remaining open issues for OB v2.
>
> ### Entity Graph Tools (#39)
> - **`upsert_entity`**: Idempotent entity creation/update with embedding, metadata merge on conflict
> - **`link_entities`**: Create/update links between any two nodes (13 relation types), self-link prevention, weight + metadata upsert
> - **`adjacent_context`**: Traverse link graph from a source node — outgoing/incoming/both, relation + namespace filtering
>
> ### QMD Timeout Fix
> - Replace racy `clearTimeout` pattern with `Promise.race` in `searchQmd`
>
> ### OB Operational Memory Contract (#40)
> - `docs/memory-contract.md`: Defines what OB is/isn't, memory tiers (hot/warm/cold), agent workflow (start → events → checkpoint → resume), namespace conventions, full tool reference
>
> ### Shared Constants
> - `LINK_RELATIONS` added to `table-constants.ts` (13 values matching `LinkRelation` type + DB CHECK)
>
> ## Test plan
> - [x] `bunx tsc --noEmit` passes
> - [x] 520 tests pass (50 new)
> - [x] Auth gates verified on all 3 tools
> - [x] Self-link prevention tested
> - [x] All 13 relation types tested
> - [x] Embedding failure non-fatal
>
> Closes #39, closes #40
>
> 🤖 Generated with [Claude Code](https://claude.com/claude-code)

---

## Discussion (1)

### rodaddy — 2026-06-08T05:08:06Z

5 of 7 proposed tools now shipped:
- `session_start` (≈ start_session_lane) — PR #46
- `session_context` — PR #45
- `append_session_event` — PR #45
- `session_wrap` (≈ wrap_session_lane) — PR #46
- `lane_load` (≈ list_active_lanes) — PR #43

Remaining: `link_entity` and `adjacent_context` depend on the entity graph schema (PR #42, merged). These can be built as follow-ups.
