<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #643 — flaky: ob-backfill quadratic-blowup test asserts an absolute wall-clock threshold (1005ms vs 1000ms)

State: OPEN
Author: rodaddy
Labels: none
Created: 2026-08-08T16:53:54Z
Updated: 2026-08-09T16:42:30Z

---

`scripts/ob-backfill.test.ts:259` asserts a wall-clock duration:

```ts
expect(performance.now() - startedAt).toBeLessThan(1_000);
```

Observed failing in CI at **1005.5852320000013 ms** — missing by 5.6 ms — on run 31267844990 (`db-integration`, `Test (JUnit reporter for the anti-skip guard)`). A re-run of the **same SHA** (30efb2a, run 31267860992) passed with the identical tree, so this is timing, not behavior. Suite result on the failing run: 3711 pass / 34 skip / **1 fail**.

This is the flake class already recorded in `docs/lane-contract.md` Tightenings (2026-08-08 round 5): "Wall-clock assertions (`toBeLessThan(1000)` ms) are CI flake generators" — previously filed as #632 and #634 for other instances. A shared runner under load makes any absolute millisecond threshold a coin flip near the boundary.

**What the test is actually defending:** that `sanitize()` is not quadratic in token count. That property is better expressed as a RATIO between two input sizes (e.g. 12k tokens takes < ~4x the time of 3k tokens) than as an absolute ceiling, because the ratio is robust to a slow or contended runner while still failing loudly on real quadratic blowup.

Found by the #563 lane (PR #639), which does not touch this file — reported rather than absorbed, per the lane contract.

---

## Discussion (1)

### rodaddy — 2026-08-09T16:42:30Z

Two more datapoints from tonight's cutover-blocker lanes (2026-08-09), both on branches that touch neither `scripts/ob-backfill.ts` nor its test:

- **PR #690** (lane/677, scheduled backup) — `check` FAILED at SHA `adb766bf`: `Expected: < 1000` / `Received: 1112.08`. 3760 pass / 35 skip / **1 fail** across 3796 tests; the one fail is this test.
- **fix/679-register-dev-safety-gates** — the SAME test failed on `pull_request` at SHA `585a9772`, and **the identical SHA passed on its `push` run**. That is the two-runs-same-SHA flake signal, on a SHA neither run's branch shares with #690.

Differential evidence that it is not branch-owned, for #690 specifically:
- `git diff origin/main -- scripts/ob-backfill.test.ts scripts/ob-backfill.ts` is EMPTY — neither the test nor its subject is touched.
- Clean `origin/main` in a separate worktree with its own fresh database ran the file 3 consecutive times: 22 pass / 0 fail each time.
- Nothing in the #690 diff references `ob-backfill`; the change is operator tooling (a launchd schedule, a deploy/backup advisory lock, a doc section).

Reading: the assertion is a bare `performance.now()` delta against a fixed 1000ms ceiling on a pure-CPU function (`scripts/ob-backfill.test.ts:251-260`), so what it actually measures is how contended the runner was. Observed spread so far is 1005ms → 1112ms, both marginal. Two concurrent lane PRs in the same window is exactly the contention shape.

Not absorbed into either lane and not retried into green; reported here per the lane contract's "file the flake instead of absorbing it" rule.
