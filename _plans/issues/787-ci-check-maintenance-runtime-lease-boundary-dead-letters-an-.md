<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #787 — CI check: 'maintenance runtime lease boundary > dead-letters an expired lease' fails intermittently (expected "succeeded")

State: OPEN
Author: rodaddy
Labels: bug
Created: 2026-08-26T17:04:47Z
Updated: 2026-08-26T17:04:47Z

---

Observed 2026-08-26 17:02Z on PR #786 (head e8e3b51, CI run 32991755910, job 98250710941): `(fail) maintenance runtime lease boundary (live Postgres) > dead-letters an expired lease that has consumed its attempts instead of reclaiming it` — `expect(received).toBe(expected)`, Expected: "succeeded". 3877 pass / 1 fail across 249 files.

The PR touches only `server/tools/search-brain.ts` and a new `server/tools/tool-failure.ts`; nothing under `server/maintenance/` or the queue SQL. The same server tree (origin/main c1ec709) passed this test on PR #785's CI run 32991176097 minutes earlier, so the failure is timing/state dependent, not diff dependent. Same family as #614 (shared-state test drift) — this one runs against CI's fresh per-run database, so the suspect is lease-expiry timing inside the test rather than shared data.

Next: open `server/maintenance/*.pg.test.ts` (or wherever the `lease boundary` describe lives), find the expiry wait, and either make the expiry deterministic (inject the clock) or assert on the dead-letter row rather than the transient status.
