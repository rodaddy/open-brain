<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #209 — Add TypeScript thin client memory wrapper

State: CLOSED
Author: rodaddy
Labels: none
Created: 2026-06-26T05:13:53Z
Updated: 2026-06-27T02:02:46Z
Closed: 2026-06-27T02:02:46Z

---

## Goal
Add a lightweight TypeScript wrapper equivalent in spirit to the Python adapter so local tools and Hermes-adjacent TypeScript callers can use Open Brain as a memory manager.

## Scope
- Typed wrapper around session_start, session_context, append_session_event, lane_upsert, search_all, brain_answer, session_wrap, and repo facts.
- Helper methods for adapter contract lifecycle calls.
- Retry/spool behavior or explicit deferral if not in this slice.
- Metadata safety checks, including protected authority keys and bounded metadata.
- Support metadata.okf and receipt metadata without forcing OKF storage.

## Acceptance Criteria
- TypeScript wrapper has focused fake-transport tests for call shape and error handling.
- Wrapper exposes lifecycle methods that map to the adapter contract.
- Public contract/version implications are documented.

## Timeline
Target date: 2026-06-27.

Parent plan: docs/agent-memory-substrate-plan.md

---

## Discussion (2)

### rodaddy — 2026-06-26T21:51:52Z

TypeScript wrapper local receipt:

- Added `src/agent-memory.ts`, a thin TypeScript wrapper over a caller-provided `callTool(name, args)` transport.
- Implemented lifecycle methods matching the Phase A adapter contract: `start`, `recall`, `appendEvent`, `compact`, `wrap`, `recordReceipt`, `nominateShared`.
- Left `exportDisclosureBundle` explicitly deferred to #215, matching the manifest status of planned.
- Added metadata authority-key rejection so callers cannot smuggle auth, namespace, token, headers, role, or session authority through wrapper metadata.
- Added `src/__tests__/agent-memory.test.ts` fake-transport tests for call shapes, receipt metadata, share nominations, compact/wrap, and metadata rejection.

Validation:
- `bun test src/__tests__/agent-memory.test.ts` passed: 7 tests.
- `bunx tsc --noEmit` passed.
- `git diff --check` passed.

Residual risk:
- This does not add HTTP/MCP transport or retry/spool behavior; callers provide transport in this slice. Receipt helper expansion remains tracked by #212.

---

### rodaddy — 2026-06-26T22:03:06Z

TypeScript wrapper update receipt:

- Expanded `src/agent-memory.ts` to include `refreshLane` over `lane_upsert`, `listRepoFacts` over `list_repo_facts`, and `upsertRepoFact` over `upsert_repo_fact`.
- Kept lifecycle helpers for `start`, `recall`, `appendEvent`, `compact`, `wrap`, `recordReceipt`, and `nominateShared` aligned with the adapter contract.
- Added metadata safety bounds for max 50 keys, 100-character keys, 100KB JSON, and reserved authority-key rejection.
- Documented that this thin wrapper uses caller-provided transport and explicitly defers HTTP/MCP transport construction plus retry/spool durability.

Validation:
- `bun test src/contract.test.ts src/__tests__/agent-memory.test.ts` passed: 16 tests.
- `bunx tsc --noEmit` passed.
- `git diff --check` passed.

Residual risk:
- No built-in retry/spool implementation in this slice; callers provide transport and any offline durability. Export disclosure remains deferred to #215.
