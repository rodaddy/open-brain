<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #299 — P2: Add Claude-Codex-Python first-class memory compatibility matrix

State: OPEN
Author: rodaddy
Labels: enhancement, memory, closure-audit
Created: 2026-07-19T02:21:13Z
Updated: 2026-08-05T22:43:22Z

---

Parent: #293

## Problem
Claude component canaries do not prove portable continuity across Codex and the native Python application. Supported contract, package, adapter, and server combinations need executable release gates.

## Acceptance criteria
- Define supported server contract, package, adapter, and client-version combinations for Claude, Codex, and native Python.
- Test save, load, natural checkpoint, lifecycle recovery, namespace isolation, exact scope, migration, spool/replay, receipts, and restore for all clients.
- Prove Claude→Codex, Codex→Claude, Claude/Python, and Python/Claude continuity on the same intended lane.
- Include clean install, existing-state upgrade, rollback, outage, mixed versions, migration, and restore-after-upgrade fixtures.
- Publish upgrade sequencing and deprecation windows.
- Block releases on matrix failures rather than accepting source-only or one-runtime canaries.
- Keep #293 open until Codex and native Python independently pass the same behavioral, isolation, provenance, and recovery requirements.

## Non-goal
Hermes remains shelved and does not block this matrix.

---

## Discussion (5)

### rodaddy — 2026-07-20T07:23:16Z

### Review-lane status pass (Claude + GPT-Sol runner lanes, controller-verified)

Two independent runner lanes, both cloned. Agreement: **real per-runtime contract-version machinery exists, but "Codex" is not a versioned client anywhere and there is no cross-client matrix gate.**

- Supported server/package/adapter/client combinations: **PARTIAL** — `src/contract.ts:buildContractManifest` advertises contract/tool versions + `compatible_client_ranges`; Python `contract.py:validate_required_memory_contract` enforces version/hash/tool compatibility. Keep this as the matrix foundation — it's solid, don't rebuild. But there's no Claude/Codex adapter-version table.
- **Codex is not a versioned client: GAP** — it appears only as a review bot, a free-text agent label, and one smoke script (`scripts/codex-memory-smoke.ts`). No Codex adapter with a negotiated contract version.
- Full behavior matrix for all clients: **GAP** — TS and Python suites are separate; no parameterized Claude/Codex/Python save→load→checkpoint→restore→replay matrix.
- Cross-client continuity (Claude→Codex, Codex→Claude, Claude↔Python): **GAP** — no executable directional continuity tests.
- Install/upgrade/rollback/outage/mixed/migration/restore fixtures: **PARTIAL** — CI clean-installs + builds both stacks and tests migration DB; no mixed-version, stateful upgrade/rollback, outage, or restore-after-upgrade fixtures.
- Upgrade sequencing / deprecation windows: **PARTIAL** — `docs/downstream-rollout.md` defines contract-first rollout checks; no explicit three-client sequencing or version deprecation windows.
- Release-blocking matrix: **GAP** — `.github/workflows/ci.yml` gates TS DB tests and the Python package independently; no cross-client matrix job/canary that can block a release.
- #293 closure on independent Codex/Python parity: **UNCLEAR** — governance not provable from source; the executable independent behavioral/recovery gate is absent.

**TOP GAP:** no release-blocking shared-lane cross-client continuity matrix. Since Rico is now actively testing Claude + GPT together via Claudex, this is the issue that current testing most directly feeds — worth capturing those runs as the seed fixtures.

---

### rodaddy — 2026-07-20T21:05:07Z

Re-verified 2026-07-20: still true. CI (`.github/workflows/ci.yml`) has exactly four jobs — `check`, `db-integration`, `python-package`, `deploy` — with no cross-client (Claude/Codex/Python) compatibility matrix and no release-blocking client-contract gate. `codex-review.yml` is a PR review bot, not a versioned-client gate, and "Codex" is not represented as a versioned client anywhere in the repo. Confirmed out of scope for PR #305.

---

### rodaddy — 2026-07-22T01:45:20Z

Closed by PR #315 (d205666): `docs/compatibility-matrix.md` renders the supported client × capability matrix from the machine-enforced sources (`src/contract.ts` min_client_versions/ranges, `contracts/memory/parity-manifest.json`), maps Codex to its real versioned surface (mcp2cli ≥0.3.6), and states the upgrade/deprecation policy. The enforcement itself landed in #313 (contract-parity CI gate binding both in-tree clients, deploy needs-blocked) — this page is the human-readable rendering with a machine-data-wins rule. Named deferred (safe, no acknowledged-data risk): executable cross-runtime handoff tests and mixed-version upgrade fixtures — documented in the page's 'what this matrix does not claim' section; revisit if cross-runtime lane handoff becomes a live workflow.

---

### rodaddy — 2026-07-24T19:26:24Z

Closure audit 2026-07-24 (post-Codex-era review)

**What was claimed**
The issue required executable behavioral coverage for all clients, directional Claude/Codex/Python continuity on the same lane, clean install, stateful upgrade/rollback, outage, mixed versions, migration, restore-after-upgrade, and a release-blocking matrix.

**What exists**
The current parity harness is valuable and real:

- Python and TypeScript consume shared fixtures.
- CI validates the parity manifest.
- Deploy depends on the parity job.
- Codex is mapped to its actual versioned `mcp2cli` surface.

But `docs/compatibility-matrix.md:69-77` explicitly states:

- no executable Claude→Codex or Codex→Claude handoff tests exist;
- same-runtime fixtures do not prove cross-runtime continuity;
- no mixed-version stateful upgrade/rollback fixtures exist.

The matrix also does not execute the issue's full migration, restore, outage, and directional handoff matrix for every named client.

**Remediation required before closure**
Add a release-blocking cross-runtime fixture runner over one intended lane, cover every directional handoff, and add stateful mixed-version install/upgrade/rollback/outage/migration/restore fixtures. Keep the current same-runtime parity gate as the lower layer.

---

### rodaddy — 2026-08-05T22:43:22Z

**Stale-blocker sweep verdict (2026-08-05): PARTIAL** — every issue this one references is closed, so it was audited against live state (scripts/stale-blockers.ts flagged it; a verification lane checked the acceptance itself).

**What actually remains:**
Two acceptance bullets, both self-declared open in `docs/compatibility-matrix.md:69-77` on trunk:

1. Executable cross-runtime continuity fixtures on one intended lane, covering all four directions named in the issue: Claude→Codex, Codex→Claude, Claude→Python, Python→Claude. Must be a release-blocking CI job (or an added step in the existing `contract-parity` job at `.github/workflows/ci.yml:428`, which `deploy` already `needs`). The existing same-runtime parity harness stays as the lower layer.
2. Stateful fixtures for clean install, existing-state upgrade, rollback, outage, mixed client versions, migration, and restore-after-upgrade. Currently covered only operationally by `docs/downstream-rollout.md` and #298, which the matrix doc admits is not the fixture coverage the issue asked for.

When both land, delete the "What this matrix does not claim" bullets at `docs/compatibility-matrix.md:71-77` in the same PR — that block is the live marker for this issue's remaining scope. Item 7 (#293's gate) follows automatically once the recovery/continuity half is executable.

Alternative disposition, needs Rico: if cross-runtime lane handoff is still not a live workflow (the #315 deferral rationale, and the matrix doc's own "revisit if cross-runtime lane handoff becomes a live workflow" trigger), this could be deliberately descoped to the delivered same-runtime parity gate and closed with the two remaining items split into a new lower-priority issue. That is a product-shape call, not something the repo state decides.

**Stale text in this issue corrected by this comment:** Status check 2026-08-05 against trunk `origin/main`: still PARTIAL, and the two gaps named in the 2026-07-24 closure audit are unchanged. MET and machine-enforced: supported client/version combinations (`src/contract.ts` `CONTRACT_VERSION = 2026-07-23.memory-tools.v23`, `min_client_versions`, `compatible_client_ranges`, rendered at `docs/compatibility-matrix.md:15-21` with Codex correctly mapped to mcp2cli >=0.3.6); upgrade sequencing and deprecation policy (`docs/compatibility-matrix.md:57-67`); release blocking for the same-runtime layer (`.github/workflows/ci.yml:428` `contract-parity` replays the 13 `contracts/memory/` fixtures against Python, and `deploy` at line 494 lists it in `needs`). NOT MET: (a) executable Claude→Codex / Codex→Claude / Claude↔Python continuity on one lane, and (b) mixed-version stateful install/upgrade/rollback/outage/migration/restore fixtures — both still self-declared open in this repo's own `docs/compatibility-matrix.md:69-77` ("No executable cross-runtime handoff tests exist yet"; "No mixed-version stateful upgrade/rollback fixtures"), and `fd` finds no such test files. `git log origin/main -- docs/compatibility-matrix.md contracts/` shows no commit since #315/#313 that addresses either. Keep open, or make an explicit descope call on whether cross-runtime lane handoff is a live enough workflow to fund.
