<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #172 — get_contract must carry full per-field/per-tool help (not just generic schemas)

State: CLOSED
Author: rodaddy
Labels: none
Created: 2026-06-19T18:51:26Z
Updated: 2026-06-19T22:28:12Z
Closed: 2026-06-19T22:28:12Z

---

## Goal

Make `get_contract` a **complete instruction manual** for contract-driven agents — full per-field and per-tool help/how-to, not just generic schemas (type/required/length).

## Why (the load-bearing fact)

Hermes agents are **direct HTTP clients of the OB server** and are **100% contract-driven**: whatever `get_contract` returns is the agent's *entire* knowledge of what it can do and how. (rtech-hermes#89 built the contract-driven read surface; the plugin reshapes `tool_contracts[*].input_schema` into the LLM tool definitions.) So if the "how/when/why" of a capability isn't in the contract, the agent cannot know it.

## Current gap

`buildContract()` (src/contract.ts) today emits:
- `capabilities[]`: one-line `description` per tool. OK but terse.
- `tool_contracts[name].input_schema`: bare field schemas — `{type, required, minLength, max, values}`. **Almost no per-field descriptions.** The only field with real usage help is `share_candidate` (added for #161). Every other param (`content`, `query`, `tags`, `namespace`, `limit`, `search_mode`, ...) has type/constraints but **no description of what it does, when to use it, or what the server does with it**.

So a contract-driven agent knows the *shape* of each call but not *how to use it well*.

## Scope

- Add per-field `description` to every `tool_contracts[*].input_schema` property that needs usage guidance (especially non-obvious ones: namespace semantics, search_mode/sources tradeoffs, tier, importance, share_candidate behavior, lane/session keying).
- Add richer per-tool how-to where a one-line capability description is insufficient (when to use this tool vs another, what the server does, what gets refused/adjudicated).
- Ensure the rtech-hermes `contract_schema_to_parameters` reshaper **carries descriptions through** to the LLM tool definitions (coordinate — if the reshaper drops descriptions, the help never reaches the agent). NESTED object fields (e.g. metadata.fields.share_candidate) must also survive the reshape.
- Bump `CONTRACT_VERSION`; sync Python `CURRENT_CONTRACT_VERSION` (parity drift-guard #157 stays green); update `src/contract.test.ts`.

## Acceptance

- Every agent-facing tool's params carry usage descriptions in `get_contract`.
- A contract-driven agent, given only the contract, can correctly use each call (descriptions are self-sufficient).
- rtech-hermes reshaper passes descriptions (and nested fields) through to the LLM surface — verified live.
- Contract version bumped; TS + Python parity green.

## Relationship

- Foundational prerequisite for / co-design with #161 (lane→shared-kb): the `share_candidate` nomination only works if its full how-to reaches the agent via the contract.
- Consumed by rtech-hermes#89 (contract-driven plugin). Coordinate the reshaper.

## Provenance
Surfaced 2026-06-19 while designing #161: Rico clarified agents do everything from get_contract, so the contract must carry full help, not generics.

---

## Discussion (1)

### rodaddy — 2026-06-19T22:28:11Z

Implemented in PR #171 (merged 2026-06-19, squash 06d1560). get_contract now carries per-field + per-tool usage help across all tools/fields (67 fields, was ~16) so contract-driven agents get a real instruction manual. tool_contracts extracted to src/contract-schemas.ts to stay under the 750-line LAW (schema_hash unchanged by the split). CONTRACT_VERSION v4->v5, per-tool versions bumped, TS+Python lockstep + drift pins. Verified: TS+Python suites green, CI green.
