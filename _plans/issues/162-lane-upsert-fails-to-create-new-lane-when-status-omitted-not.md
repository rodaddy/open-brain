<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #162 — lane_upsert fails to create new lane when status omitted (NOT NULL violation; breaks bilby/skippy writes)

State: CLOSED
Author: rodaddy
Labels: bug
Created: 2026-06-19T14:22:23Z
Updated: 2026-06-19T15:02:21Z
Closed: 2026-06-19T14:52:36Z

---

## Bug

`lane_upsert` fails to CREATE a new session lane when the caller omits `status`, with:
```
null value in column "status" of relation "ob_session_lanes" violates not-null constraint
```

## Impact (live)

- **240 failures in 24h** on hosted OB (`skippy-oc` / 10.71.1.21): **69 bilby, 171 skippy**, 2026-06-18→19.
- This is an **upstream cause** of Hermes lane writes not landing — when the first write to a new `session_key` fails server-side, the Hermes side spools it and (per rtech-hermes#84's F2) mistakes "spooled" for "saved." So the rtech-hermes spooled-vs-saved symptom has a real open-brain root cause.
- It is NOT the previously-suspected "wedged worker, fix with a bounce" issue — both workers were healthy; this is a deterministic DB constraint violation. A bounce never fixed it; it only changed which session_keys were new.

## Root cause

`src/tools/lane-upsert.ts:126` set `status = args.status ?? null` and bound that NULL into the INSERT VALUES for the `NOT NULL` `status` column (default `'active'`). An explicit NULL overrides the column default → constraint violation on INSERT. The ON CONFLICT/UPDATE path guards on `$3 IS NULL` and preserves the stored status, so existing-lane updates succeeded (masking the bug; only new lanes fail).

## Fix

PR fixes it with a separate insert value (`insertStatus = args.status ?? 'active'` as `$24`) for the INSERT VALUES, keeping `$3` nullable for the ON CONFLICT preserve-logic, and re-points the `ended_at` CASE from `EXCLUDED.status` to `$3` (required — otherwise a status-omitted update would reactivate a wrapped/archived lane). Regression test added. Full suite 726 pass, typecheck clean.

## Follow-up
- The mock test harness can't fire the real constraint; a live-DB integration test would be a stronger guard (noted as optional).
- Deploy to hosted OB (`com.rico.open-brain` on 10.71.1.21) needed to actually stop the live failures.

---

## Resolution

Closed by **PR #164** — fix(lane-upsert): cast $3::text to fix param-type inference regression (#162)

- Linkage: GitHub recorded this pull request as the closer.
- Merge commit: `61f6710d411a4e3d17872c64d3484d02cdde4473`
- Merged at: 2026-06-19T14:52:35Z
- PR state: MERGED
- Issue closed: 2026-06-19T14:52:36Z by rodaddy (COMPLETED)

### Direction taken and why — PR #164 body

> ## Summary
>
> - Fix the regression introduced by #163: `lane_upsert` failed every call with `could not determine data type of parameter $3`.
> - #163 repointed the INSERT status slot to `$24`, leaving `$3` used ONLY in `CASE` comparisons (`IS NULL` / `= 'literal'`). With no typed binding site, Postgres cannot infer `$3`'s type at plan time → every `lane_upsert` failed.
> - Cast `$3::text` in all three comparison sites (status type is `TEXT`). Behavior identical: null stays null, equality unchanged.
> - Adds **env-gated DB-backed integration tests** (`OPENBRAIN_TEST_DATABASE_URL`) that run the real query through a real pool — the coverage gap that let both the original NOT NULL bug and this regression through.
>
> Fixes #162.
>
> ## Root cause
>
> `pg` sends parameters untyped and relies on Postgres inferring each param's type from a typed usage site (e.g. a column in INSERT VALUES). After #163, `$3` was no longer bound into the `status` column — it appears only as `$3 IS NULL` and `$3 = 'wrapped'/'active'/...`. None of those pin a type, so the plan-time inference fails. Cast resolves it.
>
> ## Why #163's tests didn't catch it
>
> #163's regression test was mock-only (the repo had no live-pg harness). A mock pool returns canned rows and never executes the SQL, so neither the original NOT NULL violation nor this type-inference failure could surface in tests. This PR closes that gap with real-pool tests.
>
> ## Validation
>
> - Manual repro against live PG: bare `$3` in comparisons → `ERROR: could not determine data type of parameter $1`; `$3::text` → works.
> - DB-backed integration tests run against live Postgres (hosted OB DB): **2 pass** — (a) new lane omitted-status creates `status='active'` with no inference error; (b) status-omitted update of a wrapped lane preserves `status` + `ended_at`.
> - Default suite (mock, DB tests skip): 22 pass, 2 skip, 0 fail. `bunx tsc --noEmit` clean.
>
> ## Critical Self-Review
>
> - Highest-risk behavior: The `::text` casts on `$3`. Verified against live PG that casts make the query plan and execute, and that null/equality semantics are unchanged (a status-omitted update still preserves status and ended_at — proven by DB test, not just reasoning).
> - Assumptions that could be wrong: That `TEXT` is the correct cast target. Confirmed against migration `012_session_lanes.sql` (`status TEXT NOT NULL`).
> - Missing/weak tests: Prior to this PR, the lane_upsert path had NO real-DB coverage — that is exactly why #163's regression shipped. This PR adds it (env-gated). The default CI run still won't execute them unless a test DB URL is provided; running them in CI would require provisioning a Postgres service (noted as a follow-up worth doing).
> - Security/permission risk: None — no auth/namespace logic touched.
> - Migration/deploy risk: No schema change. Requires redeploy on hosted OB (the previous deploy of #163 is live and broken).
> - Downstream client/runtime risk: Positive — unblocks Bilby/Skippy lane writes that currently 100% fail. Bilby spool drain (rtech-hermes#84) resumes only after this is live AND verified from the client side.
> - Rollback/cleanup concern: Pure SQL/test change, trivially revertible. Diagnostic `log_thought` 5f3db27d in bilby's namespace still pending owner cleanup.
> - Fixes made before PR: `$3::text` casts (3 sites); DB-backed integration tests; updated the mock test's SQL assertion to match the cast.
> - Known residual risk: I previously declared #163 "verified/deployed" on ~40s uptime with no real lane traffic — a false positive. This time verification is a real query through a real pool PLUS a post-deploy check against actual Bilby/Skippy lane traffic before declaring success.
> - SME review-memory update: [x] `docs/sme/` updated or [ ] not applicable because:
>
> ## Review Gate
>
> - [x] Critical self-review fields above are filled with specific, non-placeholder content
> - [x] MEDIUM+ review findings were captured in `docs/sme/` or explicitly marked not applicable
> - Live Open Brain checks: [x] linked below or [ ] not applicable because:
>
> Live checks: DB-backed integration tests pass against the hosted OB Postgres (2 pass). Post-deploy, will verify real Bilby/Skippy lane_upsert traffic succeeds (no new `lane_upsert_db_error`) before closing #162.
>
> ## Downstream Rollout
>
> - [x] I checked `docs/downstream-rollout.md`
> - [x] rtech-mcps handoff is complete or not applicable
> - [x] mcp2cli cache/skill refresh is complete or not applicable
> - [ ] rtech-hermes Python runtime/plugin changes are complete or not applicable
> - [ ] Hermes live rollout/canaries are complete or not applicable
>
> Notes/evidence:
>
> - Server-side SQL fix; no client/schema/contract change. rtech-hermes items unchecked: Bilby spool drain (34 stuck `lane_upsert`) resumes after this deploys and is verified client-side (rtech-hermes#84, owner-driven).
>

---

## Discussion (3)

### rodaddy — 2026-06-19T14:42:34Z

## ⚠️ Deployed fix REGRESSED lane_upsert — still 100% failing, new error (2026-06-19)

Verified live from Bilby's real Hermes interpreter, replaying an actual stuck `lane_upsert` spool entry against hosted OB (commit `b49e5d4` deployed). The original NOT-NULL error is gone, but it's been **replaced by a new failure**:

```
lane_upsert body=Database error during lane upsert: could not determine data type of parameter $3
```

**This still breaks every lane_upsert.** The "240/24h stopped after restart" reading was a **false positive** — the service had only ~40s uptime with no lane traffic, so no new errors had a chance to fire. The first real lane write (mine, just now) fails.

### Root cause of the regression

The fix repointed the INSERT VALUES status slot to `$24` (`insertStatus`), leaving `$3` (`status`, `null` when omitted) used **only** in `CASE WHEN` comparisons:

- `status = CASE WHEN $3 IS NULL THEN ... ELSE EXCLUDED.status END` (line ~189)
- `ended_at = CASE WHEN $3 = 'wrapped' OR $3 = 'archived' ... WHEN $3 = 'active' ...` (lines ~204-208)

Postgres infers a bind parameter's type from a typed usage site (e.g. a column position). With `$3` no longer bound into the `status` column and used only in `IS NULL` / equality-vs-string-literal, **its type is undeterminable at plan time** → `could not determine data type of parameter $3`. A real DB-backed query exercises this; a mock harness does not (the PR honestly flagged the mock limitation).

### Fix — cast `$3` to its column type everywhere it's used

`status` is `TEXT`, so pin the type with an explicit cast (behavior identical; null stays null, comparisons unchanged):

```sql
status = CASE WHEN $3::text IS NULL THEN ob_session_lanes.status ELSE EXCLUDED.status END,
...
ended_at = CASE WHEN $3::text = 'wrapped' OR $3::text = 'archived'
           THEN COALESCE(ob_session_lanes.ended_at, NOW())
           WHEN $3::text = 'active' THEN NULL
           ELSE ob_session_lanes.ended_at END
```

**Also verify:** `$24` (`insertStatus`) is actually appended to the params array (the array tail wasn't fully visible) — if `insertStatus` isn't bound at position 24, that's a second bug.

**Strongly recommend** a DB-backed regression test (real pool, not mock) covering: (a) new lane, status omitted → row created with `status='active'`; (b) omitted-status update of a `wrapped` lane → `status` stays `wrapped`, `ended_at` unchanged. The mock test can't catch type-inference or constraint failures — this regression is the proof.

### Operational state
- **Live deploy is still broken.** Lane writes from Bilby/Skippy continue to fail (new error).
- **Bilby spool drain is ON HOLD** — replaying the 34 stuck `lane_upsert` entries against a still-broken path would just re-spool them. Will drain once a corrected fix is live and verified from the client side.
- Reopening this issue — the tracked fix is not yet working in production.

---

### rodaddy — 2026-06-19T14:54:15Z

## ✅ Fixed and verified live (real MCP path, not a false positive this time)

Two PRs were needed — the first fix regressed, caught from Bilby's real interpreter:
- **#163** fixed the NOT NULL violation but introduced `could not determine data type of parameter $3` (the `$3`-only-in-CASE inference bug).
- **#164** casts `$3::text` at all three comparison sites + adds **env-gated DB-backed integration tests** (the coverage gap that let both bugs through).

### Live verification (deployed commit `61f6710` on 10.71.1.21, pid 44822)
- **Real MCP `lane_upsert` via mcp2cli, status OMITTED** → `{"success":true,"status":"active","is_new":true}`. Confirmed in DB (`status=active`, ended_at null), verify row cleaned up.
- **DB-backed integration tests pass against live PG** (2 pass): new-lane omitted-status + status-omitted update of a wrapped lane (preserves status + ended_at).
- **Zero new `lane_upsert_db_error`** since redeploy; last error `14:41:24Z` predates it.

### Honest note
My first "240/24h stopped" reading on #163 was a **false positive** — declared on ~40s uptime with no real lane traffic, verified against hand-written SQL rather than the actual parameterized query. This time verification is a real write through the real MCP path, confirmed in the DB. Lesson captured in `docs/sme/correctness.md` (mock pools can't catch SQL constraint/param-type failures).

### Still owner-driven (rtech-hermes#84)
- Bilby's 34 stuck `lane_upsert` spool entries are now drainable — replay needs a nudge (won't self-fire). The path is verified working, safe to drain.
- Cleanup: diagnostic `log_thought` `5f3db27d` in bilby's namespace.

Closing — open-brain side complete and verified.

---

### rodaddy — 2026-06-19T15:02:21Z

## Diagnostic thought 5f3db27d — RESOLVED, nothing to delete

Reconciled across both sides:

**At write time (Bilby receipt):** the probe `log_thought` returned a real persisted server-side record — `{"id":"5f3db27d-f66c-44b3-a3d5-f98f50ef1a28","namespace":"bilby","embedded":true,"merged":false}`. So it did land and was embedded.

**Now (open-brain DB audit):** searched the exact full UUID `5f3db27d-f66c-44b3-a3d5-f98f50ef1a28` against all four tables (`ob_entities`, `ob_links`, `ob_session_events`, `ob_session_lanes`) — **0 rows**. Also 0 as a `canonical_id` (didn't survive as a merge target under a different id), and **0 bilby entities in the entire probe time window** (10:54–10:58 on 2026-06-19). The durable row is genuinely absent from OB's current data.

Both observations are honest and consistent: persisted then, gone now (swept/deduped/transient). **There is nothing to delete.**

Explicitly NOT done: did not delete any of the unrelated `diagnostic`-tagged real memories (corrections/facts) to satisfy the checkbox — that would destroy real data for a cosmetic cleanup.

Spool drain (Bilby side) is complete: 0 backlog, spool self-healed under real agent traffic once the fix went live, backup `.bak-20260619-pre-drain` retained. Cleanup item closed.
