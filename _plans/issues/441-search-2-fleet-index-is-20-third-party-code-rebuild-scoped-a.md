<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #441 — SEARCH-2: fleet index is 20% third-party code — rebuild scoped, and get buzz out of Development

State: OPEN
Author: rodaddy
Labels: none
Created: 2026-07-29T18:22:48Z
Updated: 2026-07-29T18:22:48Z

---

The cross-repo `fleet` index carries a third-party repository as its second-largest collection, and that repo's working copy has been dirtied by an index config commit.

## Measured 2026-07-29

```
fleet.sqlite: 10,900 documents
  buzz: 2,205  (20% of the entire index)
```

`buzz` is `github.com/block/buzz` — Block's open-source Rust project. It sits as a top-level repo in `/Volumes/ThunderBolt/Development`, so the fleet builder treats it as first-class code and it ranks against Rico's own repos in every `aqmd all` query.

Its most recent commit:

```
26c942383 2026-07-29 Rodaddy chore(qmd): track the project-local search index allowlist
```

An agent ran `qmd-backfill` on a third-party checkout and committed the index allowlist into it.

## Why this happened

Not carelessness — a documentation reachability failure. `_DOCS/QMD_INDEXES.md` documents the correct pattern (a named index over read-only clones, sources untouched) and is in no search index (#440). The `qmd-reference-index` header states the rule directly:

> qmd-backfill gives a repo YOU OWN its own .qmd/ index... **wrong for an upstream clone**: it dirties a third-party checkout, so `git pull` stops being clean.

The agent could not reach either. `open-brain` is the only project with a correctly-built research index, so there was no second example to copy.

## Scope

1. Delete and rebuild `fleet.sqlite` **scoped**, excluding prior art. It is a cache in `~/.cache/qmd/` — expendable, rebuildable, no durable state. `ROOT=$DEVROOT` unscoped is what produced the current index.
2. Revert `26c942383` in `buzz` so the checkout is pristine and `git pull` stays clean. **Separate git boundary** — that lands in that repo, on its own branch, with its own message.
3. Move `buzz` out of Development to a research root; index as a named research index if `ai-agents` actually needs it.
4. Verify `ai-agents` references it — as of 2026-07-29 it contains **zero** references to `buzz`, so it may not be wired to anything.

## Prevention

Root cause is #440 plus the missing rule in canon (#438). Fix the reachability and the next agent does not repeat it.

Design: `_plans/canon-always-known.md`
