<!-- GENERATED by scripts/sync-issues.ts. Do not edit: this file is
     overwritten on every sync. Authored plans live in _plans/<n>-<slug>.md. -->

# #683 — [cutover-should-fix] Test/CI: #563 bound's only catchers are Postgres-gated; anti-skip guard covers 16 of 51 live suites; #665 mechanism corrected

State: OPEN
Author: rodaddy
Labels: none
Created: 2026-08-09T04:39:55Z
Updated: 2026-08-09T04:39:55Z

---

Found by the pre-cutover test/CI integrity audit (2026-08-09). All mutation-proven (known violation injected, caught, restored, tree verified clean).

## Reassuring headline (recorded for trust)
The suite IS trustworthy as cutover evidence via `bun run test:isolated` (3761 pass / 35 skip / 0 fail this session) or the CI db-integration job. scripts/test-isolated.ts genuinely provisions + drops (prefix-guarded, drops on all signals); CI db-integration runs assert-db-tests-ran.ts (anti-vacuity gate); CI check fails loudly if Postgres absent. Load-bearing paths mutation-proven COVERED: auth/namespace predicate (disabled it -> 2 fail), #563 burst bound (DURABLE_MEMORY_BURST_ITEMS 10->100000 -> 2 fail), delegation header both directions, capture spool in-process.

But: bare `bun test` with no DB URL = 3366 pass / 525 skip / 0 fail, EXIT 0. **490 tests vanish, exit stays 0** — the silent-pass signature. Never cite a bare bun test full-suite count as evidence (already repo policy re #614; reconfirmed).

## SHOULD-FIX
1. **The #563 burst bound's only two catchers are Postgres-gated** (sdk-protocol.pg.test.ts:81, server-tool-parity.test.ts:50). Under bare `bun test` the burst mutation goes COMPLETELY undetected. The bound is covered exactly where the harness is strongest and nowhere where it's weakest. Consider a non-DB unit test of the burst bound too.

2. **assert-db-tests-ran.ts guards 16 of 51 live-Postgres suites; 35 unguarded.** Floor MIN_TOTAL_LIVE_TESTCASES=65 against ~490 DB-gated tests. Unguarded include get_entity namespace isolation, graph/people namespace isolation, promote_shared/list_namespaces, ingest_raw_turn, and every migration suite 025-032. These can silently skip and CI stays green — the guard's own failure mode applied to suites it doesn't name. TWO of the unguarded are namespace-isolation (security boundary). Add them to the guard's named-suite list.

3. **#665 mechanism corrected.** The issue says db-integration "runs against a shared CI database." Not quite: the container is per-run (obci-pg-${run_id}-${run_attempt}, random port), so cross-run DB sharing is NOT the mechanism. The real collision: all six jobs share runs-on [self-hosted,Linux,rodaddy] with NO workflow-level concurrency block (only deploy has one, ci.yml:527), and the fixture uses fixed namespace test-promote-shared-live + fixed content literal (promote-shared.test.ts:355,411). Concurrent push+PR jobs on the same runner can contend. The issue's fix direction (unique per-run fixture values) is right regardless; correct the mechanism note so nobody fixes the wrong layer. Consider a workflow concurrency block.

## NOTE
- Wall-clock assertions present (lane-contract round 5 flake generator): agent-context-pack-durable-lane-acquisition.test.ts:63,90 (toBeLessThan(250) vs 75ms sleep — tightest), sharing.test.ts:100, ob-backfill.test.ts:259,271, langfuse-tracing.test.ts:1163. None failed this session; latent CI flake under load.
- 19 live-marked Python tests never run in CI (pyproject.toml:301 addopts -m 'not live') — deliberate/documented (no playground service on runner), in-process suite has non-vacuity guards. Visibility only.

## Uncertainty (unresolved)
Whether two concurrent db-integration jobs actually collide on one runner needs live run logs correlating container/port across a push/PR pair. Port is randomized 20000-60000 with busy-check (collision unlikely, not impossible). #665's same-SHA disagreement is real evidence something contends; the fixed-literal fixture that makes it possible is verified, the contention itself is not.
