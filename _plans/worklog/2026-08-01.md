# Open Brain worklog — 2026-08-01

Program-boundary record for the settings-cutover front, Wayfinder repair, and
standing execution practices. Each item carries its LAW-0 evidence state.

## DONE

- **[WRITTEN] Branch `rewrite/420-settings-cutover` exists at the Step 8 tip.**
  It is the next front for issue #420: cut settings over through
  `settings.json` and delete the old TypeScript adapter. No #420 implementation
  work is recorded yet.
- **[RUNNING] Wayfinder map #443 was brought current.** New sub-issue #453
  records the rewrite detour with LAW-0 states; #418 and #420 carry the
  `wayfinder:task` label and cross-reference comments; the stale adapter-rollout
  exclusion was amended with date `2026-08-01`. An independent read-only lane
  verified the live forge state.
- **[MERGED — branch, not pushed] SessionStart is REAL and canon-only**
  (commits `4014f57`, `38353ff`). `session_start.py` and `run_session_start` in
  `session.py` inject the complete Open Brain recall pack returned by
  `OpenBrainClient.agent_context_pack` (`client.py:1279`), whose default sections
  are `profile_guidance`, `process_guidance`, and `repo_facts`. Episodic sections
  are structurally excluded and covered by a disjointness regression test. The
  hook has no size logic and fails open. Worker and independent-lane verification
  each observed clean mypy and ruff runs plus `281 passed` from pytest. Controller
  `rg` receipts verified that the canon source is the Open Brain recall call and
  that the hook performs zero file reads.
- **[MERGED — branch, not pushed] All five deleted-TypeScript-module replacements
  now exist in `python/openbrain`.** The replacements are `stop`, `session_end`,
  `subagent_stop`, `post_compact`, and `session_start`; the full #420 cutover is
  unblocked.
- **[WRITTEN] The #420 scoping specification exists.** The recon lane's full text
  is in workflow journal `wf_db373f01-3e0`. It records that live settings contain
  eight hash-pinned commands rather than the issue text's thirteen, captures the
  acceptance criteria, and identifies the SessionStart gap as the blocker that
  the merged branch commits above closed.
- **[RUNNING] #420 settings cutover was executed and independently row-proven.**
  `~/.claude/settings.json` has zero SHA-256-pinned paths; all five Python console
  scripts are wired through the uv tool install as `openbrain-capture-stop`,
  `openbrain-capture-subagent-stop`, `openbrain-capture-session-end`,
  `openbrain-capture-post-compact`, and `openbrain-capture-session-start`; gate
  hooks point to `_ob/scripts`; and the retired no-op blocks are absent. A second
  lane re-queried dogfood proof rows for Stop (`252bb247`, `2c23d0dd`),
  PostCompact summary (`71f057c8`), and post-retirement re-proof (`9dcd7313`).
  Finding event-type failure raises the named `ValueError` tracked by #431.
  The TypeScript store is retired at
  `~/.local/share/openbrain-memory/_archive/420-cutover-retired-20260801/`, the
  settings backup is `settings.json.bak-420-cutover-20260801`, rollback is
  documented in `docs/420-cutover-rollback.md`, and repo commit `25dbf0b` records
  the cutover artifacts on this branch.
- **[RUNNING; MERGED — feature branch] The LAN-bind clone-mode fix matches the
  deployed shape.** Branch `fix/lan-bind-clone-mode` contains commit `3060f5c`
  off `main` with three tests, including the proven-to-bite negative case; the
  deployed copy was already observed running this shape.
- **[MERGED — Development feature branch] Session-recap trigger vocabulary was
  added to every owning surface.** Development commit `ece0a72` adds “session
  recap” and “past session recap” to the canonical brain skill, resume workflow,
  and thin adapter. The adapter is 2,382 bytes; the only reported validator
  failure is pre-existing and unrelated.
- **[WRITTEN — not run] The hard-testing pass is designed.** Workflow journal
  `wf_49612904-efc` specifies live-database rounds for the four hooks never
  previously exercised live, `prompt_id` dedup stability, resume-shape
  rebalancing, and the LAN path. The sequence starts by proving the `-m` live
  marker fails loudly when its environment is absent.
- **[RUNNING; MERGED — feature branch] The hard-testing pass was executed at
  commit `cb7b051`.** `_plans/hard-pass-2026-08-01.md` is the plan-as-run. The
  disposable `open_brain_local_play` database was served on port `3101` from
  `app-play/play.env`. Mypy and ruff completed cleanly; in-process pytest
  reported `282 passed`; the live suite reported `15 passed` against the
  playground.
- **[RUNNING] Every targeted live hook and resume behavior met the pass
  criteria.** `session_end` accepted the lifecycle event and wrote no row;
  `subagent_stop` stored the composite `parent:agent` key and whole content;
  `post_compact` stored the full 200,000-character summary, deduplicated a
  same-`prompt_id` re-fire to one row, and produced two rows after the
  `prompt_id` changed; SessionStart returned canon-only sections, returned
  canon for `source=compact`, and surfaced `working_set` only under the widened
  override; the second Stop in the resume round delivered only appended turns.
- **[RUNNING] An independent verifier re-ran both suites and re-queried the
  playground rows.** The verifier's verdict was `HARD PASS WITH OPEN DEFECTS`.
  It also found one report inaccuracy: the non-live run deselected `15` tests,
  not `3`.

## TRIED-AND-LEARNED

- **[RUNNING] Headless Sol/Terra dispatches can orphan their own workers.** A
  headless session can start background workers and exit before collecting their
  results. Dispatch prompts now begin with explicit requirements to work
  sequentially in-session and print the complete report before exit.
- **[RUNNING] The Claudex proxy was unavailable at the first Terra dispatch.**
  The command exited `1` with `proxy unavailable`; `claudex proxy start`
  restored dispatch without another login.
- **[RUNNING] Passing the whole observation env into new hooks silently produced
  zero capture.** Config validation rejected seven undeclared `OPENBRAIN_*`
  variables, then fail-open hook behavior swallowed the error. The shipped fix is
  the `openbrain-hook-env` wrapper: it starts with `env -i` and passes only
  `OPENBRAIN_BASE_URL` and `OPENBRAIN_TOKEN`.
- **[RUNNING] Headless `claude -p` is not a valid capture proof.** Those turns
  carry `promptSource:sdk`, which capture excludes by design; a capture test must
  provide an interactively shaped prompt source.
- **[RUNNING] A session that spans its own cutover retains its original capture
  path.** Removing the advertised path during that session makes it unavailable,
  while the archived `_archive` path remains functional for the session's
  remainder.
- **[RUNNING] The `-m live` gate fails loudly when its environment is absent.**
  The required negative round produced `14 errors` and zero skips, proving the
  live suite cannot silently report green without its database configuration.
- **[BLOCKED] The planned LAN cross-host live round had a false premise.**
  `10.71.1.20` is this host itself; port `3100` is the forbidden real dogfood
  database; the disposable playground on port `3101` is loopback-bound. No
  disposable LAN target existed, so no cross-host mutation was attempted.
- **[UNVERIFIED] The Air symlink check remains pending.** No Air-side proof was
  produced by this pass.
- **[UNVERIFIED — open defects] Two residuals remain outside the proven
  behaviors.** Freshness after a same-`prompt_id` re-fire is not proven, and the
  canon-only SessionStart sections are structurally correct but empty pending a
  canon corpus.

## DECISIONS

- **[WRITTEN] Head-is-conductor decision, operator, 2026-08-01.** Recon, status
  checks, and code execution go to fully instructed workers across native Opus
  and Sol/Terra lanes. The head writes specifications, routes work, verifies
  returned evidence, and keeps receipts rather than acting as an implementation
  lane.
- **[WRITTEN] Wayfinder is the program tracking spine, operator, 2026-08-01.**
  Map #443 is the Open Brain program map. Every phase close, detour, or new front
  must update that map as part of the phase work rather than as later cleanup.
- **[WRITTEN] The standing Terra scribe lane was established, operator,
  2026-08-01.** At phase boundaries it writes local in-repo Markdown records of
  what was done, attempted, successful, unsuccessful, and decided, then runs
  `aqmd up` so qmd can retrieve the current program history. This file set is the
  first dispatch under that decision.
- **[WRITTEN] SessionStart triad and final naming, operator, 2026-08-01.** Plain
  session launch loads canon only and never back data. `SESSION RECAP` or
  `PAST SESSION RECAP` is the explicit command for old information and maps to
  the existing resume flow. Wrap remains explicit end capture. The term
  “session start” must never be overloaded for the history command.
- **[WRITTEN] Canon lives in Open Brain as vectorized recall, operator,
  2026-08-01.** `canon.md` and `_plans/canon-always-known.md` define which tiers
  count as canon, but Open Brain is the data source. A canon hook that reads
  Markdown files is wrong by design.
- **[WRITTEN] Back-history auto-load poisons subsequent tasks, operator,
  2026-08-01.** That contamination risk is why the automatic SessionStart hook
  is canon-only and historical session retrieval remains explicit.
- **[RUNNING] Hook subprocesses receive a two-variable Open Brain environment,
  operator, 2026-08-01.** `openbrain-hook-env` scrubs the inherited environment
  and forwards only `OPENBRAIN_BASE_URL` and `OPENBRAIN_TOKEN`; broader
  observation-env sourcing is rejected because it converts validation errors
  into fail-open zero-capture behavior.
- **[WRITTEN — not run] Hard testing begins with the missing-env failure,
  operator, 2026-08-01.** The first proof must show the `-m` live marker failing
  loudly without its required environment before live-database rounds exercise
  the four never-live hooks, dedup stability, resume shape, and LAN behavior.
- **[WRITTEN; supported by RUNNING evidence above] The hard-pass verdict is
  `HARD PASS WITH OPEN DEFECTS`, independent verifier, 2026-08-01.** The live
  acceptance targets are met; same-`prompt_id` freshness and canon population
  remain open, the LAN round is blocked for lack of a disposable target, and
  the Air symlink remains unverified.

## Resolve wave — map maintenance (2026-08-01)

Wayfinder resolve wave against map #443 / tracker #453. Independent read-only
lane verified live forge and branch state; no code changed this wave.

- **[RUNNING] No code changed on this wave.** `git diff cb7b051..HEAD` is exactly
  `_plans/worklog/2026-08-01.md` and `docs/sme/correctness.md` — zero TS, zero
  Python, zero test files. Branch tip gates re-run this session: mypy clean (17
  files), ruff clean, pytest `545 passed / 9 skipped / 1 failed`. The one failure
  (`test_event_vocabulary.py::test_vocabulary_is_declared_exactly_where_expected`,
  drift-guard on `python/openbrain/models/turn.py`) is **pre-existing** — red at
  base `cb7b051`, an ancestor — not this wave's regression. Branch base is not
  green; flagged, not fixed here.
- **[RUNNING] Map #443 brought current.** Decisions-so-far gained no new line —
  the wave resolved no ticket to *closed*, so the index correctly records none.
  Not-yet-specified was sharpened: the policy-index fog now records that the
  substance of [The policy layer is in no
  index](https://github.com/rodaddy/open-brain/issues/448) is RUNNING (shared
  `_DOCS`+`_ob` qmd index, 860 docs, bare-`aqmd` auto-append, verified live at
  93%), with #448 left OPEN on a single operator DECIDE — the index name
  `global_docs_instructions` vs the ticket's proposed `policy`. The
  qmd-cache-hygiene residue was re-noted as riding under #448.
- **[RUNNING] Tracker #453 brought current** with a LAW-0 state comment: #418
  MERGED-branch, #420 RUNNING/row-proven, the hard-testing pass RUNNING at
  `cb7b051` with the HARD-PASS-WITH-OPEN-DEFECTS verdict, and the four open
  residuals (same-`prompt_id` freshness, empty canon pending promotion, BLOCKED
  LAN cross-host path, UNVERIFIED Air symlink).
- **[RUNNING] #448 correctly LEFT OPEN, evidence already on the ticket.** Its own
  resolution comment maps criterion 1 (retrieval, MET/RUNNING) and criterion 3
  (SOP routing, MET/RUNNING); criterion 2 (`policy` name) is an unresolved
  operator decision. Verified independently — `aqmd internal` returns
  `_DOCS/QMD_INDEXES.md` at 93% from index `global_docs_instructions`. Not closed:
  a named criterion is unmet by operator choice, not by a build gap.
- **[WRITTEN] PROV silences #413/#414/#415/#416 and silent-drop #431 commented
  SUPERSEDED-by-rewrite, left OPEN.** Verified live in current source: the loud
  named caller-error path exists (`agent.py:546`,
  `raise ValueError(f"Unsupported event_type: {event_type}")`, plus a `_reject_*`
  guard family) and the event vocabulary is a closed `StrEnum`
  (`python/openbrain/src/openbrain/models/turn.py:93`) covering the
  formerly-unwritable `handoff`/`action`/`artifact`. The retired TS adapter these
  tickets targeted is gone (archived by #420). NOT closed: the SUPERSEDED
  classification is accurate in substance, but this session did not map each
  ticket's specific acceptance bullets (per-reason functional tests, `NO_COLOR`
  child-env assertion, receipt saved/rejected/failed/lost, reflex body-free
  proofs, the `finding`→loud-`ValueError` row round-trip) to concrete passing
  tests. Recording evidence is not certifying done — closing needs a criteria
  mapping pass or an explicit Rico retire-as-subsumed decision.
- **[WRITTEN] BULK-INGESTER (standalone bulk session-file ingester reusing the
  live capture parts) — no ticket, skipped.** Surfaced in the stocktake as a
  possible future item; not filed as a map child this wave and left off the
  frontier. It is not on the critical path to the destination and can be charted
  later if it graduates from fog.

## Migrated from harness memory (2026-08-01)

The Claude Code harness kept a private per-project memory store at
`~/.claude/projects/-Volumes-ThunderBolt-Development-open-brain/memory/` (29 fact
files plus a `MEMORY.md` index). That store defeated the Open Brain dogfood: no
other agent, runtime, or search could see it. On 2026-08-01 it was migrated into
the real memory system and the store was retired into
`.../memory/_archive-migrated-20260801/` (files moved, never deleted; `MEMORY.md`
rewritten as a pointer stub).

Where each fact went:

- **[RUNNING] 13 Development-wide operating preferences and Rico-profile facts
  captured as distilled Open Brain events** in `ob_session_events` (namespace
  `rico`, verified by psql row query, session_key
  `migrate-harness-mem-20260801`): never-speedrun / mutation-needs-per-action-
  authorization; no sleep-polling in the head; lead-with-mechanism-and-root-need;
  delegate runner lifecycle + the runner-mount facts (local sees `/Volumes`, LXC
  fleet sees `/mnt`; `CONNECTION_ERROR` often still completed; five local boxes
  exhausted system file descriptors); no flash/haiku for extraction (Sonnet
  minimum); no pre-prod key rotation for LAN-local `10.71.x`; no over-engineering
  / tight tests; challenge inherited architecture with git archaeology; the
  visible OB narration ritual; investigate session wipes rather than dismissing
  as compaction; the Rico profile; `SAS = skippy-agentspace`; the three-repo
  `pai` naming split.
- **[WRITTEN] The real-database-acceptance / full-ladder testing discipline** was
  folded into `docs/sme/correctness.md` as the dated entry "Acceptance is a
  real-database run and observed rows, not a green test."
- **[WRITTEN] Review-swarm share-path history (`reference_review_share_path`):**
  review artifacts written to a shared host-visible path
  (`/mnt/collab/reviews/open-brain/PR<N>/...`) survived the local-runner
  `emit_done()` overwriting `/work/result.json`, and survived the box being
  reaped. This is recorded as history here rather than promoted to a standing
  rule: the repo now refuses writes to the shared `/mnt/collab` volume
  (`.claude/hooks`, per `CLAUDE.md`), so the collab review-path pattern is
  superseded for current open-brain work and applies only to the older
  runner-lane review setup.

Facts NOT migrated, with reason (archive-only, superseded or already covered):

- `project_ob_infrastructure` (Open Brain on LXC `10.71.20.49`,
  `second-brain-postgres`, `/opt/open-brain`) — **contradicted by current repo
  canon.** `AGENTS.md` states there are exactly two hosts (this machine and
  core01 `10.71.1.21`) and that retired LXC hosts are out of scope; folding this
  in would point tooling at a retired host. Archived only.
- `feedback_scratch_path_collab` (scratch → `/mnt/collab`) — **superseded.** The
  repo now directs scratch to `{temp_workspace}/open-brain/_scratch/`
  (`AGENTS.md`) and refuses collab writes (`CLAUDE.md`). Archived only.
- `project_obsidian_initiative` — stale status note; `obsidian-sync.ts` already
  exists (see `docs/README.md`). Archived only.
- `reference_bilby_skippy_channel` — contains a live Discord webhook credential;
  deliberately NOT copied into any doc or event (no-secrets floor). Archived only.
- Already covered elsewhere, so recorded as duplicates and not rewritten:
  `feedback_close_means_done` (Development `AGENTS.md` "CLOSE MEANS FINISH THE
  WORK" + LAW 0), `feedback_functional_tests` (`AGENTS.md`,
  `_DOCS/STANDARDS-testing.md`), `feedback_aqmd_search_first` (`CLAUDE.md`,
  `.claude/hooks/design-lookup-gate.ts`), `feedback_no_unrequested_suggestions`
  (LAW 17), `feedback_standing_design_rejections` (`docs/sme/security.md`,
  `docs/sme/adversarial.md`), `reference_openbrain_test_db` (`AGENTS.md`),
  `feedback_pr_review_gate` (`_DOCS/AGENT_WORKFLOW.md`), `feedback_mnt_path_prefix`
  (`_DOCS/CODING_STANDARDS.md`), `project_lane_events_are_the_test_corpus`
  (`docs/dream-design.md`, `docs/dream-ethereal-runs.md`),
  `feedback_discord_username` (`_ob/skills/discord/discord-agent`).

## Frontier wave — drift guard and bulk ingester (2026-08-01)

### DONE

- **[RUNNING; MERGED — feature branch] The vocabulary census guard now recognizes
  the sanctioned seventh surface.** At the branch base,
  `test_vocabulary_is_declared_exactly_where_expected` was red because
  `python/openbrain/src/openbrain/models/turn.py:115` defines the real
  `EventType` `StrEnum`. Commit `dc14250` taught the census that sanctioned
  surface; `_plans/python-port-sequence.md` §THE WRITE PATH names `turn.py` as
  the one sanctioned reference. A planted rogue declaration tripped the guard
  and was reverted, proving the guard remained armed. Both package suites were
  green afterward: `openbrain-memory` **546 passed / 9 skipped** and
  `openbrain` **292 passed**.
- **[RUNNING; MERGED — feature branch] The bulk ingester was built and ticket
  #454 was closed with per-criterion evidence.** The ticket was created as a
  native sub-issue of map #443 and implemented in commit `d51cf56` per
  `_plans/python-port-sequence.md` §TWO APPLICATIONS. The separate `apps/bulk/`
  application stages whole files in SQLite, yields turns, reuses `records.py`
  by import, and writes only through `AgentMemory.ingest_raw_turns`; its console
  script is `openbrain-bulk-ingest`. A 300,001-character turn landed whole in
  playground `ob_raw_turns`, resume re-sent zero turns, and the live gate
  reported `2 passed`. Non-Claude adapters loud-fail by design and remain
  follow-on work.
- **[RUNNING] An independent verifier re-ran every gate for both packages plus
  the live suite and reported every frontier-wave claim VERIFIED.**
- **[RUNNING — pre-existing, unowned] `apps/hooks/README.md` remains out of sync
  with its docs generator.** The docs check was red at HEAD with this wave's
  changes stashed, so the drift predates the drift-guard and bulk-ingester work.

### TRIED-AND-LEARNED

- **[RUNNING] Resolve refusing off-map work caught a controller truncation
  defect.** A hard-coded 9,000-character slice in the orchestrator dropped the
  bulk-ingester item from the charting prompt. The defect is owned by the
  orchestrator rather than treated as missing map intent.

### DECISIONS

- **[RUNNING] The bulk ingester is the second application, not a mode of live
  capture, operator, 2026-08-01.** It reuses the live adapter's pure parsing
  parts, owns SQLite staging and format dispatch, ships Claude format first,
  and loud-fails other formats until their follow-on adapters are built.

## Merge-gate wave (2026-08-01)

### DONE

- **[MERGED — feature branch] The two pre-PR residuals are closed in source.**
  Commit `9836a79` seals the literal `event_type:"finding"` rejection through
  the console path. LAW-0 correction: the observed process exit is `1`, not
  `2`, because `capture_distilled` catches the `ValueError`, returns a FAILED
  receipt, and the console maps FAILED to `1`; the behavior is loud and
  non-zero either way. Commit `7efe827` repairs the generated hooks README and
  package-docstring drift.
- **[RUNNING] The real-harness `PostCompact` `prompt_id` residual is measured
  and answered.** Claude Code `2.1.220`, Haiku, one fixed session ID, and two
  `/compact` commands over the same session produced two compact-summary turns.
  Each compaction minted a fresh `prompt_id` because that ID is the summary
  turn's own identity: each transcript `isCompactSummary` line's `promptId`
  exactly matched its `PostCompact` event `prompt_id`. A plain `--resume` after
  the compactions did not re-fire `PostCompact`; server dedup held; no code
  change was warranted. `_plans/rewrite-gotchas.md` records the evidence under
  `Answered`.
- **[RUNNING] PRs #455 and #456 are open against `main`.** PR #455 opened the
  rewrite stack at 154 commits with the required critical self-review receipt;
  the branch is now 156 commits ahead after merge-gate fixes. PR #456 carries
  the LAN-bind mirror. Pre-PR repair `dc6997c` removed two orphaned `.check.ts`
  evidence scripts from the typecheck surface and tightened `repoOf` after the
  real `tsc` gate was red.
- **[MERGED — feature branch] The adversarial HIGH batch-bound defect is fixed
  with red proofs.** Live capture had delivered every unread turn as one batch
  even though the server caps a request at `MAX_BATCH=100`; a session with more
  than 100 unread turns could therefore stall its watermark permanently.
  Commit `a6d00bc` chunks delivery under the bound and also scopes the
  `session_seq` recompute in `ingest-raw-turn.ts` by namespace.
- **[WRITTEN — reviewer receipt; fixes MERGED on feature branch] The pinned Sol
  high review accepted the batch-bound fixer and raised eight further findings;
  five were confirmed and repaired in `805b8bb`.** The five repairs cover
  in-flight request protection from idle-TTL expiry in `transport.ts`, removal
  of the hidden roughly-14,800-character durable-memory default in favor of the
  unbounded resolver, concurrent `Bun.spawn` stderr draining, repair of NULL
  `session_seq` values from kept payloads, and an explicit failed status when
  `log_thought` chunk generation fails instead of returning false success.
- **[RUNNING] The landing lane stopped at its own gate; nothing was merged or
  deployed.** Live PR #455 checks are red at branch tip `805b8bb4`. The stop
  receipts identify a real `agent-context-pack` durable-lane equal-timestamp
  UUID tie-break defect (row `...0001` deterministically leaks past the eight on
  the playground), ruff `D212`, a `db-integration` job without pgvector in its
  job environment, and a PR-body validation failure for three missing required
  sections. The lane's refusal to merge or deploy is the intended gate
  behavior.

### TRIED-AND-LEARNED

- **[RUNNING] The server batch ceiling must be enforced by every capture
  client, not inferred from normal session sizes.** One all-unread request looks
  correct below 100 turns but becomes a permanent watermark stall above the
  server's hard bound; the red proof now exercises the oversized case.
- **[RUNNING] A clean fix-delta review does not certify the whole frozen
  candidate.** Sol accepted the `a6d00bc` fixer and then found independent
  cross-family defects elsewhere in the stack; merge readiness still depended
  on whole-PR CI and contract gates, which correctly stopped the landing.
- **[RUNNING] Green local source gates do not substitute for the GitHub job
  topology.** The merge gate exposed job-environment pgvector absence and
  PR-body schema omissions alongside source defects; those failures exist at
  the integration boundary rather than in the already-run package tests.

### DECISIONS

- **[RUNNING] Red CI is a landing stop, not a merge-with-follow-up condition,
  operator, 2026-08-01.** PR #455 remains open; no merge and no deployment may
  occur while the equal-timestamp tie-break, ruff, pgvector job environment,
  and PR-body validation failures remain.
- **[PROPOSED — in flight] The fix-CI-and-land workflow owns the next landing
  attempt.** It must repair the observed gate failures, rerun the applicable
  checks, and only then reconsider merge and deployment.
- **[PROPOSED — in flight] The gate-topology experiment compares frozen
  pre-fix `fb6da405` with the real post-fix Sol findings.** Sol is re-reviewing
  that frozen candidate so the experiment can distinguish what the original
  gate topology could detect from what only appeared after the batch-bound
  repair.

## Landing wave — CI green, merge, client deploy, and drift flag (2026-08-01)

### DONE

- **[RUNNING] CI greened on both PRs at `5b6016e`.** The reported
  `durable-lane defect` was a stale live test that still asserted the removed
  eight-event cap. The fix updated the test to expect the whole nine-event lane
  (`9/9`, `truncated:false`); restoring the cap would reverse the de-cap ruling.
  `chunk-write.pg.test.ts` exposed a separate real test-isolation defect: its
  static namespace collided across concurrent CI runs against the shared check
  Postgres. The repair uses a per-process namespace. The landing fixes also
  include the ruff `D212` docstring correction, pgvector in the
  `db-integration` job environment, and PR-body restructuring for the validate
  gate.
- **[MERGED] PR #455 was squash-merged as `eb84b02`; PR #456 was squash-merged
  as `3584527`.** Their remote branches were auto-deleted. Main-gate receipts
  are TypeScript clean, Bun `2855 pass`, Python package suites
  `547 / 295 / 98 pass`, and live suite `3063 pass`. Four shared-KB fixture
  collisions were observed and explained by the designed read-policy union;
  they are not recorded as defects.
- **[RUNNING] The local client stack was deployed.** The uv tool was reinstalled
  with six scripts, including `openbrain-bulk-ingest`; the canon pack was
  well-formed; and real capture row `7fc1279c` exists in dogfood
  `ob_raw_turns`.
- **[WRITTEN] PR #455 classifies downstream rollout as APPLIES.** Steps 2–5 —
  core01, rtech-mcps, mcp2cli, and Hermes — are not done. The merge is not a
  deployment of those downstream surfaces.
- **[RUNNING] Issue #453 remains open.** That state is correct because its
  checkpoint and reflex legs had not yet been proven at landing time.
- **[RUNNING — new flag] The dogfood clone has migration-lineage drift.** Its
  ledger claims `032_raw_turns` was applied while the table is absent, and its
  `033` / `034` lineage diverges. This wave records the condition; it does not
  claim a repaired migration state.

### TRIED-AND-LEARNED

- **[RUNNING] A red live assertion can encode retired behavior rather than
  expose a product regression.** The durable-lane failure disappeared by
  updating the stale eight-event-cap expectation to whole-lane retrieval, not
  by reintroducing truncation.
- **[RUNNING] Shared integration databases require run-unique fixture
  namespaces.** A static namespace in `chunk-write.pg.test.ts` was safe in one
  run and collided when CI jobs overlapped; per-process naming isolates the
  concurrent runs.
- **[RUNNING] The capture spine currently ingests only
  `promptSource:typed`.** The shipped capture fixtures are `sdk`, so those
  fixtures do not prove the typed-prompt ingestion leg.
- **[RUNNING] Four shared-KB fixture collisions in the live suite are expected
  under the read-policy union.** They are explained test-data overlap, not four
  product defects.
- **[RUNNING] The gate-topology experiment completed with an N=1 evidence
  caveat.** Its Open Brain receipt is event `327d09a6`; the measured result
  supports three parallel review lanes, one fixer, then a cheap Sol pass scoped
  to the fixer diff.

### DECISIONS

- **[RUNNING] Whole-lane durable recall remains de-capped, operator,
  2026-08-01.** The accepted behavior is `9/9` with `truncated:false`; restoring
  the removed eight-event cap would be a behavioral reversal, not a CI fix.
- **[WRITTEN] Merge and downstream deployment remain distinct states,
  operator, 2026-08-01.** PR #455's rollout classification applies, and its
  core01, rtech-mcps, mcp2cli, and Hermes steps remain undone until separately
  run and observed.
- **[WRITTEN] The recommended review-gate topology is HYBRID, operator,
  2026-08-01.** Use three lanes in parallel, route their findings through one
  fixer, and run a cheap Sol review over the fixer diff. The conclusion carries
  an explicit N=1 caveat rather than a universal-performance claim.
- **[WRITTEN] #453 stays open until its remaining legs are proven, operator,
  2026-08-01.** Landing the rewrite does not substitute for checkpoint and
  reflex evidence.
- **[PROPOSED — in flight] The prove-and-rehearse workflow owns the next
  evidence wave.** It is proving the checkpoint and reflex legs of #453,
  rehearsing the migration-drift repair on a disposable clone, and writing the
  operator go/no-go runbook for deploying the local service. No result from
  that workflow is recorded here yet.

## Close-out wave — #453 closure, drift diagnosis, and runbook recovery (2026-08-01)

### DONE

- **[RUNNING] Issue #453 is CLOSED after all four gate legs were proven.** The
  session-start leg returned the canon-only `openbrain.agent_context_pack.v1`;
  the capture leg persists as dogfood `ob_raw_turns` row `7fc1279c`; the
  checkpoint leg persists as `sessions` row `c7bc6eef`, whose
  `2026-08-01T09:17:37.885Z` timestamp matches the provider receipt to the
  millisecond; and the reflex leg returned an
  `openbrain.agent_reflex_pointers.v1` envelope with 20 pointers, each resolved
  to a real `thoughts` row with only the authorized `rico` + `shared-kb`
  namespace projection. The migration-drift flag was re-homed to open issue
  #458 before #453 closed.
- **[RUNNING] The migration-drift alarm was diagnosed as four orphan ledger
  rows, not a missing raw-turns table.** The original `raw_turns`-absent check
  used the wrong table name: `ob_raw_turns` exists with 41,427 rows. The live
  ledger has 50 rows against 46 current migration files; the four extras are
  `005_fts_hybrid` (pre-`.sql` duplicate), `010_chunking.sql` (renumbered), and
  `033_system_facts.sql` / `034_memory_axes.sql` (abandoned code-brain branch).
  Merged-main `bun run migrate` against the drifted rehearsal clone was a clean
  no-op. The repair SQL is optional cosmetic ledger hygiene, not a deploy
  prerequisite.
- **[MERGED] PR #457 landed as `30f22e2` with only this worklog file.** Its first
  `check` attempt failed in `chunk-write.pg.test.ts` against the shared CI
  database; an identical-head rerun of `02b9f72e` greened. The separate
  `validate` failure greened after the PR body was rewritten to the validator
  specification.
- **[MERGED] The lost runbook + repair deliverables were recovered and landed
  through PR #459 as `7beb152`.** A rehearsal worker reported commit `c2865db`,
  while the landing worker proved PR #457 contained one file; the head relayed
  the unverified report, then corrected it. Live Git inspection found
  `c2865db` as an unreachable dangling commit containing both files. They were
  recoverable verbatim with `git show` before live re-verification; the repair
  SQL blob remained identical, while the runbook gained the rebuilt-session
  receipts and current target revision. The re-proof observed live ledger
  `50` with the same four orphans, repaired clone `46` with zero orphans, and a
  wrong-database guard refusal that rolled back against
  `open_brain_local_play`. PR #459 contains exactly the runbook and repair SQL;
  every reported CI check is green, with deploy skipped by design.
- **[WRITTEN — REHEARSED, NOT EXECUTED] The local-service deployment remains an
  operator GO/NO-GO.** `_plans/local-clone-deploy-runbook-2026-08-01.md` is the
  ordered source-sync, optional ledger-repair, health-check, and rollback
  procedure. It was rehearsed against a disposable clone; it was not executed
  against `com.rico.open-brain-local-clone` or live dogfood DB
  `open_brain_local_20260724`.
- **[RUNNING] The deliberate standing state remains visible.** Cosmetic drift
  issue #458 is open. Operator-decision tickets #444, #447, #451, #419, and
  #450 remain open and were untouched by this wave. Downstream rollout steps
  2–5 remain pending until dev mode ends. Disposable databases
  `open_brain_local_drift_test` and `open_brain_local_fresh_test` still exist
  for operator-owned drop after the rehearsal artifacts are no longer needed.

### TRIED-AND-LEARNED

- **[RUNNING] A worker-reported commit is not proof that work landed.** A worker
  commit can become unreachable while its report still reads like a landed
  result. Landing is proven by the remote ref and PR file list, not by the
  worker's commit claim. Before Git garbage collection, a dangling object can
  still preserve the full tree and can be recovered path-by-path with
  `git show <commit>:<path>`.
- **[RUNNING] Migration drift checks must use the schema's real object names.**
  Checking `raw_turns` produced a false missing-table alarm while
  `ob_raw_turns` existed with 41,427 rows. Ledger-to-repo set comparison found
  the real condition: four stale filenames and no missing current migration.
- **[RUNNING] A same-commit rerun can distinguish a shared-database test flake
  from a source regression.** PR #457 attempt 1 and the green rerun used the
  identical head `02b9f72e`; the failure was the documented chunk-write
  shared-DB collision, not a change to the worklog-only candidate.

### DECISIONS

- **[WRITTEN] Local-clone deployment waits for the operator GO/NO-GO,
  2026-08-01.** The runbook is REHEARSED, not EXECUTED. The four-row ledger
  repair remains optional cosmetic hygiene; deploy does not depend on it.
- **[WRITTEN] Operator-decision tickets remain outside this close-out wave,
  2026-08-01.** #444, #447, #451, #419, and #450 were intentionally not changed
  or closed.
- **[WRITTEN; backed by RUNNING Open Brain row `327d09a6`] The review-gate
  topology recommendation is HYBRID, 2026-08-01.** Run three review lanes in
  parallel over the frozen candidate, route findings through one fixer, then
  run a cheap Sol pass scoped to the fixer diff. The measurement remains N=1.
- **[WRITTEN] Downstream rollout remains deferred until the end of dev mode,
  2026-08-01.** Steps 2–5 are still pending; this close-out wave does not claim
  hosted or downstream deployment.
- **[WRITTEN] Rehearsal-database cleanup remains operator-owned, 2026-08-01.**
  Leave `open_brain_local_drift_test` and `open_brain_local_fresh_test` in place
  until the operator chooses to drop them with the guarded clone script.
