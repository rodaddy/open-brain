# Open Brain worklog — 2026-08-04

Tracking-discipline restoration plus the config/client fix pair. Each item
carries its LAW-0 evidence state.

## DONE

- **[MERGED] PR #552 — `fix(config): a blank optional secret is an absent one,
  whitespace included` (#548).** Landed on the default branch at `d984504`,
  which is the base this worklog's own branch was cut from.
- **[MERGED] PR #553 — `fix(client): make the install prover run, and the errors
  say where to look`.** The install prover now actually executes, and its
  failures name the place to look instead of reporting a bare non-zero.
- **[WRITTEN] Local tracking discipline is restored.** The `_plans/issues/`
  mirrors had stopped around the #450s while issues ran to #553, so roughly a
  hundred issues existed only on the forge — unsearchable from the working tree,
  which is the exact failure `_plans/README.md` was written to prevent.
  `bun run scripts/sync-issues.ts` regenerated the set: 266 issues (69 open, 197
  closed) with 242 comments, 30 files new and 24 refreshed with discussion that
  had accumulated since the last sync. Nothing was deleted, so no issue was
  retitled in the gap. The backfill used the repo's own tool rather than
  hand-written mirrors; hand-writing them would have produced files the next
  sync overwrites anyway.
- **[WRITTEN] `_plans/board-map.md` records the board.** Open Brain Work Board,
  `https://github.com/users/rodaddy/projects/8`, number 8, repo-linked user
  project, node `PVT_kwHOABBGmc4BbDBt`. All seven single-select fields (Status,
  Priority, Component, Surface, Review Gate, Phase, Validation) are recorded with
  field node IDs and every option ID, plus the free-text and built-in fields.
  Automation owner is **manual/controller-driven** — nothing writes to this board
  on its own, so board state is never evidence about issue state. This closes the
  `_DOCS/BOARD_FIELDS.md` requirement that a repo board map exist before
  repo-scoped work.
- **[WRITTEN] Three PR-gate gotchas recorded in `docs/GOTCHAS.md`.** All three
  cost time on this run and all three fit the file's standing pattern — a clean
  exit reporting something false:
  1. `scripts/validate-pr-body.ts` reads `PR_BODY` / `PR_TITLE` /
     `CONTRACT_PARITY_REQUIRED` from the **environment**, never `argv`. Handing
     it a file path validates an empty string and reports every section missing,
     which produced a false diagnosis today.
  2. The self-review parser matches only plain `- Label: text`. Bold
     `- **Label:** text` fails the regex and reports the field as empty (#552).
     The SME, Live Open Brain checks, and Contract parity lines accept exactly
     the template's two dispositions; a third wording matches neither branch and
     errors as "must check exactly one disposition" (#551).
  3. Red `db-integration` on a diff that cannot cause it is evidence about the
     base, not the change. Check `git merge-base --is-ancestor origin/main
     <head-sha>` first.

## RUNNING

- **PR #551 — `fix(nats): defer the ob subject to fleet_nats and canary the
  mirror against the clone` (#550) — OPEN, rebased, CI pending.** It carried six
  `db-integration` failures in tests it never touched, purely because the branch
  predated fixes that had already landed. `gh pr update-branch --rebase` cleared
  them. Awaiting the CI result on the rebased head; nothing about the diff was
  changed to get there.

## NOTES

- The mirrors are regenerated files. Authored thinking still belongs in
  `_plans/<n>-<slug>.md`, which the sync never touches — a hand edit inside
  `_plans/issues/` is lost on the next run.
- After a sync, `qmd update -c open-brain && qmd embed` is what makes the new
  mirrors findable. Skipping `embed` leaves them keyword-searchable and invisible
  to `qmd query`.
