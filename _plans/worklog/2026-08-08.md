# Open Brain worklog — 2026-08-08

Status grammar: every claim below is MERGED (in main) or RUNNING (observed
live this session) unless marked otherwise.

## The enforcement machine closed its own loop

- PR #629 (verify-lane + merge-gate) merged THROUGH its own gate after its
  clause-8 fix; the gate's first live refusal was of itself. From then on,
  every merge in this log carried a verify-lane receipt bound to its head
  SHA plus a harvest proof — mechanically enforced, not promised.
- Controller defects caught and harvested: the clause-8 RED misdiagnosis
  (ambient env pre-emption — round 6) and, operator-caught, doing lane work
  by hand (verification runs and provider debugging absorbed by the head).
  Verification now routes to the verifier agent; diagnostics become lanes.

## Goal run: the pre-core01 set (operator: "let's go")

Nine PRs merged, every one lane-built, independently receipted, and
harvested (Tightenings rounds 6–13):

- **#638** (#637 closed) — design-lookup gate judges intent, not vocabulary;
  the lane was refused 12× by the gate it was repairing.
- **#640** (#625 closed) — silent sweep fixed: overlap guard warns, producer
  is a /health input.
- **#639** (#563) — operator ruling (ledger 23): recall never ships the
  whole corpus; bounded bursts + pointer walk. 186KB→44KB largest reply,
  all data reachable. Rollout to core01 still outstanding.
- **#642** (#451) — tiered memory enforcement per ledger 24.
- **#645** (#636 closed) — neutrality scrub: env-specific values out of
  shipped code/docs/CI (red-proven 583 violations/127 files), allowlist
  gate keeps it out. Zombie duplicate PR #644 closed.
- **#649** — capture merge-gate RETIRED on main (ledger 25: raw capture is
  automatic, distillation is DREAM's job; the gate wedged all merges on its
  first live firing, including its own fix — the enforcement-wedge class).
- **#650** (#646 closed) — capture path healed: server now establishes
  exact scope on existing lanes (2,011 dogfood lanes could never accept a
  capture; they heal lazily on next touch — bulk-heal remains an operator
  option), provider errors name keys the validator accepts.
- **#648** (#647 closed) — capture liveness per the eleven-day-old spec in
  docs/decisions/capture-never-drops-a-turn.md that had never been run.
  Reader is MERGED; /health composition is a follow-on before it reports.
- Dogfood clone redeployed at merged main 212f916, revision proof PASSED.

## Rulings recorded (docs/issue-graph.md)

Items 23 (bounded recall), 24 (tiered enforcement), 25 (capture-gate
retirement + reasoning + rejected options), 26 (post-wave decisions pass:
runner-labels fail-closed ratified; hermetic-default check style; #638
exemption kept; A/B deprioritized — quality tracks the RLVR contract, not
model choice; model-pinned dispatch must use a route that provably places
the model).

## New issues filed

#641 (pr-body-gate wrong-version — confirmed by three lanes), #643
(wall-clock flake, lane-filed), #646/#647 (both closed same day, above).
Open gate-defect set for future lanes: #618 (fifth shape recorded), #622,
#608/#632/#634, #641.

## Carried forward

- Standards-banner ack: deferred honestly again (the changed standards were
  not personally read by the controller this session).
- #563 downstream rollout + core01 cutover; #578 E2E gate is the remaining
  unstarted pre-cutover item.
- /health capture composition follow-on (#648's WRITTEN-not-RUNNING gap).
- Bulk-heal decision for the 2,011 scope-healed lanes.
- OPENBRAIN_TEMP_WORKSPACE now set in ~/.claude/settings.json env
  (worktree scatter to ~/.cache announced by three verifier runs).

## Evening wave: the E2E gate's two defects fixed, capture health RUNNING

Four more PRs merged, each verifier-receipted and harvested (rounds 15–18):

- **#657** (#654 closed) — Python client had `delegate_namespace=False`
  hardcoded since #294: `X-Namespace` never sent, delegated-intent sessions
  silently landed in namespace `admin`. Fix is opt-in
  `OPENBRAIN_DELEGATE_NAMESPACE` (default OFF; server role-gates the
  header). Verified against the LIVE dogfood service: capture lands in the
  requested namespace, weak-token delegation refused with nothing written,
  refusal names the cause. Downstream-rollout: additive opt-in; Hermes
  canary becomes required at first flag-on deployment.
- **#658** (#655 closed) — eval teardown could never remove a namespace
  (soft-delete + namespaces are emergent properties of rows). Prefix-guarded
  hard purge composed AFTER record teardown; guard mutation-proven. Cleanup
  SQL for 100 pre-existing leaked rows / 30 namespaces is PROPOSED in the
  PR body for the operator's hand.
- **#656** — capture-health observer wired into server/main.ts per ledger 28.
- **#660** (#659 closed, filed and fixed same evening) — the redeploy after
  #656 PASSED its revision proof while /health published NO capture block:
  `scripts/local-clone.ts` CHILD_ENV_KEYS allowlist silently dropped
  `OPENBRAIN_CAPTURE_HEALTH_*` (third instance of the #530 deploy-coupling
  class). Launcher now passes the keys AND announces every
  configured-but-dropped key by name.

**RUNNING, observed 22:45Z at revision 7aa82d8:** live /health publishes the
capture block — `"capture lane delivering"`, 14 sessions, 1,624 turns, no
silent roles, spool empty — and the boot log carries the launcher's ADJUSTED
line naming six MORE configured-but-dropped keys
(EMBEDDING_WATCHDOG_RESTART_SCRIPT, LOG_LEVEL, LOG_MAX_BYTES, LOG_MAX_FILES,
OPENBRAIN_MCP_AUDIT_ENABLED, SERVICE_NAME) — each needs an operator ruling:
add to CHILD_ENV_KEYS or remove from the env file. Announced, not honored,
on purpose.

One lane self-reported a reflexive argument-less `rm -rf` (deleted nothing);
harvested into round 18, not punished. Design-lookup gate paid for itself a
second time (redirected a hand-rolled shutdown path onto the existing
`backgroundRuntimes` mechanism) and also showed a new wedge shape (blocked
the `aqmd search` it demands when piped; bare form passes).

Still parked on the operator: live-eval credentials yes/no (blocks the #653
E2E-gate credentialed verify), #655 cleanup SQL, the six announced-dropped
keys, bulk-heal for the 2,011 lanes, standards-banner ack.
