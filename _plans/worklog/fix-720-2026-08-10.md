# Lane journal — #720 validator OWN_TREE fallback (2026-08-10)

Status: WRITTEN (in progress).

## Charter

`scripts/validate-pr-body.ts:91` derives `OWN_TREE` from `import.meta.dir` and
`requireDoneMeans` consults it as a FALLBACK after the tree under review. The
hook runs the PRIMARY checkout's copy, so `OWN_TREE` is always the primary
checkout — a Done-means path absent from the branch under review PASSES on the
strength of a same-named file in the primary checkout. Family A (a gate judging
from the wrong tree), one layer deeper than #706.

## Environment

- Worktree: `/Volumes/ThunderBolt/_tmp/open-brain/_worktrees/fix-720-validator-own-tree-fallback`
- ANNOUNCED ADJUSTMENT: `lane-bootstrap` cuts from `origin/main` only (round 29
  tightening, no `--base` flag). The audit (`afc5525`) and the #711 hooksPath
  fix (`183d066`) are on `wip/2026-08-07`, so the branch was re-cut with
  `git checkout -B ... origin/wip/2026-08-07` after bootstrap. Not silent.
- `core.hooksPath=_githooks` (relative, #711) — this worktree runs its own hooks.

## Timeline

(filled as the lane runs)

## Timeline (RUNNING states as observed)

1. Read #720, `docs/gate-layer-audit-2026-08-10.md`, lane-contract rounds 28-30,
   and the three checks the fix must not regress (706/709/714).
2. Wrote `scripts/done-means/720-validator-rejects-foreign-tree.sh` (7 clauses)
   BEFORE touching the validator. RED 4/7 — clauses 1, 2, 5, 6 failed. Clause 5
   drove the REAL hook and showed it ALLOWING a body citing a check absent from
   the branch, end-to-end. The audit's reproduction stopped at the validator;
   this went one layer further out.
3. Fixed `scripts/validate-pr-body.ts`: review tree -> head ref, and when a head
   ref is known those two are authoritative (own tree not consulted, refusal
   says so). Own tree answers only with no head ref, announcing its weaker basis.
4. SELF-CAUGHT DEFECT: the first refusal wording dropped the literal `looked in:`
   marker, turning `709-hook-feeds-head-ref.sh` clause 4 RED. Repaired in the
   direction of the CHECK (kept the marker) rather than moving the assertion.
   Clause 2 of the new check was then tightened to assert the same marker, and
   per round 28 the whole corrected clause set was re-proved RED against the
   pre-fix validator (still 4/7 FAIL) before any green was claimed.
5. GREEN 7/7. Regression suites: 706 5/5, 709 5/5, 714 6/6. Also green:
   pr-template-passes-validator 3/3, done-means-field-required 5/5,
   pr-body-gate-fires 8/8, `bunx tsc --noEmit`, `bun test scripts` 371/0.
6. Commit `f758ece`, pushed with the pre-push hook PASSING (no `--no-verify`).
7. PR #723 -> `wip/2026-08-07`. Its body passed the live `pr-body-gate` hook,
   i.e. the gate judging this PR is the gate this PR fixes, and it announced
   "resolved in the tree under review".
8. `verify-lane 723`: VERIFIED, 7/7 from a fresh worktree at head
   `f758ece0`, SHA-bound receipt posted. Verify worktree removed.
9. CI all green (check, contract-parity, db-integration, python-*, validate,
   GitGuardian).
10. merge-gate REFUSED the merge for a missing harvest — the rule working. The
    charter withholds root graph writes from lanes, so route 3 was used: the
    three lessons were posted as a `harvested:` comment for the tracking-scribe.
    Not a bypass; the gate's own sanctioned third route.
11. MERGED as `38719c9` (squash). Issue #720 closed with a 2b direction-and-why
    comment. Lane worktree removed.

## Announced adjustments (nothing silent)

- Branch re-cut from `origin/wip/2026-08-07` after `lane-bootstrap` cut it from
  `origin/main` (round 29's known no-`--base` gap).
- The done-means fixture is RETAINED under
  `{temp_workspace}/open-brain/_scratch/` by design and announced on stderr;
  this check never deletes (AGENTS.md).
- `gh pr merge --delete-branch` could not delete the LOCAL branch while the
  worktree held it. Remote branch deleted; local resolved by worktree removal.
- `aqmd search` exceeded 120s and was replaced with `qmd search` for the
  design-lookup gate; the lookup was performed, not skipped.

## Truth labels

- MERGED, unverified in production: the fix is in `wip/2026-08-07` at `38719c9`.
- RUNNING (observed this session): the fixed validator gated PR #723's own body
  through the live hook, and verify-lane ran the check 7/7 at the PR head.
