# Open Brain worklog — 2026-07-31

Historical program record for the LAN dogfood opening, Air onboarding, and the
Step 8 hook-capability close. Each item carries its LAW-0 evidence state.

## DONE

- **[RUNNING] LAN dogfood opened.** The deployed local clone at
  `/Volumes/ThunderBolt/open-brain-local` binds `0.0.0.0:3100`: its
  `app/src/local-clone-mode.ts` has one guarded validator line allowing
  `0.0.0.0` only for `OPEN_BRAIN_BIND_HOST`, `local-clone.env` selects that
  address, and launchd was kickstarted. Health answered on both
  `127.0.0.1:3100` and `10.71.1.20:3100`; an authenticated capture sent through
  the LAN address was observed in `ob_session_events`.
- **[PROPOSED] The LAN bind validator delta still has to land in this development
  repo before the next `local-clone-deploy.sh` run.** The deployed clone carries
  the one-line change, but this repo does not yet; another deploy would otherwise
  revert the LAN opening.
- **[RUNNING] Air onboarding reached the filesystem blocker.** On Air
  (`10.71.1.26`), the provider was installed, `OPENBRAIN_BASE_URL` was set to
  `http://10.71.1.20:3100`, and non-interactive `PATH` in `~/.env` was corrected
  by removing the dead `AI/reflex/bun/bin` entry and adding
  `/opt/homebrew/bin`.
- **[RUNNING] BLOCKED: Air captures silently drop until the operator runs
  `sudo ln -s /Volumes/ThunderBoltAir /Volumes/ThunderBolt`.** The provider's
  current root/cwd contract rejects Air's differently named volume before a
  write is attempted.
- **[MERGED] Step 8 hook-capability code and rulings are present on
  `rewrite/418-hook-capabilities`, not pushed.** Four hooks are real: `stop`;
  `session_end`, which closes the session and releases its slot; `subagent_stop`,
  which drives the same spine over `agent_transcript_path` with its own watermark
  key; and `post_compact`, which records `compact_summary`. The branch history
  carries commits `2201a22`, `44b0794`, `ef3a52c`, `0d5dc09`, and `28bbb9f`.
- **[RUNNING] The controller's final Step 8 gates passed:** mypy clean, ruff
  clean, and pytest `264 passed`.

## TRIED-AND-LEARNED

- **[RUNNING] `ob-memory-provider` has a cross-machine cwd landmine.** It
  hard-codes `DEVELOPMENT_ROOT=/Volumes/ThunderBolt/Development` and requires
  the caller's cwd to exist below that root. Any other cwd makes the fail-open
  path exit `0` with no output and no write. This was reproduced on Air and
  locally with `cwd=/Users/rico`; a machine whose development drive has another
  name therefore captures nothing while appearing successful.
- **[PROPOSED] The queued Python-rewrite fix is to make the development root
  environment-configurable.** No implementation of that fix is recorded in this
  branch yet.
- **[RUNNING] The PostCompact capture gap was measured before implementation.**
  The compaction-summary transcript record carries `isCompactSummary:true` and
  no `promptSource`; `is_operator_turn` rejects it, so the Stop spine advances
  past and drops it. That measurement is the basis for `post_compact.py`.
- **[RUNNING] A wire worker rejected a false DUPLICATE directive.** A head-side
  verdict-parsing bug supplied the wrong instruction; the worker compared it
  with the evidence already included in its prompt and recorded the supported
  GAP verdict instead. The useful pattern is: a bounded worker should refuse a
  directive that contradicts the supplied evidence.

## DECISIONS

- **[WRITTEN] Testing-budget decision, operator, 2026-07-31.** Finish all rewrite
  pieces first. Per-step after-the-fact testing stays below roughly one third of
  coding time; each step gets fast mechanical gates, followed by one hard-testing
  pass after all pieces exist. This is also rule 6 of
  `_plans/python-port-sequence.md`'s execution contract.
- **[WRITTEN] Step 8 stub rulings are recorded in
  `_plans/rewrite-gotchas.md`.** `SessionStart`, `UserPromptSubmit`,
  `PreToolUse`, `PostToolUse`, and `PreCompact` stay stubs for the grounds in the
  rulings table; `SessionEnd`, `SubagentStop`, and `PostCompact` are implemented.
