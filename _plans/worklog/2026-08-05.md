# Worklog — 2026-08-05

Session: fresh cold-start head (Claude Fable 5), branch `wip/2026-08-05-session`.
Everything below states its LAW-0 tier explicitly.

## 1. Cold-start resume test (the #450/#443 acceptance shape) — RUNNING

Prompt was "figure out where you're at" with zero re-explanation. The session
reconstructed state from canon packs + lane resume + git/gh alone: knew #538/#539,
tonight's merges, and the board. **Verdict: mostly passed.** Paper cuts found
(real findings, not complaints):

- Repo `CLAUDE.md` points at `_ob/skills/brain/scripts/resume.py` repo-relative;
  the script actually lives at Development-level `_ob/` and only runs under the
  `python/openbrain-memory` uv env (`uv run --project python/openbrain-memory`).
- Lane resume's newest event was 2026-08-03; the 2026-08-04 merges were
  reconstructed from git, not from the brain.

## 2. Hygiene sweep — RUNNING (verified this session)

- Removed merged worktrees `ob-560`, `session-2026-08-04`; kept `ob-562` (held
  uncommitted #562 work, which later became PR #570 — vindicated).
- Garbage tags `v1.0.0` + `v9.9.9` found (2026-08-01 test leftovers,
  "reachable tag commit"/"outside main commit"); **local-only, never pushed**,
  so no accidental deploy ever fired. Pending Rico-approved `git tag -d`.
- No pile of committed-unpushed work exists: swept all 36 local branches
  (`git cherry` on the two candidates — both stale duplicates of merged #551/#542),
  core01 checkout (clean), runtime dirs (not git repos). The felt "pile" is
  **117 commits merged-but-undeployed** since v0.1.0-rc.4 (July 17). Air not yet
  checked (unreachable at .26 and .22 — ssh timeout).

## 3. Release posture — decided with Rico

- Repo verified PUBLIC. Flagged: public repo + public service endpoint
  (`open-brain.rodaddy.live`) + published unpatched-issue writeups + personal
  canon in #538 = flip-private recommendation on the table (Rico's call).
- V1 deferred deliberately: still dogfooding. `v*` tag push deploys to core01
  (ci.yml:6,509-510); merging PRs does not (deploy SOP line 10). Release when
  core01 needs the accumulated fixes = `v0.9.x-rc`, attended, per
  `docs/local-release-deploy-sop.md`.

## 4. Issue-state audit — 57/57 audited (wf_bb5fcf4b-e2b, 8 agents, receipts)

done=1 (#392 → closed with evidence), superseded=1 (#417, awaiting Rico since
the content-free boundary was security-stated), partial=23, open-valid=32.
Headline finds: #529 REGRESSED to silent exit-0 zero-capture; #563 corpus grew
39.9→60.4 MiB; #561 audit-half done, masking-half unbuilt. Tracking verdict:
mirrors were current within a day — the gap is no *scheduled* reconciler, only
habit.

## 5. Epic map applied — MERGED (on GitHub + mirrored)

New epic #571 (OBS — only theme with no epic). Close-run maps appended to
#389 #342 #369 #443 #409. Standalone by design: #296–#300 (durability lane),
#461, #463 (goes last), #563 (decision). Mirrors synced, committed, indexed.

## 6. Authority grant + worker directive — operator, verbatim intent

Head coordinates ONLY (≤3 compacts for the program); all work in subagents;
**Sol-majority** for grunt (both Claude subs together barely last a week).
Persisted: `_DOCS/MODEL_ROUTING.md` (Development commit a544273). OB capture
FAILED with the live #529 signature (`session_start result did not prove exact
Open Brain scope`, status `lost` — loud, unlike the 03:56 silent repro);
receipted at issues/529#issuecomment-5187905093. New detail: operator captures
silently drop `kind` into ignored_optional_request_keys.

## 7. Close-run program — RUNNING

Ledger: `{temp_workspace}/open-brain/_goal-runs/2026-08-05-close-runs/LEDGER.md`.
Baseline: 57 open issues, 4 open PRs. Order: CI → OBS (#571, promoted because
tracing makes every later run diagnosable — Rico's argument) → overnight
mechanical batch (#381 #384 #386 #387 #388 #520 #442, runner boxes) → canon
judgment (#451 #433) → durability (#296–#300) → DREAM. SHAPE + #563 + #417 +
#538 read-through = Rico decision sessions, not close runs.

**Close-run 1 in flight** (wf_7acd45cd-074): lanes pr568 / pr570 / issue461,
each an Opus-low wrapper supervising a write-capable `claudex sol medium -p`
process. Workers push; they never merge or close.

## 8. Review + merge protocol for every program PR (operator, this session)

"Swarm stuff as you PR it." Locked as ledger law: worker pushes → controller
sizes the review (`review-sizing`) → SME-injected swarm lanes per
`docs/sme/README.md` (gotcha-agent mandatory for `python/openbrain-memory/`
paths) → **mixed-family rule: Sol-authored fixes get Claude-side review**
(routing doc Review Diversity) → findings fixed → fix-delta verification →
controller merges on green CI at the exact SHA → issue closed with receipt →
mirrors re-synced → this worklog updated. No PR merges un-swarmed.

## 9. Close-run 1 results — lanes returned ~01:55, controller-verified

- **pr568**: red `validate` was the PR-BODY linter (missing Critical Self-Review
  + Review Gate sections), not code — CI was already green on the same commit.
  Wrapper (Opus) skipped Sol deliberately and fixed the body only; flagged its
  own near-miss (almost checked a false SME box the linter can't catch). ALL
  checks now green at 81c7ae7, controller-verified live.
- **pr570**: real defect — test hardcoded macOS-only `/Volumes/...` at module
  top level, EACCES on Linux runners before any test ran. Sol (claudex sol
  medium, banner-attested) fixed it in one line (94ae538 → gitignored
  `out/test-fixtures`). All code jobs green; body gate was controller-fixed the
  same way as 568 (validated locally against scripts/validate-pr-body.ts first).
  Gotcha for SME promotion: module-top-level absolute paths in tests explode on
  foreign runners before any test executes.
- **issue461**: premise did not survive verification — the asked-for isolation
  already exists (ci.yml:36-44 per-run DBs, landed pre-issue; root cause
  ccb80c7/#505; test-level NS mitigation). 5x concurrent repro on one shared DB:
  clean, ~140ms vs the 5,400ms stall. Controller spot-verified ci.yml + ancestry
  and CLOSED #461 with the receipt. Lane also caught the silent-skip trap
  (first run "8 skip" without the test DB — did not count it as evidence).
- **#461-adjacent process note**: `bun run migrate` reads DB_*, not
  OPENBRAIN_TEST_DATABASE_URL — lane briefly migrated the dogfood DB before
  correcting. Candidate gotcha for docs/sme/.

**Review swarm launched** (wf_161bc66c-e19, Light tier): r568-sol (Sol reviews
the Claude-authored emitter work — mixed-family), r570-sol (derivation +
fail-loud adversarial), r570-delta-claude (Claude verifies Sol's one-liner).
Merges wait on swarm verdicts + green validate on 570's body edit.

Open count: 55 issues (baseline 57; #392 pre-program, #461 this run). PRs still 4.

## 10. Swarm verdicts — BOTH PRs findings-must-fix (the gate earned its keep)

- **#568**: HIGH records.py:202 (malformed usage/model rejects the whole
  transcript record — assistant text permanently lost); HIGH turn.py:219
  (default-zero coercion fabricates false measured-zero cost); MEDIUM fake-sink
  test proves composition, not pricing. The cost feature as-pushed could DROP
  TURNS — the exact opposite of a capture system's job.
- **#570**: HIGH setup-client.sh:322 — silent $HOME fallback (exit 0, empty
  stderr) survives in the very PR whose purpose is killing silent fallbacks,
  and the test at :134-167 asserts the silence. MEDIUM: dirname "$0" derivation
  breaks under symlink/PATH/sourced/zsh layouts.
- **Delta lane bonus**: the hardcoded-path class survives elsewhere —
  scripts/bakeoff-export.ts:19 prefers banned TMPDIR, falls back to macOS-only
  literal. Filed as #572 (candidate for the overnight batch).
- Findings posted verbatim to both PRs. Fix loop launched (wf_c97e9ff4-8ff):
  Sol write lanes per PR → fix-delta verify lanes → only then controller merge.
  SME promotions queued for after the fix cycle (patterns: enrichment-rejects-
  record, fabricated-zero telemetry, silent-fallback-in-fail-loud-PR,
  hardcoded-path-class).

## 11. Close-run 1 COMPLETE — both PRs merged after the full loop

Fix lanes (Sol write, wrapper-verified — every claim below was independently
re-run by the wrapper, not trusted from Sol):
- **#568 fix ff45179**: TranscriptMessage.model/usage widened to Any with
  boundary validation in raw_turn_from_line (malformed enrichment costs the
  enrichment, never the turn); TurnUsage counts now strict, required, ge=0 —
  absence omits usage entirely instead of fabricating zeros. Fail-first proof:
  wrapper reverted only the source files, new tests 6-failed, restored, green.
  Full: 594 pytest pass, mypy clean, ruff clean.
- **#570 fix 4dc13a0**: silent $HOME fallback REMOVED (stronger than "make it
  loud"); four-branch source resolver (BASH_SOURCE / zsh %x / PATH / symlink
  walk), POSIX-sh sourcing declared unsupported and fails non-zero. Inverted
  test proven against the old wrapper: "Expected: not 0" — the silence was
  real. 8/8 tests, tsc clean.
- Fix-delta verification: fixed-verified on all five findings across both PRs.
- CI green on both fix SHAs; controller squash-merged both at 06:14Z, branches
  deleted.
- **#562 CLOSED** with receipt (field proof deferred to development#87, which
  it was blocking). **#560 REOPENED deliberately** — acceptance is live
  non-NULL cost on CT 273; merged-not-deployed; closes in the OBS run with a
  live receipt.
- Process notes for the record: 568's first push was rejected by the pre-push
  hook over a worktree env gap (bun-types before bun install) — hook worked as
  designed; 570's fix lane flagged /bin/dash + bash/zsh availability as CI
  environment deps for the new tests.

SME promotions still queued (4 patterns). Next: OBS run (#571) — masking
(#561's unbuilt half), live cost proof (#560 closure), #569 decisions prep.

Scoreboard: 55 open issues (57 baseline −392 −461 −562, +572, 560 reopened
net-zero). Open PRs: 2 (#538, #539 — both Rico-gated drafts). Compacts: 0/3.

## 12. Overnight batch landed — 7/7 lanes, 6 PRs, zero failures (wf_9ce97d99-81e)

- #574 (#381 distill-lag alarm): doctor gains per-namespace lag ratios vs TTL;
  wrapper caught and fixed Sol's leftover contract-version drift (d112655).
  Honest residual in the PR: SQL never ran against real Postgres (fake pool).
- #577 (#384 sweep producer): biggest change (+594/-29); red-test proof
  independently reproduced (silent-no-op genuinely detected). Sol's first
  launch died exit-144 with zero output — wrapper smoke-tested and relaunched.
- #575 (#386 qmd sync job): additive only; nothing installed live, deploy is
  operator-step by design.
- #576 (#388 doctor qmd freshness): contract v3 bump — POTENTIAL COLLISION with
  #574's identical bump; flagged to the review swarm as a merge-order check.
- development#93 (#442 brain-skill docs): all four staleness claims re-verified
  live before rewrite; mcp2cli recipes relocated to a Codex/operator appendix,
  not deleted.
- #573 (#572 bakeoff path): fixed like 94ae538; sweep found remaining
  /Volumes hits are doc-comments/assertions only.
- #387 triage (report-only): issue premise STALE — 54/81 dirs already indexed,
  27 remain, all classified with reasons; umbrella-dir finding changes scope.
  Comment on #387; registration stays operator-eyed.
- Process finding for the hook owner: design-lookup-gate false-positives on
  'prune'/'capped' (git worktree prune, cap-hit log names) — bit two lanes.

Review swarm on all 6 PRs launched (wf_375ff2cd-89e, Claude lanes — mixed
family over Sol-authored code), including the v3-collision check between
#574/#576. Merges only after verdicts + green CI, per the standing gate.

## 13. Overnight review verdicts + PR #573 merged (#572 closed)

Swarm (6 Claude lanes over Sol-authored PRs): #573 approve → controller merged
(checks verified), #572 auto-closed. All five others must-fix. Headliners:
- CROSS-PR HIGH: #574 and #576 both ship byte-identical contract v3 for
  different payload shapes + three colliding shape-lock hunks; each looks
  MERGEABLE alone. One fix lane now owns both, sequenced (574 → rebase 576, v4).
- #574: fifth contract-version site (parity fixture) still pinned v2 — hidden
  by the silently-skipped parity suite (the exact SME [2026-06-19] pattern);
  live-DB check found the alarm fires day-one at ratio 1.46 via 27 leftover
  parity-test namespaces (noisy-alarm-masks-real-degradation, realized).
- #575: qmd formats counts with toLocaleString — 'Embedded 8,676 chunks' fails
  the parser AND the fail-loud guard → false-RED on every big sync (reviewer
  reproduced against the shipped qmd binary, not the PR text). Launchd fallback
  logs sit on the very volume whose absence is the failure case.
- #577: idempotency key from batch_anchor is permanently consumed by
  ON CONFLICT against never-deleted rows → lane backlog starvation; the
  convergence assumption is tested nowhere real.
- dev#93: the rewritten discovery section documents MCP tools/list — which
  does not exist on the transports the skill routes Claude to.

Fix loop launched (wf_7d85d7f5-611): 4 lanes (574+576 coordinated, 575, 577,
dev93), findings posted to PRs by lanes before fixing, delta-verify follows.

Scoreboard: 55 open issues (#572 closed), 7 open PRs (5 in fix loop + Rico's 2).

## 14. Fix loop landed — merges: #575, #577, dev#93, #574; #576 pending CI

- 574/576 lane performed exactly per contract: pushed 574's fixes (fifth
  version site, created_at pinned, parity-namespace exclusion), rebased 576
  onto it, resolved the three shape-lock hunks keeping both payloads, bumped
  576 to v4 at all 7 sites — then STOPPED at the non-fast-forward push and
  asked, instead of forcing on its own authority. Controller authorized
  --force-with-lease (work preserved in kept worktree), pushed clean.
- Lane also FOUND AND FIXED a defect nobody assigned: parity suite flapped
  (10/7/8 failures) against the real qmd index — per-call Bun Worker spawns
  under concurrency starving the event loop; fixed with a shared in-flight
  probe map (95096a6), then proved 3/3 stable runs matching the main baseline.
- Sol died at the harness 600s wall mid-investigation once; wrapper completed
  the minimal fix itself and said so plainly.
- 575: comma-tolerant parsing + off-volume launchd logs + parser test
  fixtures (incl. 'Embedded 12,345 chunks'). 577: idempotency rekey +
  REAL ON CONFLICT pg test run against the test DB. dev93: discovery section
  now documents only commands proven to execute; adapter description restored.
- Delta-verify: fixed-verified on all three completed lanes; residuals
  recorded (sibling parser comma class in update/status lanes; consumer
  ordering tiebreak — both pre-existing, candidates for small follow-ups).
- Controller merges: #575, #577, dev#93, then #574 (after adding the newly
  required Contract Parity body section — touching contracts/ flips it on).
  #576 body fixed the same way; merges when CI greens.
- Issue honesty per the #560 precedent: #386 and #384 auto-closed by merge and
  were REOPENED (acceptance is a RUNNING job / observed processing — operator
  steps). #442 closed with receipt (docs verified-by-execution). #381 state
  checked post-merge.

## 16. #578 phase 1 built — PRs #579 (egress verifier) + #580 (scenario fixtures)

- #579: drive/verify by run tag against Langfuse; four labelled checks
  (arrival exact-count fatal, generation metadata fatal, cost reported-unless
  --require-cost which IS #560's receipt, secret scan fatal via the in-repo
  SECRET_DETECTORS — the #561 set already lived in src/secret-patterns.ts).
  Lane corrected two brief errors from source (env names; regex location) and
  survived one Sol hallucinated-nested-worker relaunch. 8/8 unit, red-proven.
- #580: scenario support ON the existing harness; capture round-trip fails on
  the #529 exit-0-zero-write case (pinned in test at scenario-gate.test.ts:194),
  durable_memory budget shape (#563), checkpoint/wrap round-trip; EVAL-3
  discipline (throwaway ns, teardown-accounted, env-gated). 203 eval tests,
  full suite 3212 pass no regressions, deliberate-break proven.
- Both UNVERIFIED-live by design and say so; first live run is the acceptance
  event. Review swarm launched (wf_1f3533d6-462, vacuous-pass hunt priority).
- Also this hour: honest pace accounting to Rico — 53 open ≠ 20s because ~10
  are Rico-gated decisions, ~9 are epic containers, ~11 DREAM; overnight-
  addressable pool was ~12 and 7 closed. Every future run report states
  closable-by-agents vs gated-on-Rico vs container up front (ledger rule).

## 17. Quality-gate swarm verdicts — both must-fix, all vacuous-pass class

Exactly what the lane was told to hunt, found in the instrument itself:
- #579 HIGH: --drive passes on exit-0 while the Python lane declines silently
  and swallows emit exceptions — a drive that emitted NOTHING reports PASS
  with fabricated observed-counts (the primary false-#560-receipt path).
- #579 HIGH: single-shot exact-equality vs async batch ingestion — healthy
  systems fail transiently; no way to tell 'late' from 'dropped'; trains the
  operator to re-run until green.
- #579 MEDIUM: secret scan scoped to a surface that may structurally never
  carry content — a fatal check that cannot fire.
- #580 HIGH: the #529 exit-0/empty-stdout/no-receipt shape maps to a generic
  scenario_transport_error, not a capture failure; no fake covers it; the
  zeroWrite option models a DIFFERENT thing.
- #580 MEDIUM: capture_receipt_lost branch unreachable (real CLI exits 1 on
  lost) — decorative assertion.
Fix loop launched (wf_6539b063-b4b): findings to PRs, Sol fixes with
deliberate-break proof per new assertion, delta-verify hunts NEW vacuous
passes introduced by the fixes. Merges only after fixed-verified + green CI.

## 18. Quality gate MERGED (#579, #580) — live acceptance run in flight

- Fix loop delivered: drive now proves emission via the observation WATERMARK
  (offset advances only after emitter.emit returns — the fix lane read the
  Python and corrected the reviewer's own proposed stdout-scan, which cannot
  work because session.py declines silently BY DESIGN); bounded settle poll
  (waited_ms/timed_out recorded, late≠dropped); secret scan covers observation
  input/output with content_fields_present honesty; pagination full-page
  fallback; #529 shape is first-class capture_receipt_absent; lost-receipt
  branch independently reachable; ignored-key check intersects sent keys at
  all three request sites; dead schema field removed.
- Delta-verify: 579 fixed-verified (5 breaks, each failing exactly its
  assertion). 580 first verify returned not-fixed on an EMPTY delta — my
  scope bug (baseline = the fix SHA itself); verifier honestly refused.
  Corrected re-verify: 4/5 mutation-proven, item 3 partially tested (the
  line covering capture+checkpoint-event sites deletable with suite green).
- Controller closed item 3 inline (one test case, small-edit carve-out):
  capture-site ignored-key case, mutation-proven 12/0 → 11/1, pushed f51e63b.
- Both PRs merged on green CI. Live acceptance run launched
  (wf_71a0a97b-2a7): scenario suite + egress verifier against the real brain
  and real Langfuse; a green generation_metadata + non-NULL cost is #560's
  receipt. Receipts land on #578.
- Also: killed 4 orphaned runaway `qmd embed` processes (~110 CPU-hours,
  PPID 1, SIGTERM-immune ×2, index already fully embedded — pure spin).
  One likely this session's aqmd up. Flag for #386 deploy step: the nightly
  sync needs a timeout/watchdog around embed. Upstream busy-loop belongs to
  @tobilu/qmd.

## 19. First live run of the #578 gate — the instrument works; the trunk is guilty

The gate ran end-to-end live (real service, throwaway namespace seeded+torn
down clean, Langfuse authenticated, 31 settle polls) and FAILED both lanes —
each failure a real defect on the trunk, filed:
- #581 fixture sends scope key `project`; provider _SCOPE_KEYS rejects it
  (2/3 scenarios can never pass), while the mcp2cli fallback ACCEPTS it —
  the two lanes disagree on scope vocabulary. Needs a vocabulary decision.
- #582 observation zero-emit: capture child exits 0, no watermark advance,
  ZERO traces reach Langfuse; reproduced under scrubbed env — the silent-
  zero-capture class with a deterministic repro command now. Blocks any #560
  measurement (this run says NOTHING about cost either way — no-data, not
  NULL-cost).
- #583 both tools swallow child stderr (root causes recovered only by hand-
  replay); secret_scan passes vacuously at observed=0 — should be
  skipped-no-data.
Also: gate needed AUTH_TOKEN_ADMIN (agent token lacks namespace delegation),
and --drive/--verify are mutually exclusive (brief said otherwise).
Receipt comment: issues/578#issuecomment-5195058353. #560 untouched, honestly.

THE POINT: first time the system's defects were found by a scripted gate
with receipts instead of by Rico dogfooding. #578 is doing its job before
it is even done.

## 20. Live-defect lanes round 1

- #583 → PR #584 (stderr surfacing + skipped-no-data secret scan), CI green,
  pre-fix failure proofs on all 6 new tests; the lane REMOVED its own
  unrequested 240-char bound when the limitation-language hook flagged it —
  redaction, not length, is the safety property. Review lane running.
- #581 research brief (posted on the issue, file:line cited): the scope
  vocabulary IS contract-defined (agent-context-pack-contract.md:99-105) and
  excludes project; project is a CONFIG key with a real lane-metadata home;
  the fallback's acceptance is ORIGINAL flat-param design, not drift; only
  the unmerged fixture ever put it in a scope object. So #581 is a
  conformance fix, not a decision: fixture fix + fail-fast fixture-scope
  guard dispatched (fix/581-fixture-scope-conformance).
- #582 Terra diagnosis is LIVE as codex background task task-msgd2rk9-1qk2v8
  (worker PID observed; worktree fix-582 already carries a validation commit).
  Not disturbed; polling via TaskGet.

## 21. #581 and #583 CLOSED — two of the three live-run defects, same day

- PR #584 merged (#583 closed): MEDIUM fixed by controller inline (secret_scan
  passed coupled to scanned data — traces.length > 0 — with an expectedTraces:0
  test proving a skip is fatal, not a certificate). Two LOWs recorded on the PR
  as follow-ups; the redaction-structure one is flagged for Rico (design call
  under the standing size-language rule; docs/sme/security.md:641 wants the
  leak test).
- PR #585 merged (#581 closed): fixture conformance to the contract-defined
  scope vocabulary + pre-zod fail-fast guard (zod strips unknown keys silently
  — the defect class itself). Review: approve, mutation-proven. Body gate
  needed controller-authored sections + empty-commit re-trigger (the edited
  event does not fire pr-body.yml — second occurrence, pattern confirmed).
- #582 (zero-emit) remains the open blocker for #560; Terra background task
  still working (worker live, first commit observed in its worktree).

## 22. #582 and #560 CLOSED — all three live-run defects gone, cost telemetry live

- Terra's diagnosis landed as PR #586 (fix/582-observation-zero-emit): root
  cause was the driver copying unrelated OPENBRAIN_* names into the Python
  child; load_capture_settings() rejected them, capture_stop_with() swallowed
  the error, child exited 0 — the silent-zero class again. Fix: fail-fast raw
  config validation, allowlist-built child env, sqlite3 -ifexists watermark
  fallback, verifier filtered to the emitter's open-brain-capture tag (the raw
  lane's session_start also spawns a server-side trace on the SAME session id),
  and public-API field names (model/totalPrice).
- Opposite-family review (Claude lane, adversarial + SME-injected): verdict
  request-changes, one MEDIUM — `??` on the env alias fallback never engages on
  the empty string the deployed `VAR="${VAR:-}"` wrapper shape emits (the #525
  class, recurring in TypeScript). All four adversarial probes cleared live
  (allowlist compared against every declared alias; tag filter ordered BEFORE
  the traces.length>0 coupling; -ifexists cannot launder a missing DB; the
  load-bearing server-trace-exclusion test mutation-proven red). Findings
  posted to the PR before fixing (pull/586#issuecomment-5196167497).
- Controller fix c50c66d (small-edit carve-out): ?? → || plus an
  empty-string-alias regression test, mutation-proven 1-fail under the old
  operator, 22/22 restored. CI green, merged squash a5a1274, #582 auto-closed.
  SME entry appended to docs/sme/gotcha-agent.md (?? over process.env checks).
- LIVE RECEIPT run on trunk a5a1274 from a clean _validation-runs worktree:
  drive PASS (capture exit 0, watermark emit proven offset 1446), verify PASS —
  1 trace, 4 observations, 3 generations, generation_metadata 3/3, total_cost
  3/3 NON-NULL, secret_scan clean with content_fields_present=true, settled in
  one poll. #560 closed with that receipt (fix chain: #568 merged 06:14Z,
  unblocked by #586 19:12Z). Receipt also posted on #578.
- Langfuse coverage inventory (read-only Explore lane, file:line receipts):
  every MCP tool call IS traced verbatim via the installMcpTracing
  registerTool wrap (132 sites), session_key joins client+server lanes; the
  gaps ranked by debugging value — (1) retrieval internals (candidates/scores/
  chosen) not traced anywhere and in NO open issue, (2) namespace VALUE never
  emitted (only namespace_source; AuthInfo has no namespace field), (3) no
  structured ob_session_events row ids in traces, (4) embedding/dream/distill/
  NATS paths structurally invisible to the registerTool seam. #569 owns the
  coverage decision, sequenced behind #561 masking. Items 2–3 are in no open
  issue's fix shape.
- Board: 53 open issues, 2 open PRs (#538, #539 — both Rico's read-through
  gate). Gate scoreboard for the day: 3 defects found by the scripted gate at
  breakfast, 3 closed by evening, each with a live receipt.

## 23. Dogfood on trunk, canon packs live for rico, two apply-path defects found

- Local dogfood redeployed to trunk a5a1274 via scripts/local-clone-deploy.sh
  with revision proof (pid 23876 → 51594, /health revision matches). Server-side
  tracing verified live end-to-end: a real capture (provider `capture` op)
  produced an append_session_event trace in Langfuse within seconds — session
  ID, userId, caller identity, scope, duration, verbatim args/result.
- New defect found by that verification, filed #587: `release` is null on 100%
  of deployed traces — the resolver shells `git rev-parse` but the deployed
  runtime is a gitless `git archive`; /health reads the deploy stamp and knows
  the revision on the same process. Fix shape: resolver reads the stamp first.
- PRs #538/#539 diagnosed for Rico: both reds were the PR-body gate (bodies
  predate the format) plus checks run during the broken-trunk window.
  Retrofitted compliant bodies (validated locally), updated both branches
  (539 needed a keep-both merge of two independent SME-file tail appends),
  all checks green. Rico merged-by-authorization; both squash-merged.
- #538 post-merge apply: rico namespace CLEAN — 10 profile + 27 process rules
  live (DB-verified via ob_session_lanes join). Repo-facts + fan-out BLOCKED
  by two defects found on first live apply, filed #588 with receipts:
  (1) `_plan_repo_fact` sends prose `entry.source` as metadata.path, which the
  server's sourceUrlMatchesSource can never validate against a URL;
  (2) namespace fan-out with admin token + OPENBRAIN_CANON_AGENT=skippy wrote
  27 rules into namespace ADMIN (token wins, delegation ignored, reconciler
  reports success without checking landing namespace) and nagatha/bilby then
  read that namespace and reported "no writes needed". 27 stray admin-namespace
  decision rows flagged for operator cleanup in #588. #522 stays open.
- Debug method note: the missing upsert_repo_fact trace in Langfuse (queried
  by name, zero results) was itself the tell that the call died client-side —
  the new tracing answered a debugging question on day one.

## 24. Tracking honesty + git hygiene sweep (operator escalation, evening)

- Operator: issues cite closed blockers and stay open ("419 is closed and done
  but the two things looking at it still aren't"); git hygiene "smells like a
  high school locker room"; the tracking scribe was mandated, again.
- Root causes named: (1) GitHub auto-closes issue←PR (closing keywords — 9 of
  12 of today's merged PRs carried them correctly) but NEVER issue←issue, so
  "blocked on #X" outlives its blocker silently; (2) squash-merge means branch
  tips are never ancestors of main, so `git branch --merged` reports nothing
  and branch debris looked undeletable forever — cleanup must key off PR state.
- NEW STANDING TOOL: scripts/stale-blockers.ts — flags every open issue whose
  referenced issues/PRs are all closed. First run: 11 candidates. Documented in
  _plans/README.md to run with sync-issues at close-run boundaries.
- Verification swarm over 9 candidates (wf_a47b0499-e02, 9/9 returned):
  ZERO closable — 6 PARTIAL, 2 NOT_DONE, 1 RICO_GATED (#563). Each issue got a
  verdict comment with what-actually-remains and its stale text corrected.
  Notables: #463 the rewrite RUNS locally (PID-verified, server/state.ts says
  cutover) but core01 still serves src/, package.json start still points at
  src/index.ts, and server/README.md still claims "scaffold only" — stale doc
  flagged; #492 Hermes adapter is _unbuilt() on trunk, RUNNING-verified.
- GIT HYGIENE SWEEP (PR-state-keyed): deleted 63 remote branches (62 merged-PR
  + 1 superseded closed-PR worklog branch) and 40 local branches/remnants;
  removed the 08-04 session worktree; pushed wip/2026-08-05-session. End state:
  local = main + wip/2026-08-05-session + archive/pre-rewrite-20260731 +
  salvage/pre-squash-unique-work-2026-08-02; remote = the same three durable
  refs; ONE worktree (the primary checkout). Found while sweeping: the
  pre-push hook runs full typecheck even for branch DELETIONS — batch deletes
  into one push (62 refspecs, one hook run) and keep node_modules current.
- SCRIBE IS RUNNING: durable cron 46c22a9c, 3×/day — sync-issues mirrors +
  stale-blockers sweep + aqmd up, mirror-only commits on the day's wip branch,
  report-only on candidates (head verifies, scribe never closes). Harness
  auto-expires recurring jobs after 7 days; renewal is a close-run-boundary
  checklist item.
- Board: 54 open. Gate defects found this morning: 3 filed, 2 closed same day.

## 25. Close-run 2 in flight — first two closures, review swarm caught five

- Wave 1 delivered 7 PRs (590 591 593 594 595 596 597). Review swarm (Claude,
  SME-injected, one failure-proof independently re-verified per PR): #596 and
  #597 approve; 590/591/593/594/595 request-changes with real HIGHs — 591's
  fix was SELF-PROVING (copies expected values into the candidate then asserts
  equality), 590's regression test injected readStamp so the production reader
  had zero coverage + a stale-stamp vector on the core01 deploy path, 593
  masked values but not sensitive KEYS and one detector deleted field names
  (reduction), 594 mapped header-path namespace to the wrong receipt field,
  595 halts the whole ingest on one NULL timestamp and silently ingests
  deactivated rows. All findings posted to PRs; five fix lanes running.
- MERGED+CLOSED: #596→#520 (king-* facts seeded, read-back verified),
  #597→#386 (qmd-sync LaunchAgent RUNNING: registered, exit 0, 55/55 indexes,
  watchdog group-kill mutation-proven). Board 54→52.
- #591 scope flag: PR says Fixes #529 but verdict items 3-4 remain — before
  merge either the keyword goes or the residue gets its own issue.
- Scribe incident recorded: another session works this checkout (branches
  fix/77, fix/81, PR #592, gate fixes in openbrain-provider); my mirror commit
  briefly landed on their branch via a pipe-masked failed git switch — reset
  to their exact tip, force-with-lease restored, mirrors recommitted on wip.
  Lesson: no pipes on control-path git commands; shared checkout is a hazard.
